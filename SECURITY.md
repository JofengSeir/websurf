# 安全政策

## 1. 支持范围

只支持最新版本（`main` 分支 / 最新发布）。本地构建产物（`pkg/`、`dist/`、`web/*.js`、`web/*.wasm`）不随仓库分发，不在支持范围内。

## 2. 与安全相关的运行时事实

- **无后端、无账号、无数据上传**：项目不含服务端组件，也不含任何密钥或凭据；地图与录像由用户从本地选择后在浏览器内解析。
- **共享内存通道与降级**：高性能通道依赖 `SharedArrayBuffer`（需 COOP/COEP 响应头，本地 `src/serve.py` 已注入，见 `src/serve.py:39-40`）；缺失时降级为消息回退，功能等价、性能下降。
- **地图与录像属不可信输入**：`.bsp`、`.replay` 可能包含任意第三方内容且不入库；解析器对畸形输入应安全失败，来源不明的文件建议在隔离环境中打开。

## 3. 报告漏洞

**请勿通过公开渠道（Issue、讨论区）披露漏洞**——请走 GitHub Security Advisories 私下报告，或直接联系维护者。报告请包含：受影响版本/提交、复现步骤与最小样例、运行环境（浏览器版本、是否启用 COOP/COEP）、影响评估。

## 4. 处理流程

确认收到 → 评估影响与严重程度 → 修复并补充验证 → 发布补丁版本。经报告者同意，可在 [CHANGELOG.md](CHANGELOG.md) 中记录致谢。
