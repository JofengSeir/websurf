# 安全政策

## 支持范围

仅最新版本（`main` 分支 / 最新发布版本）提供安全支持。仓库内的构建产物（`pkg/`、
`dist/`、`web/*.js`、wasm 文件）均为本地构建生成，不随仓库分发，不属于支持对象。

## 环境说明（与安全相关的运行时事实）

- 项目为纯前端 WASM/Three.js 应用，无后端、无用户数据上传；地图文件由用户本地选择。
- 高性能通道依赖 `SharedArrayBuffer`（需 COOP/COEP 响应头，本地 `src/serve.py`
  已注入）；无该头时自动降级为消息回退模式（`MsgState` / `TestShared msg-*`），
  功能等价、性能降级。
- 仓库不包含任何密钥/凭据；`*.bsp` 地图文件（可能包含任意第三方内容）不入库，
  从不可信来源加载的地图应在隔离环境运行。

## 报告漏洞

请勿在公开渠道（Issue、论坛等）披露安全漏洞。请通过 GitHub Security Advisories
私下报告，或直接联系维护者。

## 处理流程

- 确认漏洞后尽快响应
- 评估影响并修复
- 修复后发布补丁版本
