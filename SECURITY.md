# 安全政策

## 1. 支持范围

仅最新版本（`main` 分支 / 最新发布版本）提供安全支持。本地构建产物（`pkg/`、`dist/`、`web/*.js`、wasm 文件）不随仓库分发，不属于支持对象。

## 2. 与安全相关的运行时事实

- **无后端、无账号、无数据上传**：项目不含服务端组件，也不包含任何密钥或凭据；地图与录像均由用户从本地选择后在前端解析。
- **共享内存通道与降级**：高性能通道依赖 `SharedArrayBuffer`（需 COOP/COEP 响应头，本地 `src/serve.py` 已注入），缺失时自动降级为消息回退模式（`MsgState` / `TestShared` 的 `msg-*`），功能等价、性能降级。
- **地图与录像属不可信输入**：`.bsp`、`.replay` 可能包含任意第三方内容且不入库；解析器对畸形输入应安全失败，来源不可信的文件仍应在隔离环境中运行。

## 3. 报告漏洞

**请勿通过公开渠道（Issue、讨论区等）披露漏洞**——请走 GitHub Security Advisories 私下报告，或直接联系维护者。报告请包含：受影响版本 / 提交、复现步骤与最小样例、运行环境（浏览器版本、是否启用 COOP/COEP）、影响评估。

## 4. 处理流程

确认收到后尽快响应 → 评估影响与严重程度 → 修复并补充验证 → 发布补丁版本；经报告者同意，可在 [CHANGELOG.md](CHANGELOG.md) 中记录致谢。
