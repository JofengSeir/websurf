# 进度台账（自 AGENTS.md §7.1 原样移出）

> 本文件是 `AGENTS.md` §7.1「已完成 / 已发生」的**完整历史台账**：2026-09-22 逐行**原样移出、内容未改**。
> 移出原因：`AGENTS.md` 达 75 KB，超出工作区指令自动加载上限（65536 字节），其尾部「§7.3 待决项」会被静默截断。
> 此后规则：新进展**追加到本文件**；`AGENTS.md` 只保留规范（§1–§6）、当前状态（§7.2）与待决项（§7.3）。
> 本文件是**执行过程记录，不是源码事实来源**；任何文档/注释结论仍以当前源码与两篇控制文件为准。

---

## 已完成 / 已发生

| 日期 | 事项 | 产出 |
|---|---|---|
| 2026-09-22 | 立项：读码建立事实基线 | `documents/plan/project-survey.md`（含 §12 主流程与时序骨架、§13 必读源码清单） |
| 2026-09-22 | 编写任务书（源码唯一事实源版） | `documents/plan/doc-rewrite-taskbook.md`（B1/B2/B3 + A0 骨架 + 六步法 + 术语表 + WG 拆分） |
| 2026-09-22 | 旧文档清理（早期记录称"归档入 `.ak/`"） | 旧文档从工作区移除；`.gitignore` 增 `.ak/` 规则 |
| 2026-09-22 | 编写本规范 | 根 `AGENTS.md`（本文件） |
| 2026-09-22 | **更正早期记录**：`.ak/` 归档区实际**不存在**；旧文档的真实状态是"已从工作区删除、仅存 git 历史"，**没有可回读的归档副本**。控制文件实际位于 `documents/plan/`（非 `documents/`）。本文件的 §2、B2、控制文件链接已据此改写 | 本文件 |
| 2026-09-22 | **工况复核**：`test/dual-mode-harness/` 退役由 owner 定调（"这就是现有的工况要求…不要恢复"）；WG7 随之撤销 | 本文件 §7.2 |
| 2026-09-22 | 基线闸门复跑 | `cargo test -p websurf-phys` = **10 passed / 0 failed**（exit 0）；漂移体检 = 9 篇 md / 行数声明 0 / 锚点 76（**越界 0**）/ 路径失效 15（全部集中在 `project-survey.md`，指向已删旧文档）/ 歧义 13 |
| 2026-09-22 | P0 首件读码：`src/phys/mod.rs` 导出面实测 | `#[wasm_bindgen] impl PhysWorld` 实测 **24 个 `pub fn`**（含 `new`）；同文件头注称"导出 12 个 API" → A 类计数对撞，详见 §7.3 |
| 2026-09-22 | **WG1 首件完成**：`src/phys/mod.rs` 注释按源码重写 | 743 → 901 行（**620 行代码逐行不变**，仅注释改动）；A 类修正 9 处；S4 全绿：来源审查 0 命中、CRLF 901 / bare LF 0 / 无 BOM、`cargo test -p websurf-phys` 10 passed（exit 0）、全仓 `check-doc-drift` **0 越界 / 0 路径失效** |
| 2026-09-22 | 控制文件同步三工程工况 | `documents/plan/` 两篇：去 harness 范围、修正 `AGENTS.md` 相对链接、清零 15 条失效路径 → 全仓漂移体检 **路径失效 0**（原 15） |
| 2026-09-22 | **WG1 首件补正**：`mod.rs` 同文件行号锚点全部失效 | 评论增行使 36 处同文件 `:NNN` 锚点整体错位 → 全部改为符号引用（跨文件锚点保留）；**12/12 抽样开箱全中**。规则写入 §3 S3 | 本文件 §3 S3、§7.3 第 13 条 |
| 2026-09-22 | 三工程 typecheck 基线复跑 | `apps/game`、`apps/debug`、`apps/viewer` 的 `npm run typecheck` 均 **exit 0** |
| 2026-09-22 | **survey 协议与规模基线复测修正** | ① SAB 布局行两处 B 类错误：权威帧双缓冲误记「128-415」→ 实测 **128-287**（`B_A0=16`/`B_A1=26` 为 8B 槽位），渲染尾槽枚举漏 `RT_X/RT_Y/RT_Z/RT_T` → 补齐 8 个常量；② 证据锚点原指注释块 `:17-47` → 改指常量 `:119-221` 与视图 `:583-585`（C 类双跳）；③ §9 规模基线复测：`src/phys` 因首件由 3763/587/16% → **3946/755/19%**，其余五区逐项与实测一致 | `documents/plan/project-survey.md` §3.3、§9、§1 |
| 2026-09-22 | survey §1 表格缺陷修复 + 口径钉死 | 原「行数 = `Get-Content | Count`」单元格内裸 `|` 使管道数不符表头（渲染错列），且该串不是可执行命令 → 改为 `(Get-Content <文件> -Encoding UTF8).Count`；并记录陷阱：`Measure-Object -Line` **不计空行**（实测同文件 1033 vs 956，差 7%） | `documents/plan/project-survey.md` §1、§9 |
| 2026-09-22 | WG2 骨架与预取证落盘 | `.tmp/wg2/skeleton.md`：`src/wasm-core` 8 模块 / 26 文件边界、**3 条 A 类**（`lib.rs` 称「26 lump」实为 `[LumpEntry; 64]`；模块说明只列 6 个而实际 8 个 `pub mod`；来源沿革叙述）、开工顺序 9 步 | `.tmp/wg2/skeleton.md` |
| 2026-09-22 | **WG2 前 3 件完成**（主控自做） | `src/wasm-core/lib.rs`（A 类 3 条：26→**64** lump、模块 6→**8**、来源沿革改职责描述）、`vbsp/bspfile.rs`（去「实证 2026-08-14」与「可能/未实现」推测、去同文件锚点、补模块头与 6 处条目文档）、`vbsp/reader.rs`（去「【修复】/此前」历史叙述、保留 vis 偏移基准与 leaves 双记录大小的不变量、补模块头）。三件 S4 全绿：非注释行逐行一致（9 / 128 / 110）、来源审查 0 命中、CRLF 无 bareLF 无 BOM、`cargo check -p websurf-wasm-core` exit 0 | `src/wasm-core/lib.rs`、`src/wasm-core/vbsp/bspfile.rs`、`src/wasm-core/vbsp/reader.rs` |
| 2026-09-22 | **查清 WG2 可用门**（重要） | `cargo test -p websurf-wasm-core` 在本机**不可用**：`windows-sys` / `getrandom` 宿主构建缺 `dlltool.exe`，exit 101（工具链缺口，非代码问题）。可用门为 `cargo check -p websurf-wasm-core`（exit 0）与 `cargo check -p websurf-wasm-core --target wasm32-unknown-unknown`（exit 0，已装该 target） | 实测 |
| 2026-09-22 | **WG1 全部完成并通过主控复验（7/7）** | 7 个文件全部：**非注释非空行与 HEAD 逐行一致**（569 / 836 / 637 / 286 / 252 / 76 / 226 行）、来源审查 **0 命中**、CRLF 且 bareLF 0 且无 BOM；`cargo test -p websurf-phys` = **10 passed / 0 failed / exit 0**；**跨文件锚点 33 个（38 处引用）越界 0、缺失文件 0、自引用 0**，随机抽样 14 个开箱全部指向所述内容 | `src/phys/**`（7 件） |
| 2026-09-22 | **WG2 增至 5/26 + 发现「外部参照实现」引用规则** | 新增完成 `vbsp/error.rs`（补模块头 + 5 个子错误类型 + 两个 `From` 实现文档；A 类：点明 `From<binrw::Error>` 对无法识别的自定义错误**走 panic**）、`vhv.rs`（去 4 处**指向仓库外**的 `file:line` 引用——第三方参考工程的源码路径，本仓内不可打开；把"两级来源"改为本仓两条通路的代码事实；补 `luma_stats` 返回语义与 `parse_vhv` 拒绝条件）。两件 S4 全绿（144/124 行代码一致、0 命中、`cargo check` exit 0） | `src/wasm-core/vbsp/error.rs`、`src/wasm-core/vhv.rs` |
| 2026-09-22 | **确立「外部参照实现」处理规则**（已下发给 3 个 WG2 子代理） | `外部参照实现` 是本仓对**第三方参考实现**的既有称法（`bsp_to_gltf_core/lightmap.rs` 载有 MIT + 版权署名），**不是**已删文档。规则：① **保留**署名/许可/来源陈述；② **删除**指向该外部工程内部路径的 `file:line`（无法打开、必然失效），改用本仓自己的常量/断言/分支作证据；③ 不得新增外部引用 | 已在 `vhv.rs` 落地；`vbsp/mod.rs`、`vbsp/data/game.rs`、`lightmap.rs` 均含此类引用，后续一律按此处理 |
| 2026-09-22 | **WG2 增至 7/26**（主控自做）+ 全量并行铺开 | 新增完成 `mosaic/mod.rs`（10 → 26 行：去 `materials-mini` / `materials-test` 来源沿革，改为 4 个子模块职责 + 上下游调用点；确认 **viewer 不引用 mosaic**）、`mosaic/manifest.rs`（去"与 TextureCollector 一致"未验证断言；写清两条互补输出与"单张失败只跳过"的容错语义；点明键取自**加载后** `texture.name` 而非传入材质名）。两件 S4 全绿（4 / 59 行代码一致、0 命中、`cargo check` exit 0） | `src/wasm-core/mosaic/mod.rs`、`src/wasm-core/mosaic/manifest.rs` |
| 2026-09-22 | **WG3 首件完成**：`src/ts-shared/phys/constants.ts` | C 类修正 2 条：① 头注引用**已删文档** `framework-decoupling §8.1` → 改为本仓 `src/scripts/check-shared-sync.mjs` 的 `eye-stand` 子检查（该脚本确实用正则抓 Rust/TS 两侧常量逐位比较）；② 去掉内部编号 `D-16` / `E-06` 与「原 7 处字面量」的沿革叙述，改为**实测消费方清单**（decoupled-loop / tick-consumer / shared-state.protocol.test / game renderer-main / viewer constants 再导出 / `phys-smoke.mjs`）。**新发现真实耦合**：`apps/game/scripts/phys-smoke.mjs:123` 用 `/export const EYE_STAND = ([\d.]+);/` **直接正则解析本文件源码**取值，故导出行写法是被脚本依赖的接口。S4：代码行 1/1 一致、0 命中、CRLF 26 / bareLF 0 / 无 BOM | `src/ts-shared/phys/constants.ts` |
| 2026-09-22 | **WG3 增至 3/20**（主控自做） | 新增完成 `input/input-layer.ts`（去「公共化 v1」与"由 game/debug 两端收敛"沿革；写清两条不可混用的口径：真实鼠标**乘** sensitivity、Q/E **不乘**；把"物理侧 sensitivity 固定 1"的依据落到 `params.ts` 的 `buildPhysicsParams` 写死 `1`；227px 单帧量补上 144Hz 前提）、`phys/angles.ts`（去 **已删文档** `framework-decoupling §2.3` 与内部档位 `D-08 / A′`；去 4 份副本的沿革；保留 `-0 → +0` 归一约定与其对等值/排序无影响的说明；去「旧式 (270−yaw) 已废弃」历史，保留 +180 的置换推导）。S4：代码行 18/18、6/6 一致、0 命中、CRLF 48 / 30、无 BOM；`apps/game` typecheck exit 0 | `src/ts-shared/input/input-layer.ts`、`src/ts-shared/phys/angles.ts` |
| 2026-09-22 | **WG2 复验：16/26 通过**（含 7 个子代理产出） | 全量复验（改进后的方法：**同时**做「严格过滤」与「剥离行尾注释后过滤」两遍比对 + 参照集非空断言 + 越界/自引用扫描 + 禁用词 + 行尾空白 + CRLF/BOM）。结果：16 件全部 `code=OK`、禁用词 0、行尾空白 0、bareLF 0、无 BOM、`cargo check` exit 0。**改进点**：原方法把「行尾注释」当代码行，导致 `phyfile.rs` 误报 `DIFF=6`；补做「剥离首个 `//` 之后内容」的第二遍比对后确认代码逐行一致（236/236） | `src/wasm-core/**` 16 件 |
| 2026-09-22 | **⚠ 自伤事故与恢复（必读）** | 为批量修禁用词写了 PowerShell 脚本，用哈希表存 `@(@('旧','新'), ...)` 配对。**PowerShell 把嵌套数组摊平**，`$fixes[$path]` 返回的是**字符串**，于是 `$p[0]`/`$p[1]` 取到的是该字符串的**第 0/1 个字符**——实际执行成了「把所有 `：` 换成 `V`」「把所有 `新` 换成 `建`」「把所有 `地` 换成 `图`」。受损 3 件：`pakfile_models.rs`（`: `→`V` 35 处）、`bsp_to_gltf_core/gltf_builder.rs`（`新`→`建` 3 处）、`bsp_to_gltf_core/lightmap.rs`（`地`→`图` 9 处）。**判定**：剥离注释后比对确认**代码零损伤**，损坏仅在注释文本；但字符级替换无法可靠逆向（`V`/`建`/`图` 都有合法出现），故**三件整体 `git checkout HEAD` 还原**，其注释重写重做。`cargo check` 还原后 exit 0 | 已恢复；规则见 §7.3 第 18 条 |
| 2026-09-22 | **禁用词模式修正（避免假阳性）** | `archive` 作为裸子串会命中**合法类型名** `ZipArchive`（`phyfile.rs`、`vbsp/data/mod.rs` 共 7 处误报）。判定禁用词时须用 `(?<![A-Za-z])archive(?![A-Za-z])`，即只匹配独立出现的 `archive`（指已删归档区），不匹配标识符片段 | 已修正判据 |
| 2026-09-22 | **本轮收尾闸门复跑（全绿）** | `node src/scripts/check-doc-drift.mjs` = 9 篇 md / 行数声明 **0 漂移** / 锚点 76 **越界 0** / **路径失效 0** / 歧义 13（裸文件名，非错误）；`cargo test -p websurf-phys` = **exit 0**；`cargo check -p websurf-wasm-core` = **exit 0**。本文件行尾复核：CRLF 184 / bareLF 0 / bareCR 0 / 无 BOM | 实测 |
| 2026-09-22 | **全部 7 个 WG2 子代理已停止** | 其中 4 个跑满 6 轮仍未产出文件（目标是最大件：`lightmap`+`convert`、`model_integrator`、`vbsp/data/{mod,entity,game}`、`mosaic/mtz`+codecs），2 个产出的文件存在**写入竞态**（我的修复被其后续写入覆盖）。结论：大文件改由主控分批自做，不再整批外包 | — |
| 2026-09-22 | **WG2 增至 18/26**（主控自做）：`mosaic/encode.rs`、`mosaic/decode.rs` | `encode.rs`：去「提取自 materials-mini/img2code.rs」（**该目录在当前工作区不存在**）与「main 逻辑函数化」沿革，改为五步编码流程 + 三处调用方；点明 `name` 只影响文本（`code_to_img` 解析后 `let _ = name;` 丢弃）；点明 `T[` 写出门限 `< 250` 与可见格格 alpha 必 ≥128 ⇒ 实际值域 128..=249。`decode.rs`：A 类修正「查自取样板」——调色板来自字节码 `C[`，**不查任何外部样板**；点明缺 `R[` 时索引全 0（整幅取首色）、缺 `A[` 时全格取 `opacity`；点明与 `mtz` 的 `render_bytecode` 字段顺序相同但 mtz 多一个 `G[签名]`。两件 S4 全绿：**代码行两遍比对逐行一致**（156/156、133/133）、禁用词 0、禁用路径 0、行尾空白 0、CRLF 226/178 无 bareLF 无 BOM、`cargo check -p websurf-wasm-core` exit 0 | `src/wasm-core/mosaic/encode.rs`、`src/wasm-core/mosaic/decode.rs` |
| 2026-09-22 | **建锚点扫描器 + 修 2 处真实锚点违规** | 新工具 `.tmp/tools/anchor-scan.mjs`（`check-doc-drift.mjs` **只扫 md**，不覆盖代码注释锚点）：全路径锚点缺失/越界/自引用判违规，简写锚点与裸行号仅作信息。首次全量扫 56 个已重编文件（锚点 173 处）：① **真实腐烂 1 处**——`src/phys/teleport.rs` 引 `src/ts-shared/phys/angles.ts` 的 `bspYawToCsYaw`（HEAD 版位于该文件第 36 行），但 WG3 重写 `angles.ts` 后文件由 38 行缩到 30 行 ⇒ 越界；② 路径形态错 2 处——`crates/wasm/src/lib.rs:22`（应用内相对路径，非仓库根）、`vbsp/bspfile.rs:109`（少 `src/wasm-core/` 前缀）；③ 顺带查出 `src/phys/mod.rs` 原句「各自 `:22`、`:26` 做 `pub use`」与实际不符（game 的 `:22` 是 `use`、`:26` 才是 `pub use`；debug 的 `pub use` 在 `:22`）。三处均改为**符号引用**，同时新增「`Cargo.toml` 以 `path = "../../../../src"` 指向本目录」的实测事实。修后复扫：**违规 0**；三件代码两遍比对逐行一致（569/286/762），CRLF 903/546/1119 无 bareLF 无 BOM | `.tmp/tools/anchor-scan.mjs`、`src/phys/mod.rs`、`src/phys/teleport.rs`、`src/wasm-core/vbsp/mod.rs` |
| 2026-09-22 | **工况事实复核：不存在「工程内隔离副本」** | 实测：`apps/*/crates/` 下**只有 `wasm`**，无 `wasm-core`；`bspfile.rs`/`mtz.rs`/`vhv.rs`/`phyfile.rs` 全仓各只有 `src/wasm-core/` 一份；三个工程的 `crates/wasm/Cargo.toml` 均为 `websurf-wasm-core = { path = "../../../../src/wasm-core" }` 且注释已写「共享解析层」。故 `apps/game/crates/wasm/src/lib.rs` 头注「本工程内的隔离副本 crates/wasm-core（仓库根…的逐字节副本）」**与当前代码不符**，且其引用的 `documents/game/implementation/lighting-merge-plan.md §9.1/§6.2` 属已删文档。→ `apps/game` 的唯一事实是**与 debug/viewer 共用同一份 `src/wasm-core`** | 见 §7.3 第 21 条 |
| 2026-09-22 | **WG2 增至 19/26**：`pakfile_models.rs`（主控自做）+ `bsp_to_gltf_core/gltf_builder.rs`（子代理） | `pakfile_models.rs`：**A 类 1 条**——旧注称 `include` 会「去掉 `materials/` 前缀」，代码实际只 `trim_matches('/')` + 去 `.vmt` 后缀（前缀补全由 `PakIndex::find` 的候选列表负责）；另修正 2 个**陈旧符号名**：`model-integrator::parse_angles` → 实测为 `parse_angles_str`、`SOLID_NONE` → 本仓枚举是 `SolidType::None`；把「碰撞门控」整段移出本模块（它在 `export_model_tri_colliders` 里是 `if alpha == 1 { continue; }`，本模块零门控代码）；修掉 HEAD 里三行注释被并成一行的问题。`gltf_builder.rs`：A 类 9 条（去「2026-09-20 实机 / 28.2% / surf_666 219-12-10-207」等无锚点历史数字；写清 `(translucent, alpha_test)` 的三分支优先级与 **translucent 时 alpha_test 被丢弃**；`$alphatest`→0.5 的说法改为 `MaterialData::alpha_test`；线框 extras 改为本仓两端事实；两条查表补「大小写敏感逐字符相等 + `find_map` 取首个」；`byte_length` 在 `pad_byte_vector` 之前取；补模块头六项）。两件**代码逐行一致**（226/226、216/216）、禁用词 0、CRLF 无 BOM、`cargo check` exit 0；主控独立抽查 `gltf_builder.rs` 的 7 条事实断言**全部命中**（常量 32/0.01、三分支、`_paths` 未被读、`mod gltf_builder;` 私有 + `#![allow(dead_code)]`、`==` 逐字符比较、`vbsp_wireframe` 两端、`byte_length` 顺序） | `src/wasm-core/pakfile_models.rs`、`src/wasm-core/bsp_to_gltf_core/gltf_builder.rs` |
| 2026-09-22 | **WG2 增至 21/26**：`texture_utils/vtf.rs`（子代理）+ `texture_utils/mod.rs` 假断言修正（主控） | `vtf.rs`：410 → 640 行，注释行 3 → 233；**代码两遍比对 343/343 与 407/407 均 d0**；删除 3 处「并入自 `header.rs` / `resources.rs` / `utils.rs`」的来源括注（**这三个文件在本工作区不存在**；`texture_utils/` 实测只有 `mod.rs`/`image.rs`/`vtf.rs`），改为按本文件符号的内容型标题；上报 4 条疑似缺陷（见 §7.3 第 27 条）。`mod.rs`：由子代理发现的**跨文件假断言**——原文称 `bsp_to_gltf_core::materials`「走同一条解码路径」，实测 `materials.rs` 用的是**外部 `vtf` crate**（`src/wasm-core/Cargo.toml` 的 `vtf = "0.3"`，调用形如 `vtf::vtf::VTF::read`），且 `materials.rs` 全文不含 `texture_utils` ⇒ 已按代码改写 | `src/wasm-core/texture_utils/vtf.rs`、`src/wasm-core/texture_utils/mod.rs` |
| 2026-09-22 | **新增复用校验工具 `.tmp/tools/verify.ps1` + 负例自测** | 一条命令完成单/多文件复验：参照集非空断言（`git show HEAD:<正斜杠>`）→ 两遍代码比对（strict / stripTrail）→ 行尾空白 → CRLF/bareLF/bareCR/BOM → 禁用词 → 禁用路径 → 外部锚点计数；任一不过 exit 1。**负例已实测**：对未入库的 `documents/plan/project-survey.md` 正确报「参照集为空」并 exit 1（证明断言真的会拦）。**踩坑**：脚本含中文，被 Windows PowerShell 5.1 按 ANSI 解码而整片乱码报语法错 ⇒ **含中文的 `.ps1` 必须带 UTF-8 BOM**；且本机 PATH 里**没有 `pwsh`**，只能用 `& <路径>` 调用 | `.tmp/tools/verify.ps1`；见 §7.3 第 23 条 |
| 2026-09-22 | **清 `AGENTS.md` 自身的 2 处越界锚点** | 我自己在 §7.1/§7.3 记述锚点腐烂事件时把那处失效锚点原样抄了一遍，于是该文件自己成了「锚点越界」的来源（漂移体检 exit 1，报 `AGENTS.md:133`）。改为符号引用后复跑：**锚点 79 / 越界 0 / 路径失效 0 / 行数声明 0 漂移** | `AGENTS.md`；漂移体检实测 |
| 2026-09-22 | **WG2 增至 22/26**：`vbsp/data/game.rs`（主控自做，434 → 527 行） | **A 类 1 条（重要）**：旧注称 V11「与 V10 的差异——angles 为 3×f32（12B，非 QAngle 6B）」**是错的**——`Angles` 在 `src/wasm-core/vbsp/data/mod.rs` 只有**一个**定义（`pitch`/`yaw`/`roll` 三个 `f32`，派生 `BinRead`），V6/V7/V10/V11 读的都是 **12 字节**，四版无此差异；已按代码写明。**C 类 4 处**：删「实测 `ze_cursed_bear_tales` 2026-08-14」的日期与地图名；删外部参照实现的 4 处工程内路径（`Structures.cs`、`StudioModel.ts`、`BspModel.ts`、`Utils.ts`，均在第三方工程里、本仓打不开）与外部符号 `sampleAmbientCube`。**B 类 1 处**：删 `decode_linear_ambient` 的「同图实测 873~1362× / 3.4~5.3×」无锚点数字，改为可核对的口径事实（与 `decode_linear` **只差 255 因子**、是调用点的口径选择；唯一读点 `prop_ambient_cube`，出口乘 `AMBIENT_SCALE`＝1.0）。**补齐**：原文件**无** `//!` 模块头 → 补模块头 + 6 项（定位/上下游/不变量/边界/测试归属）；`GameLumpHeader::find` 补**三态**返回语义；补「压缩条目用**下一条目**的 `offset` 定长 ⇒ 排在目录最后一项必报 `GameLumpOutOfBounds`」；补 V6/V10/V11 的字段顺序差异与按字段宽度累加（V10 = 72 B、V11 = 80 B）；补 `SolidType::None = 0` 的**实测消费点**（三工程 `crates/wasm/src/lib.rs` 的 `filter(\|p\| p.solid != Some(0))`）；三个 `From` 与 `const_assert_eq!` 各补文档。S4：**代码 367/367 两遍比对 d0**、禁用词 0、CRLF 527 无 BOM、`cargo check` exit 0、锚点扫描 0 违规 | `src/wasm-core/vbsp/data/game.rs` |
| 2026-09-22 | **WG2 增至 23/26**：`bsp_to_gltf_core/lightmap.rs`（主控自做，832 → 864 行） | 本件是**引用已删文档最密集**的一处：删 `documents/game/implementation/console-fix-contract.md` 的 §4.2/§4.3、`契约 §2.1/§4.2 第 1/2/4 条/§9.6.1`、`阶段 2`、内部编号 `③/④`、`计划 §2.1 第 3 条` 等全部失效引用。**C 类 11 处**：删注释里指向**外部参照实现**内部路径的 `file:line`（`Structures.cs` / `Lightmap.cs` / `LightmapLayout.cs` / `Geometry.cs`），改用本仓符号（`build_atlas` / `lightmap_uv` / `size_width` / `check_face_luxel_size` / 各常量）作落点；**保留** MIT + `Copyright (c) 2016 James King` 署名与「口径照抄外部参照实现」的陈述。**B 类 4 处**：删依赖 gitignore 夹具的实测数字（`surf_null`/`surf_666`/`ze_cursed` 的字节数、`28.2%`、`8732`、`port-verifier` 叙述、`本地语料触发不了` 断言）。**A 类 1 处**：`max_luxel_side` 旧注「本地语料 8732 < 上限 256」自相矛盾——`check_face_luxel_size` 先于该字段赋值执行，故其值**必 ≤ 256**，已按代码写明。另修正 `apps/debug lightmap-shader.ts:85-89` 这处**应用内不成路径**的锚点 → `apps/debug/src/renderer/lightmap-shader.ts` 的 `loadLightmapAtlas`（三工程各一份同构副本）。S4：**代码 630/630 两遍比对 d0**、禁用词 0、CRLF 864 无 BOM、`cargo check` exit 0、锚点扫描 0 违规 | `src/wasm-core/bsp_to_gltf_core/lightmap.rs` |
| 2026-09-22 | **WG2 增至 24/26**：`mosaic/mtz.rs`（子代理） | **A 类 5 条**：① 模块头与 `MAGIC` 的 doc 称容器是 **MTZ5**，而常量值是 `b"MTZ6"`（工作区 `src/materials/textures.mtz` 前 4 字节实测 `MTZ6`）；② `MAGIC` 上**叠了两条互相矛盾**的 doc（rustdoc 会两条都挂）；③ `render_bytecode` 称「与 `encode.rs` 的 `img_to_code` 一致」，实际多写一个 `G[签名]` 字段；④ `compress_json` 称「每区独立试 LZ」，实际 `R_META` 走 `continue` 从不试；⑤ `pack_regions` 只含糊说「定长小记录」，实测 8 B 且字段顺序为 `name_len(u16 LE)/w/h/色数/sig 长度/alpha 标志/opacity`。**补写**：6 个 `#[test]` 全改为「实际 assert 了什么」（函数体与 assert **一字未动**）；删 2 条死引用（`materials-mini/src/lib.rs` 目录不存在、`extract_all_json` 符号不存在）。**复验（主控独立跑）**：`stripTrail 797/797 d0`、`strict d8` 经**新判据**自动判定为**4 处行尾注释改写**（代码部分逐字节相同，非代码改动）、CRLF 1098 无 bareLF 无 BOM、禁用词/禁用路径/外部锚 0、锚点扫描 **0 违规**、`cargo check` exit 0；`git diff --numstat` = `248 60` 与报告一致 | `src/wasm-core/mosaic/mtz.rs` |
| 2026-09-22 | **WG2 增至 25/26**：`vbsp/data/entity.rs`（子代理；**本件是从零补写**） | **前提复核（主控独立确认）**：该文件在 HEAD 版**纯注释行 = 0**（唯一以 `*` 开头的是代码行），故不是「改写注释」而是「从零补写 445 行注释」——B1「禁沿用旧注释」无对象，也不存在可删的已删文档/外部引用。**结构零改动的最强证据**：`git diff --numstat` = **`446 0`（0 删除）**，即每一行原有内容都在原位（446 = 445 条注释 + 1 条模块头后的分隔空行）。**计数断言全部实测后写入**：45 个变体（44 带 `rename` classname 标签 + 1 个 `#[serde(skip)] Unknown`）、23 处 `#[serde(borrow)]`、36 个 `typed` 内结构体、5 个 `EntityProp` 实现、6 个 `FromStrProp` 实现、26 处 `bool_from_int`、0 个 `#[test]`。**跨文件断言 grep 反查 9 组**（`reader.rs` 的 `read_entities`、`data/mod.rs` 的重导出链、`convert.rs` 的 `bsp_models` 匹配面、`Cargo.toml` 的 `vdf-reader`、三工程 `bsp.entities.iter()` 命中数等）。**复验（主控独立跑）**：`strict/stripTrail 833/833 均 d0`、CRLF 1361 无 BOM、禁用词 0、锚点扫描 0 违规（注释内**零行号**）、`cargo check` exit 0 | `src/wasm-core/vbsp/data/entity.rs` |
| 2026-09-22 | **WG2 完成 26/26**：`bsp_to_gltf_core/convert.rs`（主控自做，1170 → 1218 行） | **A 类 2 条**：① 旧 doc 里的 `[`merge_gltf_structures`]` 是**坏 intra-doc 链接**——该符号在本仓**不存在**；线上合并路径实为 `model_integrator` 的 `ModelIntegrator::add_models_to_gltf`（`export_bsp_with_models` 里实测调它）。② 三份合并实现（`merge_model_into_root` / `create_new_gltf_structure` / `merge_gltf_structures_improved`）本仓**零调用点**（各带 `#[allow(dead_code)]`，合计约 500 行死代码），而旧注用「# 保留原因」把保留理由写成"算法参考 / 性能基准 / 教学参考 / 退化回退"等**无锚点推测**。**B/C 类**：删 12 处「阶段 0 / 阶段 2」分期标签（来自已删计划）与 1 处指向**已删文档** `console-fix-contract.md §3.1 ④` 的引用；删「原实现 `if let Ok(...)` 会把 Err 吞掉…改为 `?` 传播」这类**变更史叙述**，改写为当前不变量。**补齐**：模块头从 `//! 转换模块` 一句扩成六项（两入口 / `Arc<Bsp>` 不消费语义 / 装配顺序 / 3 条不变量 / 边界 / 测试归属 / 死代码说明）；`map_coords` 补「`[x,y,z]→[y,z,x]` 循环置换、行列式 +1」；`pad_byte_vector` 补「长度须在调用前取」；`bsp_models` 补「只认 4 种 brush 实体、世界模型排最后、下标不合法静默丢弃」；`push_bsp_model_bsp` 补「只取 visible face、`face_index = first_face + 序号`」；`push_bsp_face_bsp` 补属性语义表与 extras 形状；`bounding_box` 补空输入退化框；`BspVertexData` 补 stride 与私有字段。S4：**代码 879/879 两遍 d0**、禁用词 0、CRLF 1218 无 BOM、行尾空白 58/基线 58、`cargo check` exit 0、锚点扫描 0 违规 | `src/wasm-core/bsp_to_gltf_core/convert.rs` |
| 2026-09-22 | **WG3 增至 11/19**（主控自做 7 件） | `wasm/loader.ts`：**A 类 2 条**——① 三工程 pkg 名实测 **debug 与 game 同名**（产物 `websurf_wasm.*`、包名 `websurf-wasm`），只有 viewer 是 `websurf_viewer_wasm.*`/`websurf-viewer-wasm`，旧注「三工程 pkg 名互异」及其所举 `websurf_test_wasm`（属已退役 harness）均错；② `base64ToBytes` 调用点实测 **8 处 / 7 文件**（共享层 3：worker-dispatch/world-builder/pvs-manager；三工程 4：debug 的 main-wasm 与 default-pack、game 的 renderer-main、viewer 的 bsp 两处），旧注「三工程 6 + 共享层 2」的构成错；并把 `__VBSP_WASM_B64__` 的写入点落到 `src/scripts/lib/dist-pack.mjs` 的 `writeEmbeddedPreamble`（三工程 build-dist 均导入）与 viewer 自写的 `dist/wasm-embedded.js`。`world/types.ts` + `world/pvs-manager.ts`：删内部编号 `D-07/D-09/D-10` 与 numstat 沿革；**A 类 1 条**——`hasPvs` 只在 `clusterCount > 0` 且 base64 非空时为真，而 Rust 侧 `clusterCount > 0` 但 `pvs_offsets` 为空时位图保持全 0、base64 仍非空 ⇒ 旧注「地图无 PVS 数据时为 false」不成立；另记实测消费面：`getFaceCluster` / `visibleClusterCount` **零调用点**、debug 侧**从不调 `update`**（其 `currentClusterId` 恒 -1、`visibleCount` 恒 0）、`rootNode` 字段 TS 侧无消费点。`input/mouse-buffer.ts`：**A 类 1 条**——旧头注称「主线程 buffer 累积、每 8ms drain 发 Worker」，实测线上路径是 `process()`（两工程 `mousemove` 监听器），`push`/`drain` **零调用点**；改写为两条并列路径 + `MAX_DELTA` 与 `INPUT_CLAMP` 的两段钳制关系。`input/pointer-lock.ts`：删 Three.js PR 号 / Chromium bug 号 / `innerWidth/2.3` 等**仓库外不可核对**的引用与量级数字，改写为三条竞争路径 + 3000ms 兜底 + `settled` 幂等的代码事实。`tick/ordering-gate.ts`：删全部已删文档分节（`§8.1/§8.4/§8.5`）与内部编号（`F4-C`/`t2`/`t3-memo`/`t6`/`SG-*`/`终榜`），改写为「静态约束 δ+ε_max ≤ T + 运行时三值裁决 + i32 回绕 + 每次调用恰命中一个计数器」。`auth/compute-mode.ts`：**A 类 2 条**——① `resolveAuthTickRate` **全仓零调用点**（旧注称其为 `getConfigTickRate` 的唯一实现源；实测两工程都写作 `() => config.physics.tickRate`，权威步长 = `1/面板值`、**无 +3 偏移**）；② `apps/**` 内无 `compute-mode` 的 import、无 `set-mode` 发送方、未注入 `getComputeMode`/`onSetMode` ⇒ 共享层缺省恒落 `'coupled'`，本文件的三模式接线在生产中未启用。7 件 S4 全绿（`stripTrail` 全 d0） | 7 件 |
| 2026-09-22 | **WG3 增至 17/19**：6 件大文件由子代理产出、**主控逐件独立复验**（6 个子代理均在交付报告前失败，文件已落盘） | 6 件与行数：`phys/authority-calibrator.ts` 778、`auth/tick-authority.ts` 705、`auth/worker-dispatch.ts` 546、`auth/auth-loop.ts` 583、`tick/tick-consumer.ts` 558、`decoupled/decoupled-loop.ts` 558。**代码同一性**：`stripTrail` 全部 **d0**（300 / 428 / 320 / 315 / 314 / 304 行）；`strict` 的 d4/d24/d44/d22/d18 经判据自动确认为**行尾注释改写**；CRLF 无 bareLF 无 BOM、禁用词 0、**禁用路径 0**（`decoupled-loop.ts` 的 6 处 `worker-a.ts` 死引用与 `worker-dispatch.ts` 的 2 处 harness 注入方引用**全部清除**，§7.3 #29 结案）、锚点扫描 0 违规、三工程 `npm run typecheck` exit 0。**主控补做内容审查**（子代理未交报告，按 §7.3 #25 补强措施逐件抽 ≥3 条跨文件断言 grep 反查）：`createTickAuthority` 全仓只有单测调用、`apps/**` 内 `tickF4` **零命中**、`seed_from` 外部调用只有 `apps/game/scripts/phys-seed-smoke.mjs`、`AUTH_EVT_OPT = 1 << 8`、`state_out` 视图 22 槽（tick-authority）vs 8 槽（decoupled-loop）、`createDecoupledLoop` 零装配点且 `decoupledLoop` 环境槽无人填充、`takeInput(maxStep)` 饱和截断 vs `consumeInput()` CAS 不限幅、`phys-event` 两端 app.ts 转发到 `applyCollisionCorrection`、`src/phys/player.rs` 的 `check_jump` 以 `if !p.on_ground` 开头、`onSyncRenderState` 两端各自 `postMessage`、`test:auth-clock` / `test:jump-apex` 两个 esbuild 门存在、`jump-apex-serve.mjs` 的切片锚点 `if (sm.teleport === false) {` 仍唯一 —— **全部命中**。主控修正 2 处措辞精度（`seed_from` 补记单测桩调用；回放守卫标明为 debug 独有） | 6 件 |
| 2026-09-22 | **⚠ 块注释提前闭合事故（新失败模式）+ 校验工具升级为 13 项** | `src/ts-shared/tick/tick-consumer.ts` 模块头里写了 glob `src/ts-shared/**/*.ts`，其中 `**/` 含 `*/`，**提前闭合了块注释**，其后约 500 行注释文本被当作代码解析：`tsc` 报 40+ 条语法错并以 `TS1160 Unterminated template literal` 收尾（`apps/game npm run typecheck` **exit 2**）。**关键教训**：这类错误对 `verify.ps1` 的代码同判据**完全不可见**（那些行仍以 `*` 开头被当作注释跳过，`stripTrail` 照样 d0）——**只有类型检查/编译才会暴露**。处置：① 改写该行去掉 glob；② 给 `verify.ps1` 增第 13 项「**块注释提前闭合**」（注释续行以 `*` 开头、非独立 `*/` 行且含非行尾 `*/` ⇒ 计失败）；③ **负例实测**：把 glob 临时插回 ⇒ 判 `块注释提前闭合=1` 且 exit 1（**同时代码同判据仍 d0**，证明新项确实补上了旧判据的盲区），还原后哈希一致、`=0` 且 exit 0；④ 全量重扫 62 个已改动文件，仅此 1 处 | `.tmp/tools/verify.ps1`、`src/ts-shared/tick/tick-consumer.ts` |
| 2026-09-22 | **WG3 非测试件全部完成（19/19）**：本件 `auth/shared-state.ts`（1033 → 1032 行，主控自做） | **B 类 1 条（与 survey 同源）**：模块头写「权威帧双缓冲字节 **128-415**」，实测 `B_A0=16`/`B_A1=26` 各 10 值 ⇒ 权威帧占 **字节 128-287**；415 属解耦帧区（288-447）。**A 类 1 条**：「固定 64Hz tick」不成立——`auth-loop` 的 `fixedDt` 初值 `1/64` 但由 `setFixedDt` 改写，两工程都传面板 `tickRate`。**C 类**：删 8 处已删文档/内部编号（`phys-mode-port §3.3/§3.4.B`、`t3-memo §2.5/§13.1`、`t6-render-ahead §8.1`、`t2 裁定书②`、`t4-acceptance §1.3`、`t2-bench-brief §9.1 G1c`、`plan-v2 §1.3.1`、`P-tick-7`、`F1/F2/F3/F4-C`、`Crux-1`、`原 temp/phys-plan-discuss/…md 2026-09 清理`、`harness waitWakeup 同式`、`公共化 v1`、`v7 定案`）；删 **1 处同文件行号锚点**（`见 :141-147` → 改指符号 `I_A_PSEQ` 并补「i64[4..7] 不得用作数据槽」的约束）；删**变更史 + 无锚点实测数字**（epoch 竞态那段的「1500+ HU / 20565ms」改为当前不变量：`RT_EPOCH` 只由 `resetRenderSample` 写、写时就地读槽内值）。**补齐**：模块头给出完整 SAB 分区表（含各视图下标 = 字节÷元素宽）；`MsgState`/`ShmState` 两类的定位与差异（单线程字段 vs seqlock）；每个槽常量、每个方法的调用方与失败语义（`readRenderSample` 的 0/-1、`readAuthoritativeInto` 的 0/-1、`takeInput` 饱和 vs `consumeInput` 不限幅）；并记实测零调用点：`ShmState.wake`（`waitWakeup` 仅被未接线的解耦环使用）、`maskToKeys`。跨文件反查 4 组（Rust `KEY_MASK` 逐位一致 `0x01/0x02/0x04/0x08/0x10/0x20/0x40/0x80/0x100/0x200/0x400`、`createMainSharedState` 两工程调用点、`readRenderEpoch` 两工程 worker、`AUTH_EVT` 消费面）。S4：**代码 593/593 两遍 d0**、禁用词 0、CRLF 1032 无 BOM、块注释提前闭合 0、锚点扫描 0 违规 | `src/ts-shared/auth/shared-state.ts` |
| 2026-09-22 | **WG3 全量复验闸门（19/19 全绿）** | 19 件一次跑完 `verify.ps1`：**0 失败**（`stripTrail` 全部 d0；`strict` 的 4/22/24/44/8/18 处差异经判据确认均为行尾注释改写）；`anchor-scan` 19 件**锚点 0 处、违规 0**；三工程 `npm run typecheck` 全部 **exit 0**；全仓漂移体检 **锚点 80 / 越界 0 / 路径失效 0**（exit 0） | 19 件 |
| 2026-09-22 | **WG3 测试件 3/4 完成**：`auth/compute-mode.test.ts`、`tick/ordering-gate.test.ts`、`auth/shared-state.protocol.test.ts`（主控自做） | **A 类 1 条（4 件同缺陷）**：四篇测试文件头注的运行命令都写 `cd game && npx esbuild ../src/ts-shared/...`，但 **`apps/src` 不存在**（`Test-Path apps\src` = False）⇒ 从 `apps/game` 出发的正确相对路径是 **`../../src/ts-shared/...`**；已实测修正后三件分别 **45 / 46 / 75 passed, 0 failed（exit 0）**，并把「路径以本仓实际布局为准」写进头注。另：三文件均**无任何 package.json 脚本引用**，只能手工跑——故我把「esbuild + node」实跑定为这三件的**编译外第二道门**（比 typecheck 更强）。**C 类**：删已删文档/内部编号（`t6-render-ahead §8.1/§8.4`、`phys-plan-discuss/t2-sortgate-testcases-protocol-engineer.md，2026-09 清理`、`t2 验收 #1/#2/#3/#4/#5`、`SG-C*/P*/E*/M*/S*` 对拍映射、`t3-memo §2.2/§13.1`、`t4 合同`、`F1/F2/F3`、`原 temp/phys-plan-discuss/f1-repro-entry.ts，2026-09 清理；F1_LEGACY=1`、`v7 二值注入零回归`、`Gate 2`、`protocol-engineer 42 例套件`）、删变更史叙述（「收回原『保守取等』」「旧实现（无 VA 复检）此处返回…」「对照组=红档」等改写为当前不变量）。**保留并注明**：断言标签与 `console.log` 分组名里的 `SG-xx`/`§x.y`/`t2` 等是**字符串字面量（代码）**，头注写明「本次注释重编不改动」，口径同 §7.3 #36。**补齐**：三篇头注改为「按代码实际断言归纳的分组说明 + 可跑通的运行命令」；`shared-state.protocol.test.ts` 补清四层一致性（偶值快照 / PSEQ 复检 / **代际复检** / 探针接缝三回调各自的注入窗）与「raw store 停点对当前写者不可达、只验证写序合同」的口径。S4：三件 **代码两遍 d0**（89/89、193/193、268/268）、禁用词 0、CRLF 无 BOM、块注释提前闭合 0、锚点扫描 0 违规、**测试实跑全绿** | 3 件 |
| 2026-09-22 | **WG3 末件派发**：`auth/tick-authority.test.ts`（774 行，最大件）由子代理产出，主控独立复验 | 已下发的硬约束：① 代码零改动、断言标签字符串**逐字保留**；② 注释内**不得出现 `*/` 或含 `**/` 的 glob**（§7.3 #35 的块注释提前闭合事故）；③ 头注运行命令须改为实测可跑的 `../../src/ts-shared/...`；④ 必跑 `verify.ps1` 13 项 + `anchor-scan` + **测试实跑**（改注释不得改变通过数）。产出后由主控复验并登记 | 1 件 |
| 2026-09-22 | **WG4/WG5/WG6 骨架落盘（A0/S0 前置）+ 一条可省 2/3 工作量的实测事实** | 新落两份过程产物：`.tmp/wg4/skeleton.md`（`apps/debug` **33 件 / 16682 行 / 25%**，逐文件行数与注释行数表 + 6 条前置项 + 8 条跨层耦合 C1–C8 + 自检门）、`.tmp/wg5-6/skeleton.md`（`apps/game` **14 / 9184 / 26%**、`apps/viewer` **29 / 7216 / 23%**，逐文件表 + 5 条前置项 + viewer 的"不用哪些共享控制器"清单 + 自检门）。**关键实测**：`apps/{debug,game,viewer}/src/renderer/lightmap-shader.ts` 在三工程**逐字节相同**（三份 SHA256 均为 `125D11E2…D2E996`，`Compare-Object` 差异行 **0 / 0**，各 1815 行 / 806 注释行）⇒ 该件只需重编一次、另两份同内容复制（仍各跑 `verify.ps1` + typecheck）。**同时钉死"不得类推"**：`renderer-main.ts` 两份行数不同（debug 1792 / game 1750），三份 `crates/wasm/src/lib.rs` 行数各异（3361 / 2567 / 497）⇒ 必须各自读码，不得以行数相近为由跳过（§7.3 #25 的跨文件假断言正是这么来的）。另记 viewer 事实：**不使用** `PvsManager` / `AuthorityCalibrator` / `MouseBuffer` / `PointerLockController`（其 `core/fly.ts` 自带锁定处理），worker 侧也无 `createAuthLoop` / `createWorkerDispatch` | `.tmp/wg4/skeleton.md`、`.tmp/wg5-6/skeleton.md` |
| 2026-09-22 | **台账拆分（治理动作，历史内容零改动）**：AGENTS.md 达 75.1 KB、超出 65536 字节指令预算，尾部 §7.3 被静默截断 ⇒ 把 §7.1 的 **52 行历史逐行原样移入本文件**；AGENTS.md 降至 37.8 KB（规范 §1–§6 + §7.2 当前状态 + §7.3 待决项 + §7.4 下一步），**不再截断**。此后新进展一律追加到本文件。实测：漂移体检 exit 0（10 篇 md / 越界 0 / 路径失效 0） | `documents/plan/progress-log.md`、`AGENTS.md` |
| 2026-09-22 | **WG3 末件完成**：`src/ts-shared/auth/tick-authority.test.ts`（774 → 789 行，`git diff --numstat` = `73 58`；非注释非空行的增删**均为 0**）——子代理产出，**主控独立复验** | **A 类 8 条**（头注运行命令 `../src/...` 实际无效、内容封帽实为**两段并列判据**共用 `orphanedCap`、scratch 事件随实例作废而非"被排空"、`maxStep` 比较的是 JS `number` 原值而非定点值、孤儿化发生在**门前**故 `optimisticPublished` 恒 0、`boot` 还断言 `modeSwitch` + `tick === 0`、闭账恒等式读自 `ctx.c.gate.stats` 等，已逐条按代码改写）。**C 类**：删 `t6-render-ahead §8.1/§8.4/§8.5/§10.1/§11.2/§11.3`、`F4-C`、`t4 验收 #2`、`v7 行为`、15 处 `// ── §N …` 小标题；跨文件引用全部落到**仓内真实符号**（`createTickAuthority` / `createAuthLoop` / `createWorkerSharedState`）。**主控复验（实跑）**：`verify.ps1` **0/1 失败**（`strict 636/636/d0`、`stripTrail 636/636/d0`、CRLF 789、无 BOM、块注释提前闭合 0）；`anchor-scan` 锚点 0 处 / 违规 0；`esbuild` exit 0 + `node` **19 例全绿 exit 0**；`test(` 计数 HEAD 19 = NOW 19；跨文件断言 5 组抽查全部命中。**保留项**：两处断言标签字符串含 `Q1 口径` / `§8.5`（代码字面量，已逐字保留，见 AGENTS.md §7.3 #40） | `src/ts-shared/auth/tick-authority.test.ts` |
| 2026-09-22 | **WG3 结项闸门（非测试件 19/19 + 测试件 4/4）** | 四件测试全部**实跑全绿**：`compute-mode.test.ts` 45 例、`ordering-gate.test.ts` 46 例、`shared-state.protocol.test.ts` 75 例、`tick-authority.test.ts` 19 例（`esbuild` + `node`，均 exit 0）；19 个非测试件 + 4 个测试件 `stripTrail` 逐行 **d0**、`anchor-scan` **违规 0**、三工程 `npm run typecheck` **exit 0**、全仓漂移体检 **exit 0**（10 篇 md / 锚点 80 / 越界 0 / 路径失效 0） | `src/ts-shared/**`（23 件） |
| 2026-09-22 | **⚠ 并发过载事件（owner 指令的由来）**：一次铺开 **19 个子代理**（WG4 12 + WG5 2 + WG6 4 + 前序 1），随后**全部被掐断**（`failed before it finished`）。**静止期全量盘点**（此刻无写入者，符合 §7.3 #17③）结果：`apps/**` 仅 **18 件**被改动；**3 个批次零落盘**（debug 15 件小文件批、viewer 4 批、`apps/debug/src/renderer/lod-manager.ts` 与 `world/spawn-loader.ts` 漏做）；`lightmap-shader.ts` 为**半成品**（+120/-75 行，且残留 5 处「此前」变更史 + 1 处外部行号引用）。**owner 指令**：并发过多会触发 API 计费加额 ⇒ **此后同时最多 3 个子代理**（已登记 §7.3 #41） | — |
| 2026-09-22 | **WG4 复验（主控独立跑，静止期）**：18 件改动件里 **17 件 13 项全过**（`stripTrail` 逐行 d0、CRLF、无 BOM、块注释提前闭合 0、锚点扫描违规 0、路径引用 42/42 存在、符号断言 53/53 命中）；`apps/debug/crates/wasm` **`cargo check` exit 0**。**修掉 8 处失败**：5 件禁用词（`crates/wasm/src/lib.rs` ×2、`apps/debug/src/app.ts` ×2、`renderer/renderer-main.ts` ×2、`apps/game/src/app.ts` ×2、`lightmap-shader.ts` ×5 待重编件）、3 件 `world/*.ts` 为 **LF-only**（已归一 CRLF）。**主控自做补漏两件**：`renderer/lod-manager.ts`（316 → 306 行，A 类 4 条：`SceneDiagonalInfo` 的 `defaultCull`/`maxCull` 与代码不符、类文档写的 PVS+迟滞已不在 `update` 里、`@param pvsManager` 是不存在的形参、`updateInterval` 默认值实为 1 非 4）、`world/spawn-loader.ts`（120 → 118 行，去 `t8 实证` 与「2026-09 统一修正」沿革）。**主控自做脚本一件**：`scripts/auth-clock-verify.mjs`（209 行，去 harness §P4 引用、去「修复前」叙述与无锚点数字；`node --check` exit 0） | 19 件 |
| 2026-09-22 | **新增两条内容审查手段（把 §7.3 #25 的盲区机器化）**：① 抽取注释里的**仓内路径**引用并批量 `Test-Path`（本轮 42 条全存在）；② 抽取「`` `路径` `` 的 `` `符号` ``」型**跨文件符号断言**并逐条验证目标文件确有该符号（本轮 53 条全命中）。**另记陷阱**：PowerShell `(?m)^(\s*)X(\s*)$` 的 `\s` 会吃掉行尾 `\r`，替换不补 `${2}` 即产出 bare LF（实测 `apps/debug/src/app.ts` 报 `CRLF=2534 / bareLF=1`，已归一） | AGENTS.md §7.3 #42 |
| 2026-09-22 | **`lightmap-shader.ts` 三工程同构件完成（1 次重编 + 2 次复制）** | 子代理重编 `apps/debug` 版（1881 → 1838 行；注释 872 → 829），**主控独立复验**：`verify.ps1` 0/1 失败、`stripTrail 890/890 d0`、`anchor-scan` 违规 0、无禁用词/无 `§`/无外部锚；随后**同内容复制**到 `apps/game`、`apps/viewer`（三份 SHA256 均为 `50CBE18C…`），三份各跑 13 项 **0 失败**，`apps/game` 与 `apps/viewer` 的 `npm run typecheck` 均 **exit 0**。**C 类**：删死引用「回归护栏见 `scripts/lightmap-inject-guard-selftest.mjs` §10」——实测该脚本**工作区与 `git ls-tree HEAD` 均无**（计数 0） | 3 件 |
| 2026-09-22 | **⚠ 抓到一处「检查不可见」的真实代码改动（本轮最重要的验证升级）** | 子代理在 `lightmap-shader.ts` 的 **GLSL 源码模板字符串内**改了两处注释（删 `2026-09-21`、改写「要重编几百个 program（实测 1.4~2.5 s 冻结）」），而 `verify.ps1` 报 `strict 890/890 d0`、`stripTrail 890/890 d0`——**两轮都看不见模板字符串内部**（其内以 `//` 开头的行会被当注释剥离）。**处置**：① 用 TypeScript 编译器 API 取真正的模板字面量节点，新建 `.tmp/tools/template-proof.mjs`（与 `git show HEAD` 逐模板比对、CRLF 归一化）；② 两处**逐字回滚** → `templates HEAD=11 CUR=11 diff=0`；③ 推广到**全部 48 个已改动代码文件**：`files checked=48 template-literals=236 files-with-changes=0`；④ 固化为**第 4 道门**（每件必跑） | 见 AGENTS.md §7.3 #44 |
| 2026-09-22 | **`apps/debug/scripts/auth-clock-verify.mjs` 注释重编（主控自做）** | 209 行；删 harness §P4 引用与「修复前」变更史、去日期与无锚点数字（≈11.5 ms / ≈31% / ≈60 事件/秒）、把「面板每条 physics 配置消息都带 tickRate」按代码改为**两条入口**（滑块单发 `{tickRate}` 与录制回放发整段 `physics`）；复验 `verify.ps1` 0 失败、`node --check` exit 0。**字符串字面量内 3 处历史编号保留**（`check('B 明显优于 C（修复有效）')` 等） | 1 件 |
| 2026-09-22 | **WG4 源码闸门通过（33/33 + 脚本 1 件 = 34 件，0 失败）** | 合并闸门实测：`verify.ps1` **0 / 34 失败**（`stripTrail` 逐行 d0；8 件的 `strict` 差异经判据确认为行尾注释改写）、`anchor-scan` **锚点 0 处 / 违规 0 / 裸行号 0**、`template-proof` **158 个模板字符串逐字节相同**、`apps/debug` `npm run typecheck` **exit 0**、`apps/debug/crates/wasm` `cargo check` **exit 0**。**内容审查（主控独立重跑，非采信自述）**：15 件小文件批的**仓内路径引用 29/29 存在**、「路径+符号」跨文件断言 **34/34 命中**（用其脚本 `.tmp/wg4/symbol-proof.mjs` 重跑）。**剩余**：`apps/debug/scripts/**` 其余 17 件 | `apps/debug/**`（34 件） |
| 2026-09-22 | **⚠ 一处自我更正（子代理交叉发现）**：`src/phys/mod.rs` 的 `state` / `state_js` 头注写「字段固定 **10** 项」，而 `state_js` 函数体实测写入 **11** 个键（多一个 `contactTicks`），同一条注释**自相矛盾**（写 10 项却列出 11 个字段名）。这是我 WG1 时的漏判，AGENTS.md §7.3 #12 原记「10 字段」同样错。**处置**：按函数体逐行核对后改正两处注释为 **11 项**，复验 `verify.ps1` 0 失败 + `cargo test -p websurf-phys` **10 passed / 0 failed / exit 0** | `src/phys/mod.rs`、AGENTS.md §7.3 #12 |
| 2026-09-22 | **WG5 首件（工程内最大件）：`apps/game/crates/wasm/src/lib.rs`** 由子代理产出、**主控独立复验** | 2567 → 2585 行；`verify.ps1` **0/1 失败**（`strict 1933/1933 d8` 经判据确认为行尾注释改写、`stripTrail 1933/1933 d0`、CRLF、无 BOM、禁用词 0、禁用路径 0、外部锚 0、块注释提前闭合 0）；`anchor-scan` 锚点 0 处；`template-proof` PROOF-OK；`apps/game/crates/wasm` 的 `cargo check` **exit 0**。**A 类 18 条**（头注「工程内隔离副本」→ 按代码改为「三工程共用 `src/wasm-core`」；文档里的 `parse_bsp` 在本文件不存在；`export_glb_with_pakfile_models` 文档写了不存在的形参；`export_model_tri_colliders` 的输出类型/常量名/OBB 回退三处错；`parse_spawn_points` 的 `primary` 类型错（实为 `Option<usize>`）；`parse_pvs_data` 字段名实为 camelCase 等）。**主控抽查 4 条跨文件断言全部命中**：`MAX_TRI_TOTAL` 8 处 / `MAX_MODEL_TRIS` 0 处；`fn export_colliders_with_filter` **只在** `apps/debug/crates/wasm/src/lib.rs`；`primary: Option<usize>`；`OBB` 在 `apps/game/src`+`src/ts-shared` 命中 0。**子代理自述曾误删 15 行代码并当步还原** —— 本次复验的 `stripTrail d0` 即为「代码零改动」的机器证明 | `apps/game/crates/wasm/src/lib.rs` |
| 2026-09-22 | **WG4 15 件小文件批复验通过（主控独立跑）** | `verify.ps1` **0 / 15 失败**（`stripTrail` 全 d0；`config.ts` d8、`Collision.types.ts` d10 经判据确认为行尾注释改写）、`anchor-scan` **锚点 0 处**、`template-proof` **4 个模板全同**、`apps/debug` `npm run typecheck` **exit 0**、内容审查**仓内路径 29/29 存在**、「路径+符号」断言 **34/34 命中**（用其 `.tmp/wg4/symbol-proof.mjs` 重跑）。**A 类 20 条**，其中最重要的交叉发现是 `src/phys/mod.rs` 的 `state`/`state_js` 头注「固定 10 项」与函数体实测 **11 个键**自相矛盾（见本台账同日「自我更正」行） | `apps/debug/src/**`（15 件） |

---

## §7.3 已结案（逐行原样移出，内容未改）

> 2026-09-22 自 AGENTS.md §7.3「当前阻塞与待决」**逐行原样移出**（共 19 条）；移出只换存放位置，**未改一字**。
> AGENTS.md §7.3 保留的是**仍生效的规则**与**待 owner 裁决项**。

| # | 事项（原文） | 状态（原文） |
|---|---|---|
| 6 | `src/phys/mod.rs` 头注"导出 12 个 API" vs 实测 24 —— **已在 WG1 首件按代码修正** | 已解决 |
| 12 | 计数对撞（**survey 已同步**）：`PhysParams` 实为 **18 字段**；`set_params` 实收 **15 键**（`Patch` 结构体 15 个 `Option` 字段，旧注释误列已不存在的 `noPrestrafe`）；`duck_surf_tests` 6 项 + `p2_gate_tests` 4 项 = **10 项**。**⚠ 其中一条我记错了、已更正**：`state()` 实返回 **11 个字段**（`posX/posY/posZ`、`yaw`、`pitch`、`velX/velY/velZ`、`onGround`、`contactTicks`、`eyeHeight`），**不含 timeMs**；WG1 当时记「10 字段」是漏了 `contactTicks`，导致 `mod.rs` 的 `state` 与 `state_js` 两处头注**自相矛盾**（写「固定 10 项」却紧接着列出 11 个字段名）。**本轮由 WG4 小文件批的子代理交叉发现**，主控按 `state_js` 函数体逐行核对后**已修正两处注释为 11 项**，并复验：`verify.ps1` 0 失败、`cargo test -p websurf-phys` **10 passed / 0 failed / exit 0** | 已解决（含一处自我更正） |
| 13 | **流程规则（本次新增，写进 §3 S3）**：代码注释内**禁止写同文件行号锚点**——评论一改行号即整体失效。同文件一律用符号名指代；跨文件锚点必须写成 `path:line` 且逐条开箱复核。`mod.rs` 首稿曾写 36 处同文件锚点，已全部改为符号引用（跨文件锚点保留并 12/12 开箱命中） | 已解决；后续文件一律遵守 |
| 14 | 过程产物已落临时区（不入库，`.gitignore` 的 `**/.tmp/` 覆盖）：`.tmp/wg1/facts-src-phys.md` = WG1 事实表（24 个导出 + 消费点全表、14 条结构事实、常量与不变量、测试基线、自检记录）；`.tmp/wg2/skeleton.md` = WG2 骨架 | 已完成 |
| 15 | **疑似冗余代码（只记录，未改）**：`src/wasm-core/vbsp/reader.rs` 的 `read_leaves` 里，进入 v1 分支后再判 `if self.length % LEAF_V1_SIZE != 0 { return Err(InvalidLumpSize) }` 是**不可达分支**——`use_v1` 的定义本身已含 `self.length % LEAF_V1_SIZE == 0`，故该条件恒假。行为无影响（既不会误报错，也不会漏判），但属死代码 | 已知；按 §6 第②类"只记录不修"，未改动 |
| 16 | **行尾约定澄清（重要）**：本仓 `core.autocrlf = true`、**无 `.gitattributes`**，`git ls-files --eol` 显示**索引中全部文件均为 `i/lf`**——即入库内容恒为 LF，工作区的 CRLF 只是检出产物。实测 `git diff --numstat` 在 `core.autocrlf=true/false` 下输出完全一致，故工作区 CRLF/LF 混用**不影响提交内容**。另注：`src/phys/duck_surf_tests.rs` 在未改动状态下即为 `w/lf`，证明 LF 工作区文件本就存在 | 已知；任务书 §6.1 的"CRLF"要求对入库内容实为空转（对文档仍按 CRLF 执行） |
| 20 | **疑似代码缺陷（只记录，未改，②类）**：`src/wasm-core/mosaic/decode.rs` 的 `code_to_img` **不校验 `宽 ≥ 1` / `高 ≥ 1`**（同功能的 `mosaic/mtz.rs` 的 `parse_bytecode` 有 `w < 1 \|\| h < 1` 判断），也不校验解出的索引是否 < 调色板色数（`bits=2` 允许索引 0..3，而 `C[` 可能只有 2 色 ⇒ `colors[idx[i]]` 越界）。另 `let n = (w * h) as usize;` 在 `u32` 上相乘，`w`/`h` 由文本解析且无上界。**以上均为静态读码所得的事实；越界/溢出的具体后果未运行验证**，注释里只如实写了"校验范围"未作断言 | 已知；按 §6 第②类只记录不修 |
| 22 | **死引用/外部引用清单（锚点扫描实测）**：① `src/ts-shared/decoupled/decoupled-loop.ts` 有 **6 处**指向 `worker-a.ts` 的锚点 ⇒ C 类，**待 WG3**（该文件的真实身份已查清，见第 29 条）；② `lightmap.rs` 的 21 处 `*.cs` 外部引用与 1 处已删文档引用 —— **已处置完毕**（§7.1 的 23/26 行）；`vbsp/data/game.rs` 的 3 处外部引用 —— **已处置完毕**（§7.1 的 22/26 行） | ② **已解决**；① 待 WG3 |
| 24 | **判据修正：行尾空白按「基线」判，不按「恒为 0」判**。实例：`src/wasm-core/texture_utils/vtf.rs` 的空白行含 4 个空格，与 HEAD 版对应行（`git show HEAD:...` 第 108 行）**逐字节相同**——HEAD 全文 2 处、重编后 1 处，**没有新增**。重编只允许增删注释，**不得顺手清理原有空白行**（那属代码/格式改动）。⇒ 复验判据为「行尾空白行数 **不高于** HEAD 基线」 | 已按此判据复验 `vtf.rs`（通过） |
| 29 | **`decoupled-loop.ts` 引用的 `worker-a.ts` 已查明身份**：该文件确实存在于 git HEAD，路径为 `test/dual-mode-harness/src/worker-a.ts`——即它属于**已退役的 harness 工程**（工作区已删除）。故那 6 处锚点是「指向已退役工程」的 C 类死引用，处置同 §7.3 第 4 条口径（删除或去锚点），待 WG3 | 已查清；待 WG3 处置 |
| 32 | **复验工具判据升级（消除了 §7.3 第 17 条⑥ 的假阳性）**：`strict` 轮只按「整行是否以注释开头」过滤，故**行尾注释被改写**也会报差异——`mtz.rs` 由此报 `strict d8`。现在脚本对有差异的行**自动再比一次「代码部分」**（剥离首个 `//` 后 TrimEnd）：两边相同 ⇒ 打印「N 行差异全部是行尾注释改写」并**不计失败**；不同 ⇒ 打印「★代码部分不同」并判失败。代码同一性的**主判据仍是 `stripTrail` 必须 d0**（它同时兜住「字符串里含 `//` 导致剥离截断真代码」这一风险由 strict 轮交叉发现） | 已升级并实测（`mtz.rs` 判为 8 处行尾注释改写、`entity.rs` d0） |
| 33 | **⚠ 已知偏差（如实登记）+ 新增可见化指标**：`src/wasm-core/bsp_to_gltf_core/materials.rs` 的 **13 处「纯空白行」尾随空格被归一成真空行**（HEAD 13 处 → 现 0 处）。核实：这 13 处在 HEAD 里**全部是只有空格的行**，**没有一行是代码行**，故不构成代码改动；但 `strict` / `stripTrail` 两轮**都过滤空行**，这类改动对代码同判据**天然不可见**——这正是我此前遗漏的检查面。**处置**：给 `verify.ps1` 增加第 12 项指标「**空行尾空格=N/基线M**」，与行尾空白并列显示（**不判失败**，但从此必须看得见）。**判据最终定为**：① 非空白行两遍逐行一致；② 行尾空白与空行尾空格**只允许减少**且必须显示；③ 减少量出现在下一次复验输出里可查 | 已登记；指标已生效（该文件现显示 `空行尾空格=0/基线13`） |
| 34 | **`convert.rs` 的死代码事实（只记录，未删；是否删由 owner 裁决）**：三份 GLTF 合并实现 `merge_model_into_root` / `create_new_gltf_structure` / `merge_gltf_structures_improved` 在本仓**零调用点**（各带 `#[allow(dead_code)]`，合计约 **500 行**，占该文件代码量的一半以上）；**线上合并路径是 `model_integrator` 的 `ModelIntegrator::add_models_to_gltf`**。旧注释用「# 保留原因」把保留理由写成"算法参考 / 性能基准对比 / 教学参考 / 退化回退"，并声称「当前生产路径使用 `merge_gltf_structures`」——**该符号在全仓不存在**（坏 intra-doc 链接），已按代码改写为「无调用点」的事实陈述 | 已知；只记录不删 |
| 37 | **`apps/debug/scripts/auth-clock-verify.mjs` 内含 3 处失效引用（脚本资产，属 WG4 范围）**：① 第 196 行的替代覆盖说明引用 `t4-chain.test.ts §P4`——**该文件全仓不存在**；② 同处引用 `worker-dispatch.ts L268-272` 的**行号锚点**（同文件注释已被重编，行号会漂）；③ 第 194 行 `check('dispatch 接线层覆盖（createWorkerDispatch 桩构造）', false, ...)` 是**故意判失败**的占位断言（脚本自身的"如实汇报"分支）。三处均未擅改 | **注释部分已处置**：`auth-clock-verify.mjs` 已按当前代码重编并复验（`verify.ps1` 0 失败、`stripTrail` d0、`node --check` exit 0）——删掉 harness §P4 与「修复前」叙述、日期与无锚点数字。**剩余 3 处历史编号在字符串字面量里**（`check('B 明显优于 C（修复有效）')`、`console.log('修复 2 接线层…')`、`console.log('替代覆盖：t4-chain.test.ts §P4 + worker-dispatch.ts L268-272 …')`），按「代码零改动」未改，**待 owner 裁决** |
| 39 | **指令预算超限 —— 已按候选处置①执行（2026-09-22，保留 owner 复核权）**：原 AGENTS.md 为 **75.1 KB**，超过 harness 的 65536 字节指令预算，加载时**尾部会被截断**（截断点约在 §7.3 中段），即 §7.3 末尾若干条与 §7.4 不会作为指令自动载入（显式 `read` 仍可读全）。§7.1 的历史行按「不改历史行」规则持续累积是主因。**候选处置**（待 owner 裁决）：① 把 §7.1 的逐件明细移出为 `documents/plan/progress-log.md`，AGENTS.md 只留规范 + §7.2/§7.3/§7.4；② 压缩 §7.1 的历史行（违反现行「不改历史行」规则，需 owner 授权）；③ 维持现状。**实际执行处置①**：§7.1 的 52 行历史**逐行原样**移入新建的 [progress-log.md](progress-log.md)（39.1 KB）⇒ AGENTS.md 由 **75.1 KB 降至 37.8 KB**，**回到 65536 字节预算内、不再截断**（本行能被完整自动载入即为实证）；全仓漂移体检 **exit 0 / 0 越界 / 0 路径失效**（md 篇数 9 → 10）。历史行**未改未删、仅换存放位置**；owner 若要求改回，可按同一脚本逆向合并 | **已解决（保留 owner 复核权）** |
| 45 | **死引用清除（C 类，实测）**：`apps/{debug,game,viewer}/src/renderer/lightmap-shader.ts`（三工程同构）头注原引「回归护栏见 `scripts/lightmap-inject-guard-selftest.mjs` §10」——实测**该脚本既不在工作区、也不在 `git ls-tree HEAD`**（两处计数均为 0）⇒ 属死引用，随重编删除（三份同步删）。**同时登记**：该文件内 `lightmap.rs` 错误消息式的「字符串内含无锚点数字」并不存在；本轮两处被误改的 GLSL 注释已回滚，其内 `2026-09-21` 与「实测 1.4~2.5 s 冻结」属**字符串内容**，按「代码零改动」保留（同 §7.3 #36/#40 口径，待 owner 裁决是否清理） | 已解决（死引用已删；字符串内容保留并登记） |
| 47 | **自查发现并修掉的一处「行数声明漂移」**：我在本文件 §7.2 写 `crates/wasm/src/lib.rs`（2567）时用了**裸文件名**，`check-doc-drift.mjs` 把它映射到 `apps/debug/crates/wasm/src/lib.rs`（实测 3481 行）⇒ 判「行数声明漂移」并 **exit 1**（体检变红）。**处置**：改写为完整路径表述并**去掉数字**（对进行中的文件，行数会随重编变化，写死数字必然漂移）。**规则**：台账/文档里提到**仍在重编的文件**时，不写行数；确需写时用**完整路径**且只在文件冻结后写 | 已解决（体检回到 0 漂移 / exit 0） |
| 48 | **WG4 小文件批复验中新增登记的「零装配点 / 零消费点」事实（只记录不修）**：① `apps/debug/src/renderer/fog-manager.ts` 的 `FogManager` 全仓仅定义处 1 处命中、`renderer-main.ts` 明确不设 `scene.fog` ⇒ **无装配点**；② `apps/debug/src/physics/math/vec3.ts` 13 个导出函数**零调用点**（仅 `Vec3` 类型被 5 处 `import type`）；③ `Collision.types.ts` 的 `TraceResult` 全仓零消费点；④ `apps/debug/src/config.ts` 的 `MovementConfig` / `SmoothingConfig` / `TeleportConfig` 三段与 `physics.duckScale` **零读取点**（传送判定实际用 `teleport-manager` 的 `TRIGGER_RADIUS`/`TRIGGER_COOLDOWN`）；⑤ `apps/debug/src/world/custom-teleports.ts` 的 `saveCustomTeleports` 仅本文件内使用。以上**均按代码如实写入注释**，是否删代码由 owner 裁决 | 已知；注释已如实标注 |
| 49 | **`apps/debug/src/wasm.d.ts` 的类型缺口比 §7.3 #8 记的更大**：除 `PhysWorld` 缺 7 个方法外（声明 17 个成员含 constructor vs `src/phys/mod.rs` 24 个 `pub fn`），其 `BspProcessor` 声明还缺 `export_glb_with_pakfile_models_with_defaults_and_lights`——而 `src/ts-shared/phys/world-builder.ts` 的 `BspProcessorLike` **要求**该方法。注释已如实写「声明落后 + 两处运行时 cast 点（`captureFullPhysState` / `restoreFullPhysState`）」，**未改类型** | **待 owner 裁决**（补类型属代码改动） |
| 2026-09-22 | **全仓进度盘点（实测，用于 WG11 报告与剩余排期）** | `git status` 实测：**改动 101 个文件**（`src/**` 56 + `apps/**` 43）、删除 91 个路径、未跟踪 4 个（新增文档与骨架子目录）。**逐工程源码完成度**：`apps/debug` **33/33 已完成**；`apps/game` **7/14**（未改 7 件：`input/input-bridge.ts`、`input/keyboard.ts`、`input/keymap.ts`、`renderer/renderer-main.ts`、`worker/worker-types.ts`、`config.ts`、`savepoint.ts`——其中多数正由在跑批次处理）；`apps/viewer` **2/29**（在跑批次处理 `replay/**` 前 7 件）。**脚本资产**：`apps/debug/scripts` 1/18、`apps/game/scripts` 0/20、`apps/viewer/scripts` 0/2 ⇒ **共 1/40**。**文档**：已落 4 篇（`documents/architecture/overview.md`、`documents/ts-shared/overview.md`、`documents/norms/annotation-and-verification.md` + 台账本身），WG8 余 3 篇、WG9/WG10 未开始 | 实测 |
| 2026-09-22 | **台账二次瘦身（治理动作，内容零改动）**：`AGENTS.md` 由 **50929 → 38814 字节**——把 §7.3 中 **19 条已结案项**（#6/#12/#13/#14/#15/#16/#20/#22/#24/#29/#32/#33/#34/#37/#39/#45/#47/#48/#49）**逐行原样**移入本文件「§7.3 已结案」小节，正文只留「仍生效的规则」与「待 owner 裁决项」，并加指针行。目的：降低每一轮读取与更新台账的指令开销（此前曾因超 65536 字节预算被静默截断）。**已自检**：抽样 #12/#39/#49 在台账历史区**存在**、在 AGENTS.md **不存在**；漂移体检 13 篇 md / 越界 0 / 路径失效 0 / exit 0 | `AGENTS.md`、`documents/plan/progress-log.md` |
| 2026-09-22 | **新增规范篇 1 篇（P4）**：`documents/norms/annotation-and-verification.md` | 内容：三条禁令（B1/B2/B3）、注释书写规范（代码零改动 / 注释内不写行号、跨文件用「路径+符号」/ 块注释不得提前闭合 / 模板字符串内容＝代码 / CRLF+无 BOM / 不动既有空白）、**四道门**（`verify.ps1` 13 项 → `anchor-scan` → `template-proof` → 编译门）与「不得只凭第一道门判定」的实证理由、**内容审查两手段**（路径核验 / 符号核验）、7 条已验证陷阱、记录约定。体检：**13 篇 md / 锚点 131 / 越界 0 / 路径失效 0 / exit 0** | `documents/norms/annotation-and-verification.md` |
| 2026-09-22 | **WG8 完成（共享层文档 5/5）+ 规范篇 1 篇** | 本轮新增三篇并全部通过体检：`documents/phys/overview.md`（定位与边界 / 文件职责 / **24 个导出**分组织表 / 契约：`set_params` 15 键 vs `PhysParams` 18 字段、`state()` **11 键**、`state_out` 22 槽与视图重建约束 / 5 条不变量 / 测试 / 遗留）、`documents/materials/overview.md`（**三条互不相同的贴图通路**——外部 `vtf` crate 的导出期解码、本仓 `texture_utils` 容器解析（仅 debug 的 wasm 绑定调用）、mosaic 字节码；MTZ6 容器与默认包；缺失资源回退两入口；PAK 索引；缺陷索引）、`documents/wasm-core/overview.md`（26 个 `.rs` 的 8 组模块清单 / 入口面表 / 主流程 / 5 条不变量 / 遗留索引）。**主控逐篇开箱抽查锚点**：phys 11 个、materials 6 个、wasm-core 8 个**全部指向所述符号**；体检 **16 篇 md / 锚点 193 / 越界 0 / 路径失效 0 / exit 0**；三篇禁用词 0。**自查踩坑 1 处**：phys 稿把「测试模块 + 项数」写成表格行 ⇒ 被判**行数声明漂移**（裸文件名 + 数字，与 §7.3 #47 同源），已改为行文表述 | `documents/{phys,materials,wasm-core}/overview.md`、`documents/norms/annotation-and-verification.md` |
| 2026-09-22 | **去行号债务全部清偿（§7.3 #19 结案）**：`src/phys` + `src/wasm-core` 共 33 件的 `文件:行号` 锚点改为符号引用，**主控独立复验** | 复验判据全部实跑：`verify.ps1` **0 / 33 失败**（`stripTrail` 逐行 d0；`phyfile.rs` d6、`mtz.rs` d8、`vbsp/data/mod.rs` d4 经判据确认为行尾注释改写；CRLF、无 BOM、禁用词 0、禁用路径 0、块注释提前闭合 0；`lightmap.rs` 外部锚 1 = 已登记的字符串字面量；`materials.rs` 尾空白 0/基线13、`convert.rs` 58/58、`vtf.rs` 1/1 均为已知基线）；`anchor-scan` **全仓 106 个已改动文件 / 锚点 0 处 / 违规 0 / 裸行号 0**；`cargo test -p websurf-phys` **exit 0**；`cargo check -p websurf-wasm-core` **exit 0**；**内容审查 53 条「路径 + 符号」断言全部命中（0 缺失）**。转换质量抽样（开箱）：`renderer-main.ts:1418` → `RendererMain.consumePhysEvents`（目标文件 3 处命中）、`tick-authority.ts:400/:513` → `publishMeta`（15 处命中） | `src/phys/**`（7 件）、`src/wasm-core/**`（26 件） |
| 2026-09-22 | **WG4 脚本批推进（2/18）+ 三工程 `check-wasm-api.mjs` 头注重编（主控自做并复验）** | 三个工程各一份薄配置脚本，改写要点：① 删 `F4:` 前缀、`（D-03 / T-03）` 内部编号与已删文档的 `规范 §8.2` 引用；② 把「检查引擎在共享层」落到符号（`src/scripts/lib/wasm-api-contract.mjs` 的 `assertDtsExports` / `assertTsImportsCoveredByExports` / `extractExportsFromPkgJs` / `extractExportsFromDts` / `readDtsApiNames`——实测 5 个符号在该文件全部存在）；③ viewer 那份删掉 `bsp.ts:3` 行号锚点，改为「唯一导入点是 `apps/viewer/src/core/bsp.ts` 的 `import { BspProcessor, initSync }`」（grep 实测 `apps/viewer/src` 内对 pkg 的符号导入仅此 1 处）；④ game 那份把已知覆盖缺口写成事实并指向本文件 §7.3 #9（**不写差额数字**，避免漂移）。**四道门**：`verify.ps1` **0/3**、`anchor-scan` 锚点 0、`template-proof` PROOF-OK、`node --check` 三件 exit 0；`content-review.mjs` 23 条仓内路径全在。**另记**：debug 那份 `console.log` 里的 `F4` 属**代码字面量**，按「代码零改动」保留，已登记 §7.3 #52 | 三工程 `scripts/check-wasm-api.mjs` |
| 2026-09-22 | **WG5 9 件批完成并复验（13/14）** | 范围与行数：`apps/game/src/worker/main.ts`(481→521)、`config.ts`(292→281)、`worker/worker-types.ts`(202→255)、`input/keyboard.ts`(122→130)、`input/keymap.ts`(122→132)、`savepoint.ts`(106→117)、`input/input-bridge.ts`(75→88)、`world/types.ts`(17→20)、`wasm.d.ts`(6→16)。**主控独立复验**：`verify.ps1` **0/9**（`stripTrail` 全 d0；`worker/main.ts` 的 `strict d4` 经判据确认为行尾注释改写）、`anchor-scan` 锚点 0、`template-proof` PROOF-OK（5 个模板全同）、`content-review.mjs` **93 条仓内路径 + 40 条「路径+符号」断言全部命中**、`apps/game` `npm run typecheck` **exit 0**。**抽检实证（不采信报告）**：`input-bridge.ts` 的 `addInput` 函数体确为 `void dx; void dy; void keysMask;`——丢弃三参的空操作，与注释一致。**A 类（旧注释↔代码冲突）要点**：`worker/main.ts` 健康守护段旧注称「直接 `respawn()` 拉回出生点」，实际只发 `health-log`、不改任何状态；`authYFloor` 旧注「出生点优先」实为先判 `authDeathY`；`config.ts` 的 `exposure` 旧注「面板 1~3」实为 `bindSlider` 的 `0.1, 8, 0.01` 量程；`worker-types.ts` 的 `WorldJsonMessage` / `InputMessage` **方向写反**。**疑似缺陷另登记为 §7.3 #53** | `apps/game/src/**`（9 件） |
| 2026-09-22 | **WG6：viewer `replay/` 前 7 件复验通过 + 主控自做 2 件** | 复验 7 件（`shavit-replay` 616、`timeline` 358、`player` 255、`sampling` 88、`build` 73、`protocol` 45、`helpers` 16）：`verify.ps1` **0/7**（`stripTrail` 全 d0；`shavit-replay.ts` 的 `strict d12` 经判据确认为行尾注释改写）、`anchor-scan` 锚点 0、`template-proof` **54 个模板逐字节相同**、`content-review.mjs` 23 条仓内路径全在、`apps/viewer` `npm run typecheck` **exit 0**。**主控自做**：`apps/viewer/src/wasm.d.ts`（6→13 行；写入三条实测事实——`pkg/` 是构建产物、本文件在 `apps/viewer/src` 内**零导入点**、`core/bsp.ts` 直连 pkg）与 `apps/viewer/src/worker/main.ts`（110→128 行；写入魔数嗅探先于文本解码、字节缓存的复用条件、transfer 列表的后果、进度只发 `parse` 两条、异常在 `handle` 内收敛、无 Worker 时主线程 `importOnMain` 回退）；两件四门绿（`verify.ps1` 0/2、锚点 0、`template-proof` PROOF-OK、`content-review` 8 路径 + 4 符号）+ `esbuild --bundle src/worker/main.ts` **exit 0**。**抽检**：`protocol.ts` 注释称「ReplayImporter 每次都传文件」——实测唯一调用点 `apps/viewer/src/replay/panel.ts` 的 `runImport` 在 `!this.file` 时早退，故该断言成立 | `apps/viewer/src/replay/**`（7 件）、`apps/viewer/src/wasm.d.ts`、`apps/viewer/src/worker/main.ts` |
| 2026-09-22 | **三项裁决与一处自我更正（主控）** | ① `src/phys/player.rs` 锚点批的「加一个逗号」**予以接受**：该行（`因此，前进 + 横移…`）是 WG1 新写行、HEAD 无此句；相邻的 `因此前进` 会命中禁用词 `此前`，加逗号后实测全文件禁用词 0、语义不变、代码零改动（`stripTrail` d0）。② `src/phys/p2_gate_tests.rs` 的**文件末尾换行已按 HEAD 还原**（HEAD 无末尾换行，被 WG1 补成了 CRLF 行尾）：用 `.tmp/tools/strip-eof-nl.mjs` 只删末尾 2 字节、并逐字节断言前缀相同；还原后 7937 字节、`verify.ps1` 0 失败、`cargo test -p websurf-phys` exit 0。③ **自我更正**：我此前记「`src/wasm-core/vbsp/data/mod.rs` HEAD 有末尾换行、现无」是**测量方法造成的假象**——那次用 `git show` 按行拼接，掩盖了末尾换行状态；改用 `git cat-file blob` 读原始字节后实测 **HEAD 与工作区一致（都无末尾换行）**，改写者无过。**全仓末尾换行审计**（新工具 `.tmp/tools/eoln-audit.mjs`，逐文件对比 HEAD blob 与工作区的「是否以换行结尾」）：**116 个已改动且有 HEAD 基线的文件，不一致 0 个** | `src/phys/player.rs`、`src/phys/p2_gate_tests.rs`；`.tmp/tools/{eoln-audit,eoln-check,strip-eof-nl}.mjs` |
| 2026-09-22 | **⚠ 新陷阱（主控自己在 `apps/viewer/src/wasm.d.ts` 上踩中，已固化认知）** | 注释里写「`.gitignore` 的 `**/pkg/` 规则」——`**/` 内含 `*/` ⇒ **块注释被提前闭合**，其后注释文本被当作**代码**解析，于是**两道门同时报警**：`verify.ps1` 报 `块注释提前闭合=1`（判失败），`template-proof.mjs` 报 `TEMPLATE-CHANGED … templates=0, diff=11` 即 **PROOF-FAIL**（提前闭合后，注释里的反引号文本被当成模板字面量参与比对）。改为「`.gitignore` 中忽略 `pkg` 目录的规则」后两门同回绿。**结论**：§7.3 #35 的后果不止「只有编译器能暴露」——它还会**污染 template-proof 的比对基线**，两条报警要一起读；写法上**注释内任何位置都不得出现 `*/`** | `apps/viewer/src/wasm.d.ts`（已修正并复验：0/2 失败、PROOF-OK） |
| 2026-09-22 | **台账治理 + 新增四件过程工具** | ① **修掉 §7.3 的表格结构缺陷**：原文把「已结案项已移入…」的引用块插在**表头与首行之间**、并在 #46/#50/#51 前留了连续空行 ⇒ 按 GFM 规则整张 §7.3 表**不渲染为表格**（呈纯文本）。已把引用块移到表头之上、压掉多余空行，§7.3 现为一整张表；`AGENTS.md` 实测 **44441 字节 / CRLF 182 / bareLF 0 / 无 BOM**。② 新增过程工具（均在 `.tmp/tools/`，不入库）：`content-review.mjs`（抽取注释里的仓内路径与「`路径` 的 `符号`」断言并逐条机器核验，本轮已用于 21 件）、`eoln-audit.mjs`（全仓末尾换行审计）、`eoln-check.mjs`（单文件 HEAD/CUR 对照探针）、`strip-eof-nl.mjs`（按 HEAD 还原末尾换行，带前缀逐字节断言）；另固化 `fix-blanklines.mjs`（表格空行压缩，一次性） | `AGENTS.md`；`.tmp/tools/*` |
| 2026-09-22 | **独立内容审查轮（viewer `replay/` 7 件）+ 主控逐条修并复验** | 审查方式：由独立审查员通读 7 件「代码 + 注释」，**不采信注释自身**，每条断言回实现/调用点/测试核对；结果 **A 类 7 条 / B 类 1 条 / C 类 0 条**，并反查跨文件断言 **25/25 命中**、仓内路径 **21/21 存在**、禁用词 0、无行号锚点、无含 `*/` 的 glob。**A 类逐条**：① `timeline.ts` 跑段高亮注释把「减数/被减数」写反（代码里被减数是轨道内部时间 `track.clip.t` 上的帧时间、减数含全局 `track.offset`）；② 同文件帧读数注释的 pre/run 边界与代码差 1（代码一律按 `idx` 比，注释却用 `idx + 1` 的「序号」去比）；③ `sampling.ts` 的 `[−180, 180)` 声明对 `b − a < −540` 不成立（JS `%` 保留被除数符号，结果 < −180）；④ `player.ts` 的「工具/调试用」实为**全仓零调用点**；⑤ `protocol.ts` 的「ReplayImporter 每次都传文件」（实为该类形参可空、由唯一调用点 `panel.ts` 的 `runImport` 保证非空）；⑥ 同文件「字段与 `Clip` 同名同型」实为**真子集**（少 `Clip.id` / `Clip.rule`，由主线程 `payloadToClip` 本地补）；⑦ 同文件 `rule` 的注释把下一个字段 `name` 的内容并了进来。**主控处置**：7 条全部按代码改写（另把 `shavit-replay.ts` 头注里「规格来源 = 仓库外第三方实现的 `replay-file.inc`」改为按代码可核实的表述——判据是头行魔数字面量 `{SHAVITREPLAYFORMAT}` 与 `{FINAL}`/`{V2}` 后缀，且明写细节全部取自本模块读取代码；该外部实现名在工作区无法核实，故按 §7.3 外部引用口径删除）。**复验**：`verify.ps1` **0/5**（`stripTrail` 全 d0）、`anchor-scan` 锚点 0、`template-proof` **32 个模板全同**、`content-review` 14 路径 + 3 符号全命中、`esbuild --bundle apps/viewer/src/app.ts` **exit 0** | `apps/viewer/src/replay/{timeline,sampling,player,protocol,shavit-replay}.ts` |
| 2026-09-22 | **WG4 脚本 2 件（主控自做）+ 一处潜在崩溃登记** | `apps/debug/scripts/jump-apex-window.mjs`（27→44 行）与 `apps/debug/scripts/jump-apex-trace.mjs`（46→56 行）：头注按**生产者**（`apps/debug/scripts/jump-apex-measure.mjs` 的采样器：`t/y/vy/g` 为渲染线、`ay/avg/avy` 为权威帧快照、权威取不到为 `null`）写清数据来源与字段，并写明两个脚本**时间基准不同**——`jump-apex-window.mjs` 收 `performance.now()` 绝对毫秒、`jump-apex-trace.mjs` 收相对首帧毫秒；另如实登记两处**写死常量**（`15360.97` 的 Δy 参照高度、`302.05` 的单跳起速基线，并指出同族 `jump-apex-report.mjs` 改用 2% 分位数当基线、`jump-apex-verify.mjs` 用 302）。**四道门**：`verify.ps1` **0/2**、`anchor-scan` 锚点 0、`template-proof` 12 模板全同、`content-review` 7 路径全在、`node --check` 两件 exit 0。**登记疑似缺陷（只记录不修）**：`jump-apex-trace.mjs` 的两条回退路径都失败时 `start` 仍为 −1，随后 `S[start].t` 取 `undefined` 会抛 TypeError（注释已如实写明） | `apps/debug/scripts/{jump-apex-window,jump-apex-trace}.mjs` |
| 2026-09-22 | **台账行尾归一（格式治理，内容零改动）** | `documents/plan/progress-log.md` 实测有 **9 处 bare LF 行尾**（历史行：第 63/65/66/68/69/71/73/104/105 行，系此前用脚本写入时混入）。按仓库约定（CRLF + UTF-8 无 BOM）归一为 CRLF：**bareLF 9 → 0**、CRLF 114、74599 字节；**内容逐字未改**（仅行尾字节）。归一后 `check-doc-drift.mjs` 仍 **16 篇 md / 锚点 198 / 越界 0 / 路径失效 0 / exit 0** | `documents/plan/progress-log.md` |
| 2026-09-22 | **WG6：`apps/viewer/src/app.ts` 由主控完成并复验（525 → 532 行）** | 按代码改写要点（A/B/C 类）：① 头注与两处正文里的 `t4`/`P2-4` 内部编号一律删除，改为按代码的事实表述（举例：`t4` 那句「播放基准 = .replay 帧自身坐标」保留事实、去掉编号）；② **删掉一处跨工程假断言**——原注「速度 HUD 挂 `#telemetry`（game 同款位置）」，实测 `apps/game/web/index.html` **没有** `#telemetry`（grep 0 命中），且 viewer 的 `#telemetry` 是顶层覆盖层元素（HTML 注释写「中心偏下」）、按键簇挂 `#timeline`（在 `#dock` 内）；③ 补实测事实：`resolveInitialSpawn` 的三级来源（出生点实体 → bbox 内 `info_teleport_destination` → bbox 中心高位俯瞰且 `index = −1`）、`TrackSet.replaceClip` 保留「配色/显隐/偏移/名字」、`player.seek` 夹到 `[rangeStart, rangeStop]`、`visuals.update` 收全轨道采样后按 `Track.visible` 过滤、`flashReplayStatus('')` 立即恢复持久内容、`crossOriginIsolated` 在 viewer 只打印不做通道选择（debug/game 才用它选通道）。**四道门**：`verify.ps1` **0/1**（`stripTrail` d0；`禁用词=1/基线2`，唯一命中是代码字符串）、`anchor-scan` 锚点 0、`template-proof` 20 个模板逐字节相同、`content-review` 2 条仓内路径 + 1 条「路径+符号」断言命中、`esbuild --bundle src/app.ts` **exit 0** | `apps/viewer/src/app.ts` |
| 2026-09-22 | **WG4：`jump-apex-report.mjs` 由主控完成并复验（280 → 292 行）** | 改写要点：① 删掉旧头注里**无法在代码中定位**的实测数字（「318Hz 采样、0.75s 滞空」）与「自动连跳下 onGround 沿会把一次跳跃切两段」的因果叙述，改为按代码写清**分段锚点是发射冲量**这件事本身（`vy[L] − vy[L−1] ≥ LAUNCH_DVY`）；② **修掉一处与代码不符的旧注**——旧注写「顶点允许 ±1 HU 抖动」，实际 `iApex` 只在 `y` **严格**变大时更新（容差 0.5 HU 只出现在段末判据 `y ≤ y(L) + 0.5`）；③ 补齐代码里存在但注释没写的机制：段重叠保护（`L ≤ lastAnchor` 丢弃、`lastAnchor` 随段末推进）、帧级 smoke-gun 统计的样本封顶（各 40 条、打印前 10 条）、`seconds/renderHz` 由 `dt` 均值推算（只统计 `0 < dt < 200` 的帧）、`authDts` 是 `ava` 递增的相邻帧间隔、`trace` 形参在两个调用点都传 `true`（该分支当前总会执行）、`JUMP_REPORT_OUT` 设置时「stdout 照常打印 + 另写 UTF-8 文件」并 `exit 0`。**四道门**：`verify.ps1` **0/1**（`strict d4` 经判据确认为行尾注释改写、`stripTrail` d0）、`anchor-scan` 锚点 0、`template-proof` **30 个模板逐字节相同**、`content-review` 1 条路径命中、`node --check` exit 0 | `apps/debug/scripts/jump-apex-report.mjs` |
| 2026-09-22 | **工具升级：`verify.ps1` 禁用词判据改为「按 HEAD 基线」+ 新陷阱（BOM）** | **问题**：判据原为「禁用词恒为 0」，但**面向用户的字符串字面量属代码**（实例：`apps/viewer/src/app.ts` 的 `'可能原因：浏览器禁用了 WebGL…'`），含此类字面量的文件**永远无法通过**第一道门。**改法**：新增 `$refBanned`（HEAD 文本的同款计数），输出改为 `禁用词=N/基线M`，判失败条件改为 `N > M`。**双向实测**：`app.ts` → `禁用词=1/基线2`、exit 0；向 `src/phys/seed.rs` 追加一行 `// 负例探针：大概` → `禁用词=1/基线0`、**exit 1**，逐字节还原后 SHA256 与注入前一致（`da1dd98a80246bf62ddddf5d5b1b701bb7f94d07fd457c8724a5735899712291`）且回到 exit 0。**新陷阱（血泪）**：用 `edit` 工具改 `.ps1` 会**去掉 UTF-8 BOM**（实测前 3 字节变成 `35,32,229`），Windows PowerShell 5.1 随即按 ANSI 解码并抛 `Array index expression is missing or not valid` / `Missing closing '}'` 等**假语法错**——脚本根本没执行，而 `$LASTEXITCODE` 仍可能是 0 ⇒ **假绿**。处置：改完 `.ps1` 必须补回 `EF BB BF` 并复跑确认（本次已补，文件 7463 字节、CRLF 125 / bareLF 0） | `.tmp/tools/verify.ps1`、`.tmp/tools/negtest-{append,restore}.mjs`（过程工具，不入库） |
| 2026-09-22 | **WG5 收尾：`apps/game/src/renderer/renderer-main.ts` 由子代理产出、主控独立复验（WG5 14/14 完成）** | 1750 → 1771 行（代码行 **1222 → 1222**）。**主控四道门 + 编译门实跑**：`verify.ps1` **0/1**（`strict` 的 18 处差异被脚本自身判为「行尾注释改写、代码部分逐字节相同」、`stripTrail` **1222/1222 d0**、`CRLF 1771 / bareLF 0 / 无 BOM`、`禁用词=0/基线3`、`禁用路径=0`、`块注释提前闭合=0`、`尾空白=0/基线1`——HEAD 唯一那处行尾空白位于被整体重写的 `ENABLE_PVS` 注释块内，判据「不高于基线」通过）；`anchor-scan` 锚点 0 / 违规 0 / 裸行号 0；`template-proof` **30 个模板字符串逐字节相同**；`content-review.mjs` **29 条仓内路径 + 14 条「路径+符号」断言全部命中**；`apps/game` `npm run typecheck` **exit 0**。**主控开箱复核最重的一条 A 类断言**：旧注称「WASM 导出未写入 `extras.faceIndex`，故 `getFaceCluster` 恒 −1」是**假断言**——实测 `src/wasm-core/bsp_to_gltf_core/convert.rs` 的 `add_models_to_gltf` 逐 primitive 写入 `{"faceIndex":…,"hasLightmap":…}`（grep 命中 4 处，含 `format!` 写入点），故已按「`src/ts-shared/world/pvs-manager.ts` 的 `getFaceCluster` 全仓零调用点」改写。**规模**：A 类 33 条 / B 类 5 条 / C 类 8 条（4 处 `test/dual-mode-harness/**`、1 个不存在的 `scripts/lightmap-frame-capture.mjs`、2 处已删文档分节、1 处外部库行号、1 处裸行号、1 处无路径的模块名）。**新登记疑似缺陷 5 条**（→ §7.3 #57） | `apps/game/src/renderer/renderer-main.ts` |
| 2026-09-22 | **WG6：`apps/viewer/src/replay/types.ts` 由主控完成并复验（164 → 169 行）** | A/B/C 类处置：① 删内部编号 `t4`、`Q2`；② **删已删文档引用**（`documents/viewer/implementation/shavit-replay-format.md`，C 类）与外部实现名 `replay-file.inc`（与 `shavit-replay.ts` 同口径）；③ 删无法在仓内定位的实测数字（「视角·运动方向平均 cos=0.9992」）；④ **修掉一处与代码不符的旧注**——`TrackSample` 原写「轨道已播完或未开始为 null」，实测 `apps/viewer/src/replay/tracks.ts` 的 `localTime` 是「未到片头 → null；已播完 `Math.min(local, duration)` 夹到末帧」；⑤ 补齐可核验事实：`Clip.duration = t[n−1]`（`count = 0` 时为 0）、`resolvedPath` 由 `clipFromShavitReplay` 固定写 `'.replay'`、`YawMode='shavit'` 的实现是 `decodeFrames` 的 `wrapDeg(yaw + 180)` + `clampPitch(−pitch)` + roll 0、`AxesMode='shavit'` 与 `apps/viewer/crates/wasm/src/lib.rs` 的 `rotate_yup` 同构（坐标循环置换 det=+1）；⑥ `zoneOffset` 的闭环公式**保留但改为可核验出处**——`apps/viewer/test/replay-selftest.ts` 用 `(frameCount + zo0 − (1 − zo1)) × tickInterval` 与头部 fTime 做闭环断言，删掉指向已删文档的 `§2.1`；⑦ 注明 `zoneOffset` 在本仓唯一消费者是 `apps/viewer/src/ui/replaymeta.ts`（非零时并入 title）。**四道门**：`verify.ps1` **0/1**（`stripTrail 69/69 d0`）、`anchor-scan` 锚点 0、`template-proof` OK、`content-review` **9 条路径 + 3 条符号断言全命中**、`esbuild --bundle apps/viewer/src/app.ts` exit 0 | `apps/viewer/src/replay/types.ts` |
| 2026-09-22 | **范围口径更正（实测盘点，新增 WG4b/WG5b/WG6b 三个工作组）** | 此前 §7.2/§7.4 只把 WG4 的 18 个 `.mjs` 计入「脚本」，**game/viewer 的脚本目录与 `apps/viewer/test/` 从未列入任何工作组**。本轮逐目录实测：`apps/debug/scripts` **20 个文件**（18 `.mjs` + `pages-index.html` 95 行 + `path-baseline.md` 44 行）、`apps/game/scripts` **20 个 `.mjs`**、`apps/viewer/scripts` **3 个**（`build-dist.mjs` 352、`dist-README.md` 86、`check-wasm-api.mjs` 已改）、`apps/viewer/test` **3 个**（`smoke-cdp.mjs` 875、`replay-selftest.ts` 837、`node-shims.d.ts` 9）。⇒ **未派发**：debug 13 个 `.mjs` + 1 个 html、game 19 个 `.mjs`、viewer 2 + 3 个。**另**：`apps/viewer/test/replay-selftest.ts` 自身注释含 2 处已删文档引用（`…shavit-replay-format.md` 的 `§6`、`§2.1`），登记为 WG6b 的必清项。**未擅自扩大范围**：按 §4「排除 `apps/debug/fixtures/**`」不动夹具；`path-baseline.md` 与 `dist-README.md` 按 §2 仍按脚本资产保留 | `AGENTS.md`（§7.2 新增 WG4b/WG5b/WG6b 行、§7.4 改写） |
| 2026-09-22 | **WG6：viewer `core/` 7 + `ui/` 4 由子代理产出、主控独立复验（共 11 件，1713 → 1953 行）** | **主控四道门 + 编译门实跑**：`verify.ps1` **0/11**（11 件 `strict` 与 `stripTrail` **全部 d0**——本批交付连行尾注释都没改过；`CRLF` 全对、`bareLF=0`、无 BOM；`尾空白=0/基线0`、`空行尾空格=0/基线0`；`禁用词` 两件由 HEAD 的 1 降为 0、其余 0/基线0）、`anchor-scan` 锚点 0 / 违规 0 / 裸行号 0、`template-proof` **26 个模板字符串逐字节相同**、`content-review.mjs` **63 条仓内路径 + 41 条「路径+符号」断言全部命中**、`apps/viewer` `npm run typecheck` **exit 0**（交付方另用 TS 编译器 `removeComments` 后逐字符比对做第二套独立证明，11/11 相同）。**规模**：A 类 14 条 / B 类 5 条 / C 类 4 条（C 类 = `test/dual-mode-harness` 引用、跨文件行号锚点 `config.ts:238-247`、仓外 `in_buttons.h` 与 `replay-file.inc`）+ 已删计划编号与无来源实测值 18 处（`D-08`/`D-16`/`P2-4`×2/`S7`/`S9`/`t2`/`t3`/`t11`×2 等）。**主控开箱复核两条 A 类**（不采信交付自述）：① `core/pose.ts` 旧注「viewer 内 20 余处 `from './pose.js'` 调用方」实测**恰 5 处**（`apps/viewer/src/app.ts`、`ui/mapinfo.ts`、`replay/helpers.ts`、`core/fly.ts`、`core/spawn.ts` 各 1）；② `core/constants.ts` 旧注把 `MOUSE_MAX_DELTA` 说成「指针锁定后丢弃下一个 mousemove（2000-5000+ px 跳变）」，实测该常量 = **1000**、是 `FlyCam.delta` 的**单次事件绝对值上限**，而「丢弃首个 mousemove」是另一机制 `FlyCam.discardNextMouse`（在 `pointerlockchange` 无条件置位，不比像素阈值）。**新登记疑似缺陷 10 条**（→ §7.3 #58，含 `ensureWasm` 首次失败永久缓存、`fly.ts` 两个死字段、`setLightGamma(2.2)` 越窗等） | `apps/viewer/src/core/{pose,constants,spawn,bsp,dom,fly,scene}.ts`、`apps/viewer/src/ui/{telemetry,replaymeta,hud,mapinfo}.ts` |
| 2026-09-22 | **文档行尾归一（格式治理，内容零改动）+ 新陷阱入规范篇** | 实测本批新建的 **6 篇** `overview.md`（`documents/{architecture,materials,norms,phys,ts-shared,wasm-core}/`）落盘时**全是 LF**（`CRLF=0`、`bareLF=73/87/66/85/120/149`）——写作工具产出 LF，而**没有任何门会拦文档行尾**（`verify.ps1` 只覆盖代码文件）。已用 `.tmp/tools/normalize-md-eol.mjs` 逐篇把孤立 LF 归一为 CRLF（**只改行尾字节**；本仓索引恒存 LF，故入库内容本就一致）：6 篇合计 `bareLF 580 → 0`；`AGENTS.md` 与三篇控制文件/台账原本就是 CRLF（复查 `OK`）。归一后 `check-doc-drift.mjs` 仍 **exit 0 / 越界 0 / 路径失效 0**。**该陷阱（含「`Select-Object -First N` 截断上游进程导致假退出码」「`edit` 工具改带 BOM 的 `.ps1` 会丢 BOM ⇒ PowerShell 假语法错」）已写入 `documents/norms/annotation-and-verification.md` §5 陷阱清单第 8–10 条** | `.tmp/tools/normalize-md-eol.mjs`、`documents/norms/annotation-and-verification.md`、6 篇 `overview.md` |

## §7.3 规则与工具（逐行原样移出，内容未改）

> 以下 11 行自根 `AGENTS.md` 的 §7.3 表**逐行原样**移入（2026-09-22，治理动作：AGENTS.md 逼近 65536 字节注入上限）。它们记录的是**已验证的规则、过程工具与已解决经验**，不是待决项；同内容的陷阱清单另见 `documents/norms/annotation-and-verification.md` §5。

| 17 | **验证陷阱（已固化为检查项）**：① Windows 下 `git show "HEAD:<path>"` **必须用正斜杠**，反斜杠会报 "exists on disk, but not in 'HEAD'" 并让参照集为空，随后 `Compare-Object` 抛错、被 `if ($d)` 静默判为"无差异"→ **输出假 PASS**；② 校验脚本必须先断言参照集非空；③ 子代理写入期间**不得**做"逐行一致/来源审查"判定（实测 `teleport.rs` 在两次读取之间自行修复了中途状态，中间态会误判为代码改动）；④ `cargo test` 经 PowerShell 管道会带出与结果无关的 `[exit code: 1]`，判据取 `$LASTEXITCODE`（`*> $null` 后取）而非管道退出码；⑤ **过滤注释行必须用字面判断**：`-notlike '*'` 里 `*` 是通配符、匹配一切，会把两侧都清空成 0 行从而再次假通过；正确写法是 `.StartsWith('//')` / `.StartsWith('/*')` / `.StartsWith('*')`（本轮 TS 校验即因此先产出 `ref=0 cur=0` 的假 PASS，改正后 `constants.ts` 1/1、对照件 18/18）；⑥ **比对代码时要同时跑两遍**：只按「整行是否以注释开头」过滤，会把**行尾注释**当成代码——`phyfile.rs` 因此误报 `DIFF=6`；必须补一遍「剥离首个 `//` 之后内容」的比对，两遍都无差异才算代码未改 | 已解决；六条均已写入本表并用于复验 |
| 18 | **⚠ 脚本自伤规则（血泪）**：**禁止**用哈希表 + 嵌套数组 `@{ path = @(@('旧','新')) }` 存替换对再 `$p[0]`/`$p[1]` 取值——PowerShell 会**摊平嵌套数组**，`$fixes[$path]` 退化成**字符串**，此时 `$p[0]`/`$p[1]` 是字符串的**第 0/1 个字符**，于是"整串替换"静默变成"单字符替换"。本轮据此把 `：`→`V`（35 处）、`新`→`建`（3 处）、`地`→`图`（9 处），损坏 3 个文件、被迫整体还原重做。**此后规则**：① 批量替换一律写成**元组数组**并对每个元组用**具名变量**（`foreach ($pair in $pairs) { $old = $pair[0]; $new = $pair[1] }`）或直接用 `[pscustomobject]`；② 替换后**必须回读并断言**"旧串计数为 0 且新串计数 = 预期"；③ 批量改动前先 `git stash`/记 hash，损坏时用 `git checkout HEAD -- <file>` 还原并**如实登记**，不得靠字符级逆向修补 | 已恢复并登记 |
| 19 | **⚠ 锚点腐烂规则（本次血泪，改写 §3 S3）**：跨文件 `path:line` 锚点在**目标文件被后续工作组重编**后必然失效，实例即 §7.1 的 `teleport.rs` → `angles.ts` 的 `bspYawToCsYaw` 一处（WG3 重写使 38 行 → 30 行；该失效锚点连带把 `AGENTS.md` 自身的两处引用打成越界，已改为符号引用，漂移体检随之回到 0 越界）。更危险的是"行号仍在范围内但内容已变"——那种错**静默**。**此后规则**：代码注释内**一律不写行号**，同文件用符号名、跨文件用「相对仓库根路径 + 符号名」（符号不存在时会直接暴露，不会静默错）；文档仍按任务书 §11 写 `文件:行号`（文档在代码冻结后编写，由 `check-doc-drift.mjs` 校验）。**WG11 债务**：现存已重编文件里仍有**简写锚点 61 处（已解析）+ 裸行号 25 处**（`src/phys/mod.rs` 16 处、`src/phys/teleport.rs` 6 处等），WG11 需跑 `.tmp/tools/anchor-scan.mjs` 全量去行号并复核 | **规则已生效；债务已清偿（2026-09-22 本轮）**：`src/phys` + `src/wasm-core` 共 33 件的 `文件:行号` 锚点全部改为符号引用（示例：`renderer-main.ts:1418` → 「`apps/debug/src/renderer/renderer-main.ts` 的 `RendererMain.consumePhysEvents`」、`tick-authority.ts:400/:513` → 「`src/ts-shared/auth/tick-authority.ts` 的 `publishMeta`」）。**全仓复扫：106 个已改动文件 / 锚点 0 处 / 违规 0 / 裸行号 0**；该批复验 0/33 失败（`stripTrail` 全 d0）、`cargo test -p websurf-phys` 10 passed、`cargo check -p websurf-wasm-core` exit 0、**内容审查 53 条「路径+符号」断言全部命中** |
| 21 | **`apps/game` 的「工程内隔离副本」叙述与代码不符（A/C 类，待 WG5 处理）**：`apps/game/crates/wasm/src/lib.rs` 头注称「本工程内的隔离副本 crates/wasm-core（仓库根 src/wasm-core/ 的逐字节副本，websurf-wasm-core 0.1.0-fork）…副本化的授权、范围与回并路径见 `documents/game/implementation/lighting-merge-plan.md` §9.1 与 §6.2」。实测 **`apps/*/crates/` 下只有 `wasm`**，三个工程的 `crates/wasm/Cargo.toml` 一律 `websurf-wasm-core = { path = "../../../../src/wasm-core" }`（其自身注释已写「共享解析层（仓库根 src/wasm-core/）」）；`bspfile.rs`/`mtz.rs`/`vhv.rs`/`phyfile.rs` 全仓各只有一份。所引文档亦属已删文档。**唯一事实：三工程共用同一份 `src/wasm-core`** | **已解决（WG5 本轮按代码改写并复验）**：`apps/game/crates/wasm/src/lib.rs` 头注已改为「三工程共用同一份共享解析层」；主控复验 `verify.ps1` 0 失败、`cargo check` exit 0 |
| 23 | **过程工具**（两件，均在 `.tmp/tools/`，不入库）：① `anchor-scan.mjs`（锚点扫描，`node .tmp/tools/anchor-scan.mjs <文件...>`）；② `verify.ps1`（单/多文件复验，`& .tmp/tools/verify.ps1 -Files <文件...>`：参照集非空断言 + 两遍代码比对 + 行尾空白 + CRLF/bareLF/bareCR/BOM + 禁用词 + 禁用路径 + 外部锚点计数，任一不过 exit 1）。**两个坑**：本机 PATH 里**没有 `pwsh`**，只能用 `& <路径>` 调用；含中文的 `.ps1` **必须带 UTF-8 BOM**，否则 Windows PowerShell 5.1 按 ANSI 解码、整片乱码报语法错。另：`src/scripts/check-doc-drift.mjs` **只扫 `documents/**/*.md`**（实测"9 篇 md"），**完全不覆盖代码注释里的锚点**——凡涉及 `src/**`、`apps/**` 注释锚点的验收，必须另跑 `anchor-scan.mjs` | 已可用；`verify.ps1` 已升级为 **13 项**（新增「块注释提前闭合」，见 #35）。**本轮又补一个健壮性缺口**：脚本原先对**工作区不存在的路径**直接 `Resolve-Path` 抛错——在 `$ErrorActionPreference='Stop'` 下会**终止整个批次**，从而吞掉后续文件的结果与末尾汇总行（实测：把 `git status` 的全部改动 `.ts` 传进去时，因列表内含 39 个已删除的 harness 条目而在末尾中断，`校验失败` 汇总行未打印、退出码变成「脚本报错」而非「校验结论」）。已改为：**路径不存在 ⇒ 打印「工作区不存在（已删除？）——跳过」、计入失败、continue**；负例实测（传一个已删路径 + 一个正常文件）→ 正确输出「缺失」行 + 正常文件的 13 项指标 + 汇总 `1 / 2` + exit 1，**批次不再中断** |
| 25 | **⚠ 我的 S4 盲区（如实登记）**：`src/wasm-core/texture_utils/mod.rs` 曾以「代码逐行一致 / 禁用词 0 / 锚点 0 违规」通过我的复验，但它含一句**跨文件假断言**——「`bsp_to_gltf_core::materials` 走同一条解码路径」。实测：`materials.rs` 用的是**外部 `vtf` crate**（`src/wasm-core/Cargo.toml` 的 `vtf = "0.3"`，调用形如 `vtf::vtf::VTF::read`），该文件全文**不含** `texture_utils`。**盲区根因**：我的复验只验「代码未改 + 无禁用词 + 锚点可达」，**不验注释里跨文件事实的真假**。此事由 `vtf.rs` 的子代理发现，已按代码改写。**补强措施**：① 跨文件引用一律落到**符号名**（符号不存在会直接暴露）；② 复验时**抽 ≥3 条跨文件断言 grep 反查**；③ 对「他人文件的断言」尤其要查——本件的问题正是断言了别人模块的行为 | 已修正 `texture_utils/mod.rs`；判据已补强 |
| 35 | **⚠ 块注释提前闭合（新失败模式，已固化为 `verify.ps1` 第 13 项）**：块注释的续行里只要出现 `*/`，注释就被**提前闭合**，其后全部注释文本会被当作代码解析。触发实例：在注释里写 glob `src/ts-shared/**/*.ts`（`**/` 含 `*/`）。**此错对「代码逐行一致 + 禁用词 + 锚点」三套判据全部不可见**（那些行仍以 `*` 开头被跳过，`stripTrail` 照样 d0），**只有 `tsc` / `cargo` 才会暴露**。⇒ 规则：① 注释内**不得写**含 `**/` 的 glob 或任何 `*/` 序列（改用「`X` 目录下的全部 `.ts`」这类表述）；② 每件必须跑编译门（TS 三工程 `npm run typecheck`，Rust `cargo check`），**不得只靠 `verify.ps1` 判定通过**；③ 已负例实测新检查项会拦（插入 glob → `块注释提前闭合=1` + exit 1） | 规则已生效；工具已升级为 13 项 |
| 42 | **新陷阱 + 新增两条内容审查手段（复用价值高）**：① **陷阱**：PowerShell 正则 `(?m)^(\s*)X(\s*)$` 中 `\s` **会吃掉行尾的 `\r`**，替换串若不回补 `${2}` 就会把该行变成 bare LF（实测 `apps/debug/src/app.ts` 因此报 `CRLF=2534 / bareLF=1`）⇒ 行尾用 `[ \t]*` 或显式回补 CR，改完必跑 `verify.ps1` 看 CRLF/bareLF 计数。② **审查手段 A**：自动抽取注释里的**仓内路径**引用（`(apps|src|test)/….(ts|rs|mjs|json|html|toml)`）并 `Test-Path` 批量核验——本轮 16 件共 **42 条路径全部存在**。③ **审查手段 B**：抽取「`` `路径` `` 的 `` `符号` ``」型跨文件断言，逐条验证**目标文件里确有该符号**——本轮 **53 条全部命中**。两条手段把 §7.3 #25「跨文件假断言」的盲区从"抽 3 条人工看"升级为"全量机器核验" | 已固化为复验步骤（脚本式，可复用） |
| 44 | **⚠ 新盲区 + 新工具（本轮最重要的验证升级）**：`verify.ps1` 的 `strict`/`stripTrail` 两轮**都看不见模板字符串内部的改动**——模板里以 `//` 开头的行（GLSL/HTML/多行文案）会被当成「注释行」剥离，于是**在该字符串内改注释等于改代码却不被报出**。**实例**：`apps/debug/src/renderer/lightmap-shader.ts` 的子代理在**GLSL 源码字符串内**改了两处注释（删 `2026-09-21` 日期、把「要重编几百个 program（实测 1.4~2.5 s 冻结）」改写），`verify.ps1` 报 `strict 890/890/d0`、`stripTrail 890/890/d0`、**完全通过**。**处置**：① 新建 `.tmp/tools/template-proof.mjs`——用 TypeScript 编译器 API 取**真正的模板字面量节点**（`NoSubstitutionTemplateLiteral` / `TemplateExpression`），与 `git show HEAD:<file>` 逐模板比对（CRLF 归一化）；② 已把该件两处**逐字回滚**（回滚后 `templates HEAD=11 CUR=11 diff=0`），并重新复制到 `apps/game`、`apps/viewer`；③ **推广到全部 48 个已改动代码文件**：`files checked=48 template-literals=236 files-with-changes=0`（除那两个尚未回滚的副本外全绿）；④ 此后**每个含模板字符串的文件必须跑 `template-proof.mjs`**，与 `verify.ps1` 并列。**注意**：`template-proof.mjs` 的比对基线是 `git show HEAD`，**对未入库文件无效**（会 SKIP）。**2026-09-22 补充（误报修正）**：该工具对**非 JS/TS** 文件（`.rs`、`.md` 等）会用 TS 解析器按 JS 硬解析、退化成「反引号配对」启发式 ⇒ 注释里新增的 `` `路径` ``/`` `符号` `` 引用被误报成模板改动（实例：`src/wasm-core/mosaic/mtz.rs` 报 `TEMPLATE-CHANGED (templates=0, diff=1)`，而该件 `verify.ps1` 的 `stripTrail 797/797 d0` 已证代码逐字节未改；`AGENTS.md` 同因被误报 `diff=894`）。已加扩展名白名单：非 JS/TS 打印 `SKIP(非 JS/TS，反引号启发式不适用)`。**修正后 135 个静止期文件实跑**：`files checked=98 template-literals=430 files-with-changes=0 skipped(非 JS/TS)=37` → **PROOF-OK** | 已固化为第 4 道门（含扩展名白名单） |
| 46 | **WG8–WG10 文档树路径口径（定稿，供后续执行者直接照用）**：P2 五篇 = `documents/architecture/overview.md`、`documents/phys/overview.md`、`documents/wasm-core/overview.md`、`documents/ts-shared/overview.md`、`documents/materials/overview.md`；P3 每工程 = `documents/<app>/README.md` + `overview.md` + `sequences.md` + `differences.md` + `documents/<app>/implementation/*.md`；P4 = 根 `README.md`、`CHANGELOG.md`、`documents/norms/*.md`（规范类）、`documents/index.md`（导航，**最后按实际存在的文件重建**）。**格式约定**：文档内锚点一律写「相对仓库根路径:行号」，由 `check-doc-drift.mjs` 校验（越界即失败）；**裸文件名 + 数字**会被判为「行数声明」并与实测比对（本轮已实测踩到一次，见 §7.3 #47 的同类问题）；文档正文同样受 §6.3 禁用措辞约束。**已实测的校验盲区**：`check-doc-drift.mjs` 只扫 `documents/**` 的 md，**不覆盖代码注释**（代码侧另有 `anchor-scan.mjs`） | 已生效 |
| 56 | **⚠ 工具升级 + 新陷阱（`verify.ps1` 判据）**：① **禁用词改为按 HEAD 基线判**——面向用户的字符串字面量属**代码**，本来就含这些字样（实例：`apps/viewer/src/app.ts` 的 `'可能原因：浏览器禁用了 WebGL…'`），原先「恒为 0」的判据会让含此类字面量的文件**永远无法通过**；现输出 `禁用词=N/基线M`，判失败条件改为 `N > M`。**双向实测**：`app.ts` 现 `禁用词=1/基线2` → exit 0；向 `src/phys/seed.rs` 追加一行 `// 负例探针：大概` → `禁用词=1/基线0` + **exit 1**，逐字节还原后 SHA256 与注入前一致（`da1dd98a…`）且回到 exit 0。② **新陷阱：`edit` 工具写 `.ps1` 会去掉 UTF-8 BOM**——实测改完 `verify.ps1` 后前 3 字节变为 `35,32,229`（`#`、空格、`0xE5`），Windows PowerShell 5.1 遂按 ANSI 解码并抛 `Array index expression is missing or not valid` / `Missing closing '}'` 等**假语法错**（脚本根本没跑，`$LASTEXITCODE` 还可能是 0 ⇒ **假绿**）。**处置**：改用 `edit` 工具动过 `verify.ps1` 后**必须补回 BOM**（`EF BB BF`）并复跑一次确认；已补（7463 字节、CRLF 125 / bareLF 0） | 已升级并双向实测 |
| 2026-09-22 | **WG6：viewer `replay/` 剩余 5 件由子代理产出、主控独立复验 + 一处注释修正（28/29）** | 范围与行数：`panel.ts` 318 → 364、`trackpanel.ts` 214 → 239、`visuals.ts` 207 → 248、`importer.ts` 176 → 237、`tracks.ts` 112 → 132。**主控四道门 + 编译门实跑**：`verify.ps1` **0/5**（`tracks.ts` 的 `strict d2` 经判据确认为行尾注释改写、其余四件 `strict`/`stripTrail` **全 d0**；`CRLF` 全对、`bareLF=0`、无 BOM、`尾空白/空行尾空格` 均 0/基线0；`禁用词` 全 0）、`anchor-scan` 锚点 0 / 违规 0 / 裸行号 0、`template-proof` **17 个模板字符串逐字节相同**、`content-review.mjs` **24 条仓内路径 + 14 条「路径+符号」全命中**、`apps/viewer` typecheck **exit 0**。**规模**：A 类 24 条 / B 类 7 条 / C 类 0 条；跨文件断言 51 条经交付方脚本反查全命中（含 2 条**故意放的负例探针**被正确报出）。**主控开箱复核并修正 1 处**：`visuals.ts` 新稿把 `MAX_TRAIL_POINTS` 的抽稀式写成「步长 = `ceil(count / 本值)`、实际点数不超过本值 **+ 1**」——实测代码是 `stride = max(1, ceil(total / MAX_TRAIL_POINTS))`（`total = clip.count`）与 `count = floor((total − 1) / stride) + 1`，**恒 ≤ 本值**（`total ≤ M` 时 `stride = 1` ⇒ `count = total ≤ M`；`total > M` 时 `stride ≥ total/M` ⇒ `(total−1)/stride < M` ⇒ `count ≤ M`），且注释把 `total` 误写成 `count`（同函数内 `count` 另有含义，语义自撞）。已按代码改写两处注释；改后复验 `verify.ps1` 0/1、锚点 0、`template-proof` OK、`content-review` 5 路径 + 4 符号全命中 | `apps/viewer/src/replay/{panel,trackpanel,visuals,importer,tracks}.ts` |
| 2026-09-22 | **中期 WG11 闸门（静止期全量实跑，排除在跑件）** | 取 `git status` 全部 `M` 且存在、再排除 14 件在跑文件的 **135 件**做全量闸门：① `verify.ps1` **133 / 135 通过**——两处「失败」是 `AGENTS.md` 与 `.gitignore`，二者**不属代码注释范围**（前者是本次任务的规范/台账、含被引用的禁用词样本与指向已删文档的历史指针；后者是配置文件的归档注释），其余 133 件 `stripTrail` 逐行 d0、`strict` 差异全部经判据确认为行尾注释改写、`禁用词` 一律不高于基线。② `anchor-scan` **锚点 0 / 违规 0 / 裸行号 0**。③ `template-proof`（**修正后**：加 JS/TS 扩展名白名单）`files checked=98 template-literals=430 files-with-changes=0 skipped(非 JS/TS)=37` → **PROOF-OK**。④ 编译门：`cargo test -p websurf-phys` **exit 0**、`cargo check -p websurf-wasm-core` **exit 0**、`cargo check`（`apps/{debug,game,viewer}/crates/wasm`）**三件全 exit 0**、三工程 `npm run typecheck`（debug/game/viewer）**全 exit 0**。⑤ 文档体检 `check-doc-drift.mjs` **16 篇 md / 行数声明 7（漂移 0）/ 锚点 199（越界 0）/ 路径失效 0 / exit 0** | 全仓（135 件静止期文件） |
| 2026-09-22 | **台账二次瘦身（治理动作，内容零改动）+ 工具误报修正** | ① `AGENTS.md` 由 **56272 → 42521 字节**：把 §7.3 的 **11 条规则 / 工具说明 / 已解决项**（#17/#18/#19/#21/#23/#25/#35/#42/#44/#46/#56）**逐行原样**移入本文件的「§7.3 规则与工具（逐行原样移出）」小节（脚本迁移，目标行按 `\| <id> \|` 精确匹配、缺一即中止，零改写）；指针行同步更新，§3 S3 与 §7.1 的两处跨条引用改为指向本文件的小节名。原因：AGENTS.md 逼近 65536 字节注入上限（曾因此被静默截断）。② **`template-proof.mjs` 误报修正**：该工具对**非 JS/TS** 文件（`.rs`/`.md`）会用 TS 解析器按 JS 硬解析、退化成「反引号配对」启发式，于是注释里新增的 `` `路径` ``/`` `符号` `` 引用被误报（实例 `src/wasm-core/mosaic/mtz.rs`，而该件 `stripTrail 797/797 d0` 已证代码未改）；已加扩展名白名单并对非 JS/TS 打印 `SKIP`。③ 文档行尾归一（6 篇 `overview.md` 由 LF → CRLF，内容零改动）+ 三条新陷阱写入规范篇 §5（第 8–10 条） | `AGENTS.md`、`documents/plan/progress-log.md`、`.tmp/tools/{slim-agents,template-proof,normalize-md-eol}.mjs`、`documents/norms/annotation-and-verification.md` |
| 2026-09-22 | **WG4b：debug 脚本 8 件批由子代理产出、主控独立复验（脚本 13/18）** | 范围：`jump-apex-smoke`、`glb-mesh-count`、`jump-apex-auth-diag`、`phys-surf-crouch-smoke`、`optimize-scene-verify`、`frame-bench`、`build-dist`、`jump-apex-serve`。**主控四道门 + 语法门实跑**：`verify.ps1` **0/8**（`stripTrail` 全 d0；`strict` 的 d2/d14/d2/d8 全被判为行尾注释改写；两件 `禁用词=1/基线1` 位于代码字符串内）、`anchor-scan` 锚点 0 / 违规 0 / 裸行号 0、`template-proof` **91 个模板字符串逐字节相同**、`content-review.mjs` **25 条仓内路径 + 9 条「路径+符号」全命中**、`node --check` 8/8 exit 0。**A 20 / B 2 / C 6**（C 类含不存在的 `.tmp/glb-mesh-count.mjs`、已删文档编号 `D-04/T-04/§5.2 R-15/D-23/E-08`、退役 harness 字样、外部实现名 `CanUnduck`）。**主控开箱复核两条**：① `frame-bench.mjs` 的缺省地图路径确实是 `join(repoRoot,'maps','surf_666.bsp')`，而 `Test-Path maps` = **False** ⇒ 不带参数必以 2 退出；② `phys-surf-crouch-smoke.mjs` 的 `git ls-files --eol` 现为 `i/lf w/crlf` ⇒ 该件工作区原为纯 LF、已按仓库约定归一（交付方主动披露；其余 7 件本来就是 CRLF）。**由此更正一处旧盘点错误**：`Get-Content` 对 **LF/CR 结尾混杂**的文件会漏计行数（本件旧记 145，实为 157→160），故台账里凡「未冻结文件」的行数已**一律不再写**（规范篇 §6 既有规则）。**新登记疑似缺陷（→ §7.3 #60）**，其中第 ① 条是**链路级**：`jump-apex-measure` 的就绪判据与采样读的是**扁平**字段，而 `RendererMain.getCurrentState()` 返回**嵌套**结构 ⇒ 整条 jump-apex 采样链当前跑不通（属代码改动，待 owner 裁决） | `apps/debug/scripts/**`（8 件） |
| 2026-09-22 | **WG6 末件：`apps/viewer/crates/wasm/src/lib.rs` 由子代理产出、主控独立复验（29/29 完成）** | 497 → 545 行（+48 全为注释行）。**主控四道门 + 编译门实跑**：`verify.ps1` **0/1**（`strict 355/355 d0`、`stripTrail 355/355 d0`、CRLF 545 / bareLF 0 / 无 BOM / 尾空白 0/基线0 / 禁用词 0/基线0）、`anchor-scan` 锚点 0、`template-proof` 该件按设计 `SKIP(非 JS/TS)` 且总判 `PROOF-OK`、`content-review` 7 路径 + 4 符号全命中、`cargo check`（`websurf-viewer-wasm`）**exit 0**；`git ls-files --eol` = `i/lf w/crlf` ⇒ 该件工作区原为 LF、已归一（交付方主动披露并附「内容逐行与 HEAD 一致」断言）。**A 类 9 / B 类 2 / C 类 1**：C 类是把本仓**共享层自有类型** `Packfile` 说成外部 `vbsp 0.6.0` 的私有字段（本仓不依赖该 crate）；A 类含「`app.ts` 只调用三方法」（实为 `apps/viewer/src/core/bsp.ts` 唯一导入 `pkg/`）、「classname 列表按优先级排序」（实为 `.any()` 匹配、顺序无关；优先级由独立 `primary` 判据决定）、材质函数漏写第三张表（无光照名集合）、`static_props` 声称与碰撞体共用（本工程无碰撞导出）、`map_name` 恒为空串而旧注未提。**新登记疑似缺陷 6 条（→ §7.3 #61）**，最重一条是 `.MDL` 三件套替换隐患（判据大小写不敏感而配对名替换大小写敏感 ⇒ `.MDL` 条目会让 `vvd`/`vtx` 槽位填进同一份 `.mdl` 字节）。**另登记待决**：`apps/viewer/pkg/websurf_viewer_wasm.d.ts` 是 wasm-bindgen 产物、会整段抄走 Rust 文档注释，按 §4 未动 `pkg/`，需 `npm run build:wasm` 才会对齐（属构建动作） | `apps/viewer/crates/wasm/src/lib.rs` |
| 2026-09-22 | **台账治理（三轮，内容零改动）+ 一次自伤修复** | ① **§7.3 #58/#59 曾被一次错误编辑合并成一行**（后一行内容被并进前一行的状态格），已用 `.tmp/tools/fix-row58-59.mjs` 按 ` |：` 边界**拆回两行**（AGENTS.md 45295 → 45434 字节），复核三行行首标签 #55/#58/#59 齐备。② **「行数声明漂移」自伤**：我在 §7.4 给**仍在重编**的 `apps/viewer/crates/wasm/src/lib.rs` 写了「（444 行）」，`check-doc-drift.mjs` 立刻判 **exit 1**（声明 444 / 实测 545——该件正被在跑子代理写大）；已用 `.tmp/tools/strip-counts.mjs`（+ 残留清理）把 WG4b/WG5b/WG6b 三行与 §7.4 第 1–3 条里的**未冻结文件行数一律去掉**（6 行、约 60 处数字），体检回到 **exit 0（行数声明 1 / 漂移 0）**。**这正是规范篇 §6 既有规则「台账里提到仍在重编的文件时不写行数」，本轮由我违反**——已如实登记。③ §7.4 第 2、3 条与 §7.2 的 WG4/WG6 行同步到最新派发状态（可用 `.tmp/tools/update-74.mjs` 复核） | `AGENTS.md`、`.tmp/tools/{fix-row58-59,strip-counts,finalize-ledger,update-74}.mjs` |
| 2026-09-22 | **WG4b：`pages-index.html` 按源码逐条核对并改写（主控自做；按脚本资产处理）** | 实测该件被 `.github/workflows/deploy-pages.yml` 的 `cp apps/debug/scripts/pages-index.html deploy/index.html` 消费（部署站入口页模板），故按 §2 归**脚本资产**、与 `dist-README.md` 同口径。**逐条回代码核对（5 条全命中）**：① 「本项目的默认蹲键也是 Ctrl」命中 `apps/debug/src/input/keyboard.ts` 的 `ControlLeft`/`ControlRight` 映射（值 `duck`）与 `apps/game/src/input/keymap.ts` 的 `duck` 默认数组（两处都含 `ControlLeft`/`ControlRight`）；② 「Game Build 依赖 SharedArrayBuffer（需 COOP/COEP），GitHub Pages 不提供 → 自动回退」命中 `apps/game/src/app.ts` 的通道选择（`crossOriginIsolated` 且 `typeof SharedArrayBuffer !== 'undefined'` 才建 SAB，否则 `sharedBuffer = null`）与 `src/ts-shared/auth/shared-state.ts` 的 `createMainSharedState` / `createWorkerSharedState`（`buffer` 为 null → `MsgState`）；③ 「ESC → 按键 → 改绑蹲」命中 `apps/game/src/panel/panel-controller.ts` 的 `keys（按键）` 模块（`renderKeyList` / 录制重绑 / 恢复默认键位）；④ 三个入口链接 `./debug/`、`./game/`、`./viewer/` 与 `deploy-pages.yml` 的 `mkdir -p deploy/${app}` + `cp -r` 组装布局一致；⑤ 三个代号 `WebSurf-debug` / `WebSurf-game` / `WebSurf-viewer` 分别在 `apps/{debug,game,viewer}/web/index.html` 的 `<title>` 可定位。**改写 2 处（B 类事实错误）**：① 旧文写线上「功能受限」，与代码自带状态文案冲突——`apps/game/src/app.ts` 的 `setStatus('兼容模式（无 SharedArrayBuffer）：功能可用，性能降级')`，已改为「功能可用、性能降级（自动回退到 MsgState 消息通道）」；② 旧文写「运行 `game/play.cmd`」，实测根级无 `game/` 目录、脚本在 `apps/game/play.cmd`（`Test-Path` 三工程各 True），已改路径。**删 1 处不可定位断言（C 类）**：「Edge 实测可以先按 W 再按 Ctrl 录进去」在工作区内无任何可核对来源，改写为「若被占用录不进去，可试着先按 W 再按 Ctrl」（去掉被测量主体与浏览器名，保留操作提示）。**四道门 + 新增第 5 道结构门**：`verify.ps1` 结构性计数全绿（`行=94 CRLF=94 bareLF=0 BOM=False 尾空白=0/基线0 空行尾空格=0/基线0 禁用词=1/基线1 禁用路径=0 外部锚=0 块注释提前闭合=0`），其 `strict` / `stripTrail` **d5 即本次文本改写本身**（HTML 无「注释 vs 代码」之分，工具按代码处理 ⇒ 按资产口径披露为预期差异，非失败）；`anchor-scan` 锚点 0 / 违规 0 / 裸行号 0；`template-proof` `SKIP(非 JS/TS)` + 总判 `PROOF-OK`；**新建 `.tmp/tools/html-struct-proof.mjs`**——比对 `git show HEAD:<file>` 与工作区的 `<...>` **标签序列**并要求逐项相同（证明只改文本节点、未动结构）：首稿把 `<strong>…</strong>` 一并删掉，立即被判 **HTML-STRUCT-DIFF 12（HEAD=114 → work=112 标签）**，补回 `<strong>` 包裹后复跑 `HEAD=114 work=114` → **HTML-STRUCT-PROOF-OK**（该工具与 `stripTrail`/`template-proof` 并列，用于一切带标签的资产） | `apps/debug/scripts/pages-index.html`、`.tmp/tools/{html-struct-proof,append-row}.mjs` |
| 2026-09-22 | **WG4b：debug 脚本 4 件批（`jump-apex-measure` / `jump-apex-verify` / `plot-path` / `path-acceptance`）由子代理产出、主控独立复验（脚本 17/18）** | 代码行 279 / 230 / 240 / 262 全部与 HEAD 逐行相同（四件 `stripTrail` 全 d0；`strict` 的 d4/d12/d4/d2 经判据判为行尾注释改写）。**主控四道门 + 语法门实跑**：`verify.ps1` **0/4**（CRLF 全对、bareLF 0、无 BOM、尾空白 0/基线0、禁用词 0/基线0、块注释提前闭合 0）、`anchor-scan` 锚点 0 / 违规 0 / 裸行号 0、`template-proof` **78 个模板字符串逐字节相同**、`content-review` **18 条仓内路径 + 8 条「路径+符号」全命中**、`node --check` 4/4 exit 0。**主控开箱复核 3 条 A 类**（不采信交付自述）：① `apps/debug/src/renderer/renderer-main.ts` 的 `RendererMain.getCurrentState()` 实测返回**嵌套** `{pos:{x,y,z}, yaw, pitch, vel:{x,y,z}, onGround}`，而 `jump-apex-measure.mjs` 的就绪判据读扁平 `diag.posY` ⇒ 恒为假；② `plot-path.mjs` 的输出目录实测为 `plot-` + 输入文件**基名**（不是时间戳目录）；③ 该件 `accel=false` 时确实把 `cal.computeAuthAccel` 覆写为恒零向量。**规模 A 31 / B 3 / C 2**（C 类含指向不存在的 `debug/` 目录、把 `.gitignore` 的 glob 照抄进块注释 ⇒ 提前闭合，被 `node --check` 与 verify.ps1 第 13 项当场抓到）。**一处只能由工具侧解决的门禁事实（已处置）**：`jump-apex-verify.mjs` 的**代码**里有 3 处退役 harness 路径（`HARNESS_PKG` / `HARNESS_WASM` 常量 + SKIP 分支文案），属代码禁改，而 `verify.ps1` 的「禁用路径」原判据是「恒为 0」⇒ 该件永远不可能通过。已把判据改为**与禁用词同口径的 HEAD 基线判**（输出 `禁用路径=N/基线M`，失败条件 `N > M`），**双向实测**：四件 `0 / 4` 通过（该件 `3/基线3`）；临时在注释行追加一处 `dual-mode-harness` ⇒ `4/基线3` + exit 1，逐字节还原后 SHA256 与注入前一致。工具改后已补回 UTF-8 BOM（`EF BB BF`，8045 字节）并复跑确认；判据变更同步写入 `documents/norms/annotation-and-verification.md`（门表第 1 行 + 陷阱第 11 条）。**新登记疑似缺陷 2 条（→ §7.3 #60 ⑨⑩）** | `apps/debug/scripts/{jump-apex-measure,jump-apex-verify,plot-path,path-acceptance}.mjs`、`.tmp/tools/verify.ps1`、`documents/norms/annotation-and-verification.md` |
| 2026-09-22 | **WG5b：`t13-*` 三件由主控自做并复验** | 三件与行数：`t13-literal-sweep.mjs` 126 → 139、`t13-ulp-sensitivity-control.mjs` 125 → 136、`t13-input-surface-probe.mjs` 152 → 160。**四道门 + 语法门**：`verify.ps1` **0/3**（`stripTrail` 全 d0、`t13-input-surface-probe` 的 `strict d4` 判为行尾注释改写；CRLF 全对、bareLF 0、无 BOM、禁用词 0/基线0、禁用路径 0/基线0）、`anchor-scan` 锚点 0 / 违规 0、`template-proof` 4 个模板逐字节相同、`content-review` **12 条仓内路径 + 5 条「路径+符号」全命中**、`node --check` 3/3 exit 0。**A 类硬伤逐条回代码改写**：① `t13-literal-sweep` 头注称字面量「分三类」，代码实为**四类**（R/F/I/D）；同件 F 类量级注释写 `1e-3..1e6`，而代码是 `2^(−10..9)`（9.8e-4 .. 512）—— 均按代码改写。② 该件原头注断言「fast parser（OFF）与 float_roundtrip（ON）两构建」，而脚本只吃一个 `--wasm-dir`、仓内**没有**生成 OFF 变体的脚本 ⇒ 改写为「把另一构建的产物目录传进来即可对比两条解析路径」+ 可定位的 feature 事实（`src/Cargo.toml` 给 `serde_json` 开了 `float_roundtrip`，`src/wasm-core/Cargo.toml` 与三工程 `crates/wasm/Cargo.toml` 取默认 features）。③ `t13-ulp-sensitivity-control` 的 C 面观测量旧注写 `origin.y`，代码读的是 `state().posY`。④ `t13-input-surface-probe` 头注称「覆盖三条既有输入路径」，代码实为**四条**（A/B/C/D）；且 A 面旧注写「dist 的 1-ULP 差异可见」，与同文件 D 段「A 面对 1 ULP 不敏感」**自相矛盾** —— 按代码改写为「A 面落点要过碰撞解算、分辨率被量化（量化尺度见 `apps/game/scripts/t13-ulp-sensitivity-control.mjs` 的 A-threshold 扫描）」，D 面实测为精确回显（`src/phys/teleport.rs` 的 `TeleportManager::from_json` 逐字段取 `origin`，`src/phys/mod.rs` 用 `dest.origin` 组事件，`take_event()` 直读）。⑤ 删除无法在仓内定位的措辞：`t3 实测 fast parser 1-ULP 案例`、`serde_json 文档点名`、外部归档文档引用（C 类）。**新增可核验锚点**：三处 API 由 `src/phys/mod.rs` 的 `#[wasm_bindgen] impl PhysWorld` 导出；`GRAVITY = 800.0` 落在 `src/phys/player.rs`。**新登记疑似缺陷（→ §7.3 #62）** | `apps/game/scripts/{t13-literal-sweep,t13-ulp-sensitivity-control,t13-input-surface-probe}.mjs` |
| 2026-09-22 | **WG5b：`apps/game/scripts/**` 11 件批由子代理产出、主控独立复验（WG5b 15/20）** | 11 件与行数（前 → 后）：`_dbg_keys` 18 → 50、`_dbg_floor` 29 → 56、`phys-p2-trace` 35 → 64、`wasm-hash-pin` 37 → 54、`phys-diag-flat` 37 → 58、`phys-gate-probe2` 39 → 70、`phys-p2-ground` 62 → 92、`phys-p2-regression` 75 → 91、`phys-teleport-gate` 136 → 146、`phys-surf-crouch-smoke` 157 → 175、`phys-smoke` 189 → 217。**主控四道门 + 语法门实跑**：`verify.ps1` **0 / 9**（tracked 9 件；`stripTrail` 全 d0，`phys-diag-flat` d2 / `phys-teleport-gate` d12 / `phys-smoke` d2 的 `strict` 差异经判据判为**行尾注释改写**；CRLF 全对、`bareLF=0`、无 BOM、尾空白 0/基线0、禁用词 0/基线0、`phys-p2-regression` 的 `禁用路径=0/基线1`——基线那 1 处正是被删的 `documents/archive/…` 引用）、`anchor-scan` 锚点 0 / 违规 0 / 裸行号 0、`template-proof` `files checked=9 template-literals=30 files-with-changes=0` + 2 件 `SKIP(no HEAD)` → `PROOF-OK`、`content-review` **57 条仓内路径 + 30 条「路径+符号」全命中**、`node --check` **11/11 exit 0**。**规模 A 18 / B 3 / C 6**。**主控开箱复核两条 A 类（不采信自述）**：① `apps/game/scripts/_dbg_floor.mjs` 从 `state_full_json` 读 `s.onGround` ⇒ 该列恒 `undefined`——实锤：`src/phys/mod.rs` 的 `state_full_json` 是 `serde_json::to_string(&self.extract_seed(..))`，种子结构体字段名是 snake_case（`src/phys/seed.rs` 的 `on_ground`），camelCase 的 `onGround` 只在 `state()` 出口（`src/phys/mod.rs` 的 `set_bool(&obj, "onGround", …)`）；② `phys-diag-flat.mjs` 旧注的「首步 Δv = −6.25、y≈99.95」与代码不符——`src/phys/player.rs` 的 `air_move` 前后各施加一次 `0.5 × gravity × dt`、`GRAVITY = 800.0`、`dt = 1/64` ⇒ 首步 Δv = **−12.5**、位移 = −(6.25)·(1/64) ≈ **−0.0977**（即 y ≈ 99.9023），与交付方实测一致。**两件 gitignored 文件的判据（本轮新事实 + 主控裁决）**：`_dbg_keys.mjs` / `_dbg_floor.mjs` 被 `apps/game/.gitignore` 的 `scripts/_*.mjs` 规则忽略、**不在 HEAD**，`verify.ps1` 必然报「参照集为空」⇒ 批次汇总恒为 `2 / 11`。**裁决**：这类文件**改用「编辑前快照 + 同款两遍算法」作为替代判据**（主控新建 `.tmp/tools/snapshot-diff.mjs` 并**自己复跑**：`_dbg_floor` `strict/stripTrail` 双 **d0（28/28）**；`_dbg_keys` 唯一差异是**首行 UTF-8 BOM**，剥离格式属性后双 **d0（18/18）** ⇒ 代码逐行相同）；此后 `verify.ps1` 的批次列表**不再包含**这类文件，规则已写入 `documents/norms/annotation-and-verification.md`（陷阱第 12 条）。**已披露的格式归一 3 处**（字节层，非代码）：`_dbg_keys.mjs` 去 BOM + 统一 CRLF（原 1 CRLF / 17 bareLF）、`_dbg_floor.mjs` 与 `phys-surf-crouch-smoke.mjs` 由纯 LF 转 CRLF。**新登记疑似缺陷 7 条（→ §7.3 #63）** | `apps/game/scripts/**`（11 件）、`.tmp/tools/snapshot-diff.mjs`、`documents/norms/annotation-and-verification.md` |
| 2026-09-22 | **WG6b：`apps/viewer/scripts/**` + `apps/viewer/test/**` 5 件完成并复验（WG6 收尾）** | 5 件与行数（前 → 后）：`apps/viewer/test/node-shims.d.ts` 9 → 10、`scripts/build-dist.mjs` 352 → 356、`scripts/dist-README.md` 86 → 94、`apps/viewer/test/replay-selftest.ts` 837 → 837、`apps/viewer/test/smoke-cdp.mjs` 875 → 880。**主控四道门 + 语法门实跑**：`verify.ps1` **0 / 4**（四件 `strict` 与 `stripTrail` **全 d0**——本批连行尾注释都未改；CRLF 全对、`bareLF=0`、无 BOM、尾空白 0/基线0、`build-dist.mjs` 的 `禁用词=2/基线2` 位于代码字符串内、`禁用路径=0/基线0`、块注释提前闭合 0）、`anchor-scan` 锚点 0 / 违规 0 / 裸行号 0、`template-proof` **80 个模板字符串逐字节相同** + 1 件 `SKIP(非 JS/TS)` → `PROOF-OK`、`content-review` **6 条仓内路径 + 1 条「路径+符号」全命中**、`node --check` 2/2 exit 0。**规模 A 15 / B 3 / C 11 / X 14**（X = 变更史/日期/不可定位实测数字）。**主控开箱复核两条 A 类**：① 旧注写「用法 `npm run test:smoke`」与「viewer dev 默认 8080」——实测 `apps/viewer/package.json` 只有 `local:smoke`（`node test/smoke-cdp.mjs`，无 `test:smoke`），`dev` 是 `python ../../src/serve.py 8100 .`（**8100**；8080 是 `apps/debug` 的端口，而脚本缺省 `SMOKE_URL` 正指向 8080）；② 旧注的 `bspYawToCsYaw(270−yaw)` 形式——实测该函数在 `src/ts-shared/phys/angles.ts`（`wrapDeg(bspYaw + 180)`），工作区无 `270−yaw` 形态。**`scripts/dist-README.md` 的判据裁决（本轮新增规则）**：该件是 `build-dist.mjs` 消费的 **prose 资产**（markdown 无注释语法），其说明文字正是重写对象，故 `verify.ps1` 的「代码同一性」判据**不适用**——实测计数 `strict:67/72/d19`、`stripTrail:67/72/d17`（d19/d17 即本次文本改写本身），而同一份输出的其余 10 项全过：`行=94 CRLF=93 bareLF=0 bareCr=0 BOM=False 尾空白=0/基线0 空行尾空格=0/基线0 禁用词=0/基线0 禁用路径=0/基线0 外部锚=0 块注释提前闭合=0`（`CRLF=93` 少于 `行=94` 是因该件与 HEAD 一样**末尾无换行**，交付方已核对 `git diff` 仅出现一次 `\ No newline at end of file` 且两侧共有）。**主控裁决：接受该件的文本重写**（还原为 HEAD 会把 single-only 标题、两条失效路径与事实错误一并带回），并把它写入规范篇陷阱第 13 条（prose 资产的判据集合）。**新登记疑似缺陷 5 条（→ §7.3 #64）**，其中 D1 是**跨 3 文件的链路级**：`test/maps/surf_null_4.replay` 已不在该路径（同名同尺寸文件在 `test/replay/`）。**未复跑 `npm run test:replay`**（交付方实跑 ok=93 / FAIL=0 / SKIP=1；主控未复跑的理由：`stripTrail` 四件全 d0 已证代码逐字未改，行为门在此为冗余） | `apps/viewer/scripts/{build-dist.mjs,dist-README.md}`、`apps/viewer/test/{smoke-cdp.mjs,replay-selftest.ts,node-shims.d.ts}`、`documents/norms/annotation-and-verification.md` |
| 2026-09-22 | **⚠ 范围覆盖盘点（新增工具 + 一处真实漏项，必须记）** | 新建 `.tmp/tools/coverage-scan.mjs`：取 `git diff --name-only HEAD`（已改）∪ `git ls-files --others --exclude-standard`（新增），与工作区内**实际存在**的范围文件（根 `src/`、`apps/{debug,game,viewer}/`，按扩展名排除 `.wasm/.bsp/.replay/.mtz/图片/.lock/.map`，按目录排除 `node_modules/target/pkg/dist/.tmp/fixtures`）相减 ⇒ **未改动 95 件**。**逐条分诊**：① **在跑** 7 件（game 6 + debug 1）；② **三方/产物类，明确排除**：`src/vendor/vmdl/**` 19 件（vendored crate）、`apps/game/temp/*.txt` 13 件（构建日志）、三个 `web/{app.js,worker.js}`（gitignore 产物）与 `web/coi-serviceworker.js`（第三方）、`src/phys/{LICENSE,NOTICE}`、三个 `package-lock.json`、三个 `package.json`（JSON 无注释可改）；③ **真实漏项（本盘点最重要的产出）**：**`src/lib.rs`**（phys crate 根，含 `//!`/`//` 注释与「提取自 game/」式沿革叙述，WG1 只覆盖了 `src/phys/**`，**从未列入任何工作组**）、`src/serve.py`、`src/scripts/{check-doc-drift,check-shared-sync,wasm-stale-check}.mjs` + `src/scripts/lib/{dist-pack,wasm-api-contract}.mjs`、`src/scripts/{cargo-env,ensure-node-deps,install-wasm-bindgen}.cmd`、三个工程的 `{build-dist,play,start-dev}.cmd` 共 9 件、三个 `web/index.html` 与 `web/styles.css` 共 6 件；④ **配置面待 owner 定**：7 个 `Cargo.toml`（含 `src/Cargo.toml` 里那条带 `t3` 标签的注释、三工程 `crates/wasm/Cargo.toml` 的模块结构头）、4 个 `.gitignore`、3 个 `tsconfig.json`——它们含注释但 §4 的 P0–P4 未列，且 §4 的「排除」清单也没排除它们（口径冲突，登记待决）。**处置**：③ 类**立即开工**——已派 1 个子代理做 `src/` 侧 8 件（`src/lib.rs`、`src/serve.py`、5 个 `.mjs`、1 个 `.cmd`），另 15 件（9 个工程 `.cmd` + 6 个 web 资产）由主控自做；④ 类登记为待 owner 裁决，不擅自动手。**工具另记一条能力**：该盘点可反复跑，用于 WG11 收尾前确认「范围内无未改动文件」 | `.tmp/tools/coverage-scan.mjs`、`AGENTS.md`（§7.3 新增 #65） |
| 2026-09-22 | **WG12 补漏（主控自做）：`apps/viewer/web/styles.css` 16 个注释块按代码重写 + 新增 CSS 结构证明工具** | 16 个注释块（原 19 行注释）逐条按当前 CSS 重写：头注原来的「S10：圆角/边框/悬停统一走变量」改为**可核验的令牌清单**（令牌定义在 `body` 而非 `:root`，含 `--sidebar-w: 348px` 与窄屏 `@media (max-width: 1000px)` 收到 `320px`）；`── HUD（左上，只读状态，不挡点击）` → `#hud` 三行 id 与 `pointer-events: none`、`:empty` 隐藏；`.map-reload` / `.track-row` / `.spawn-item` / `.fold` / `.tm-keys` / `.replay-meta` 五处**补上构造点**（实测：`apps/viewer/src/ui/mapinfo.ts` 造 `filebtn map-reload` 与 `spawn-item.*primary`、`apps/viewer/src/replay/trackpanel.ts` 造 `track-row`、`apps/viewer/src/core/dom.ts` 用 `details/summary` 造 `fold-*`、`apps/viewer/src/ui/replaymeta.ts` 造 `meta-name/mi/mk/mv/headline`、`apps/viewer/src/ui/telemetry.ts` 造 `tm-keys`）；遥测块删掉跨工程断言「game 同款」与不可定位的「用户定调」，改为按代码写「距底 24% / 左边界 `calc((100% − (--sidebar-w + 26px)) / 2)` 与 `#dock` 的右边界同参 / `.full` 时 `left: 50%`」。**判据（该件属规范篇陷阱第 13 条族）**：`verify.ps1` 结构计数全绿（`行=399 CRLF=399 bareLF=0 bareCr=0 BOM=False 尾空白/空行尾空格 0/基线0 禁用词 0/基线0 禁用路径 0/基线0 块注释提前闭合 0`），`strict/stripTrail` 的 `d3` 即被改写的注释行本身（CSS 注释对工具是代码 ⇒ 判据不适用）；`anchor-scan` 锚点 0 / 违规 0；**新建 `.tmp/tools/css-proof.mjs`** 做第二套证明——剥掉 `/* */` 后逐字符比对规则文本：`HEAD 17198 / CUR 17198 字符 逐字符相同`、注释块 `HEAD=16 CUR=16` ⇒ **CSS-CODE-IDENTICAL**（该证明强于按行比对，能覆盖跨行注释块）。**规范篇陷阱第 13 条已扩展**：把「注释语法不被 `verify.ps1` 识别」的资产族补全为 `.cmd` / `.html` / `.css`，并写清各自的专用证明工具（`cmd-proof` / `html-struct-proof` / `css-proof`） | `apps/viewer/web/styles.css`、`.tmp/tools/{css-proof,patch-viewer-styles}.mjs`、`documents/norms/annotation-and-verification.md` |
| 2026-09-22 | **WG12 补漏（主控自做）：`apps/game/web/styles.css` 18 个注释块按代码重写 + CSS 结构证明** | 该件 44 个注释块里**有 26 个是纯分节标签**（如 `/* 圆角三档 */`、`/* 控件行 */`、`/* 下拉框 */`、`/* 左侧模块导航 */`），逐条回代码核对**与实现一致**，按「已核对无误」保留；**另 18 个块改写**，其中：① 头注去掉跨工程的「风格语言迁移自 viewer（S10 体系）」与自述式「语义保留 / 完全不变」承诺，改为**令牌清单**（令牌在 `:root`，含 `--bg-app`、`--success/#9bd4a6`、`--warn/#ffd9a0`）+ **JS 接线四条**（`ledBreath`、`--ch-*` 由 `apps/game/src/panel/panel-controller.ts` 写入、末节可见性钩子、JS 生成类名清单）；② 语义色块把「同 viewer / viewer 无语义绿」改写为带路径的取值比对（`--danger` = `apps/viewer/web/styles.css` 的 `--danger`；`--warn` = 该件 `.note-warn` 文本色；`--success` 为本工程自有、只被 `#status.success` 使用）；③ **修正一处事实错误**：旧注写「隐藏文件控件的样式由 `#loadMapBtn` 承担」，而该 id 在本文件**没有任何规则**——实测外观由 `#panel .map-btn` 承担、点击由 `apps/game/src/app.ts` 里 `#loadMapBtn` 的监听器转发；④ 按键簇块去掉日期与「对照 viewer」的无路径写法，补上 `apps/game/src/app.ts` 的 `"—"` + `.off` 与面板侧 `.kempty「（已禁用）」`（`apps/game/src/panel/panel-controller.ts`）；⑤ `dist 版：无子元素的简单圆点` 改为按选择器写实的「`:empty` 退化形态（3×3 圆点）」；⑥ 六处「对照 viewer …」全部补成 `apps/viewer/web/styles.css` 的 `.tab.active` / `#guideBtn` / `.sec-title` / `.field-input` / `.btn.small` / `.note-warn` / `.note-info` / `.track-btn.danger`（逐条回该文件核对存在）；⑦ 可见性钩子块删掉变更史式「不再写 style.display」，改为「由 JS 切类名（进度值写 `--load-pct`）驱动显隐」。**判据（规范篇陷阱第 13 条族）**：`verify.ps1` 结构计数全绿（`行=615 CRLF=614 bareLF=0 bareCr=0 BOM=False 尾空白/空行尾空格 0/基线0 禁用词/禁用路径 0/基线0 块注释提前闭合 0`）、**代码行 `521/521` 相等**（差值仅来自注释行）、`strict/stripTrail` 的 `d8` 即被改注释行本身；`anchor-scan` 锚点 0 / 违规 0；**`css-proof.mjs` 第二套证明**：剥 `/* */` 后规则文本 `HEAD 21310 / CUR 21310 字符 逐字符相同`、注释块 `HEAD=44 CUR=44` ⇒ **CSS-CODE-IDENTICAL**。**末尾状态核对**：用 node 取 `git show` 与工作区**逐字节**比对（不用 `Out-String`，它会补换行）——两侧末行均为 `}` 且**无末尾换行**；`git show` 是 LF 归一化输出（索引 `i/lf`），工作区全 CRLF，两者不可直接比行尾。**旧措辞残留扫描**：`S10|同 viewer|对照 viewer|2026-0|dist 版|语义保留|不再写` 在两个 web CSS 中**全 0 命中** | `apps/game/web/styles.css`、`.tmp/tools/{css-proof,patch-game-styles}.mjs` |
| 2026-09-22 | **WG4b 收尾（子代理交付 + 主控独立复验）：`apps/debug/scripts/input-replay-verify.mjs`（1228 → 1256 行）⇒ **WG4b 18/18 完成**（`pages-index.html` + 17 个 `.mjs`）** | 该件自带静态服务与无头浏览器，注释面最宽（描述回放链路的 12 个阶段与 6 个判据）。**主控独立复验（不采信自述）**：`& .tmp/tools/verify.ps1 -Files apps/debug/scripts/input-replay-verify.mjs` ⇒ `strict:1061/1061/d2 [行尾注释改写2处=OK] stripTrail:1061/1061/d0 | 行=1256 CRLF=1256 bareLF=0 bareCr=0 BOM=False 尾空白=0/基线0 空行尾空格=0/基线0 禁用词=1/基线6 禁用路径=0/基线0 外部锚=0 块注释提前闭合=0`（代码行 1061 逐行同 HEAD，2 处差异是行尾注释改写；禁用词 1 属**基线内**——旧 HEAD 的注释里原有 6 处，本次改写降到 1）；`anchor-scan` 锚点共 0 / 违规 0；`template-proof` **126 个模板字符串逐字节相同** ⇒ `PROOF-OK`；`node --check` exit 0。**登记疑似缺陷 6 条（只记录不修，静态读码、未运行脚本；→ §7.3 #66）**，最重一条是**链路级**：全仓只有 `apps/debug/src/app.ts` 的 `replayCapture.record` 一处录制入口，而本脚本等待的是 `inputRecorder` 的录制产物 ⇒ 录制载荷恒为 0 帧，0/0b/0c/0d/A 各段必然走到失败分支，脚本最终只打印「回放未能开始」。**口径更正（本轮自查）**：#60① 的「扁平字段恒 undefined」**只属 `jump-apex-measure.mjs`**；本件读 `captureReplayState()` 的嵌套路径与 `getCurrentState()` 的扁平路径**都是对的**，此前把两件并成一条的写法已在 §7.3 #60① 更正 | `apps/debug/scripts/input-replay-verify.mjs` |
| 2026-09-22 | **静止期全量闸门（主控自跑，185 件）：代码面无失败，8 件资产类按专用判据通过** | 命令：`& .tmp/tools/gate-all.ps1 -Exclude <12 件在跑文件>`（脚本按 `git status --porcelain` 取全部已改/未跟踪且存在的文件，40 件一批依次调 `verify.ps1`）。**实测 5 个 chunk，只有 2 个 chunk 退出非 0（0..39 与 80..119）**，失败的 8 件**全部是已知资产/配置类**：`.gitignore`（配置面，3 行注释改动）、`AGENTS.md`（本规范篇，`strict:181/141/d294`、`禁用词=21/基线0` —— 21 处是 §1 禁令表**必须列举**被禁措辞本身，与规范篇 §1 同型）、`apps/debug/scripts/pages-index.html`（HTML 资产，按标签序列判）、`apps/game/web/{index.html,styles.css}`、`apps/viewer/web/{index.html,styles.css}`（HTML/CSS 资产，按 `html-struct-proof` / `css-proof` 判）、`apps/viewer/scripts/dist-README.md`（prose 资产，`strict:67/72/d19` 判据不适用）。**其余 177 件**：`strict`/`stripTrail` 全 `d0`，或差异经工具自判为**行尾注释改写**（`[行尾注释改写N处=OK]`）；全表 `bareLF=0`、`bareCr=0`、`BOM=False`、`尾空白/空行尾空格 0/基线0`、`禁用词 N ≤ 基线M`、`外部锚=0`（仅 `AGENTS.md` 记 1，属规范篇自引用旧文件名）。**结论：无一件因代码被改动、行尾、BOM、禁用词或禁用路径而失败**。**顺带记两个工具坑**：① `gate-all.ps1` 末尾的 `exit 1` 会**终止整个 pwsh 进程**，因此用 `*>` 写日志时最后一行汇总 `== chunks with failures ==` 会丢失（判读改为对日志逐行过滤 `^--- failing chunk` / `★代码部分不同`，不依赖汇总行）；② 本会话控制台把 UTF-8 中文读成 GBK（`Get-Content apps/debug/package.json` 显示为乱码），**判读中文内容必须用 read 工具**，不得据 `Get-Content` 的输出判定文件内容 | `.tmp/gate/gate-01.txt`、`.tmp/tools/gate-all.ps1` |
| 2026-09-22 | **WG5b-c（子代理交付 + 主控独立复验）：`apps/game/scripts/{phys-seed-smoke,build-dist,phys-rate-parity-v2,phys-dual-pipe}.mjs` ⇒ WG5b 20/20 完成** | 行数：`phys-seed-smoke` 361 → 377、`build-dist` 220 → 237、`phys-rate-parity-v2` 202 → 219、`phys-dual-pipe` 199 → 209。**主控复验（一次跑 4 件）**：`verify.ps1` ⇒ `== 校验失败文件数: 0 / 4 ==`，逐件 `strict` 差异均由工具自判为**行尾注释改写**（d26 / d20 / d48 / d44）且 `stripTrail` 全 `d0`（代码行 290 / 166 / 168 / 141 逐行同 HEAD），`行=377/237/219/209 CRLF 同数 bareLF=0 BOM=False`，`禁用词 0、1、0、0`（分别 ≤ 基线 0/1/1/0），`禁用路径 0`；`anchor-scan` 12 件合跑**锚点 0 / 违规 0**；`template-proof` **76 个模板字符串逐字节相同**（9 件合跑）⇒ `PROOF-OK`；`content-review` 33 路径 + 12 符号**全命中**（`bad=0`）；`node --check` 9/9 exit 0。**主控抽查两条数值断言**（不采信自述）：`phys-seed-smoke.mjs` 的掩码数组实测 **30 项**（`136-140` 行 10+9+11），前 24 项做 A 段推进、`masks.slice(24)` 的 6 项做 `set_state_ex` 对照 ⇒ 注释「24+6 tick、静态计数 30」成立；`build-dist.mjs` 的 `KEEP_SINGLE`（60 行）确无 `coi-serviceworker.js` 而 `KEEP_MULTI`（62-69 行）有 ⇒ 「single 产物引用该文件必 404」成立。**旧注错误 A 15 / B 5 / C 4 组**（`40 tick` 实为 30、「恰晚 1 tick」实为「更晚即可」、`walk_move 消费 ground_normal` 而该字段在模拟内无读取点、`{pos,vel,accel,timeMs}` 实为 `{pos,velY,accelY,timeMs}`、`accelY` 单位 u/s² 实为 HU/s 每毫秒、multi 清单漏 `coi-serviceworker.js`、COI 注释挂错行、`v2` 的「≤2u 吸附 / 实测 0.0313 / 嫌疑 a·b」等不可定位内容已清）。**登记疑似缺陷 15 条（只记录不修；→ §7.3 #67）** | `apps/game/scripts/{phys-seed-smoke,build-dist,phys-rate-parity-v2,phys-dual-pipe}.mjs`、`.tmp/wg5b-c/**` |
| 2026-09-22 | **WG12 补漏（子代理交付 + 主控独立复验）：`src/` 根与工具层 8 件，并新增 `.py` / `.cmd` 资产证明族** | 件与行数：`src/lib.rs` 12 → 19、`src/serve.py` 64 → 73、`src/scripts/check-doc-drift.mjs` 117 → 127、`src/scripts/check-shared-sync.mjs` 229 → 238、`src/scripts/wasm-stale-check.mjs` 69 → 77、`src/scripts/lib/dist-pack.mjs` 194 → 210、`src/scripts/lib/wasm-api-contract.mjs` 203 → 213、`src/scripts/install-wasm-bindgen.cmd` 105 → 114。**主控复验（一次跑 8 件）**：`verify.ps1` ⇒ 10/12 通过；**唯一 2 件失败（`src/serve.py`、`src/scripts/install-wasm-bindgen.cmd`）纯属工具盲区**——`verify.ps1` 只把 `//` 当注释，于是 Python 的 `#`/`"""docstring"""` 与 cmd 的 `REM` 都被计入「代码行」（与 `cargo-env.cmd` 同型，属规范篇陷阱第 13 条族）。**主控自建替代判据**（不采信子代理的同类工具）：`.tmp/tools/py-proof.py` 三重断言 —— Python `ast` 抹 docstring 后 `ast.dump` **相同**、`tokenize` 码流（剔 COMMENT/NL/INDENT/docstring STRING）**相同**、剥注释与 docstring 后的真实代码行 **41/41 相同** ⇒ `PY-CODE-IDENTICAL`；cmd 用既有 `.tmp/tools/cmd-proof.mjs` ⇒ `命令行 HEAD=78 CUR=78 diff=0 nonASCII=0`。其余门：`anchor-scan` 12 件合跑**锚点 0 / 违规 0**；`template-proof` **76 模板逐字节相同** `PROOF-OK`；`content-review` 33 路径 + 12 符号全命中；`node --check` 9/9。**主控抽查三条断言**：`src/serve.py` 头注「三个 start-dev.cmd 与 debug/game 的 play.cmd 都传 `"%~dp0."`」——全仓 `grep serve\.py` 实测**恰好 5 处**（3 个 `start-dev.cmd` + `apps/{debug,game}/play.cmd`，`apps/viewer/play.cmd` 不直接调用）**成立**；`dist-pack.mjs` 注释「`bytes` 取 `text.length`（UTF-16 码元数）」与 `127` 行 `return { path: target, bytes: text.length, preamble }` **一致**（该口径差已登记为缺陷）；`src/serve.py` 头注引用的 `createMainSharedState` 与 `crossOriginIsolated` 分派在 `src/ts-shared/auth/shared-state.ts` **实存**（`1020-1030` 行）。**旧注错误 A 44 / B 3 / C 8 / X 22**，其中最重要的一条是子代理自查改正的**假断言**：`dist-pack.mjs` 旧注称自己是「唯一允许拼装 `__VBSP_*` 的地方」，实测 `apps/viewer/scripts/build-dist.mjs` 亦写 `__VBSP_WASM_B64__`（主控已复核该写入点存在）。**唯一格式改动（已披露）**：`wasm-stale-check.mjs` 工作区原为纯 LF（`bareLF=69`），按仓库约定转为 CRLF；未清理任何既有行尾空白。**登记疑似缺陷 10 条（只记录不修；→ §7.3 #68）** | `src/lib.rs`、`src/serve.py`、`src/scripts/{check-doc-drift,check-shared-sync,wasm-stale-check}.mjs`、`src/scripts/lib/{dist-pack,wasm-api-contract}.mjs`、`src/scripts/install-wasm-bindgen.cmd`、`.tmp/tools/py-proof.py`、`.tmp/t8/**` |
| 2026-09-22 | **WG12 补漏（主控自做）：`apps/debug/web/index.html` 注释重写（689 → 699 行）＋ 修掉 HTML 结构证明工具的行尾陷阱** | 该件 35 个注释块（21 个 HTML + 14 个 CSS）逐条按代码复核：**纯分节标签**（如「顶栏」「主容器」「侧边栏」「控件网格」「元数据 KV」「调试线框」）逐条回代码核对一致后保留；**13 处改写**，其中最重的三处：① **删掉变更史式 PVS 说明与不可定位实测数字**（旧注「2026-09-11 照搬 game 的剔除实现…实测 surf_666 的 PVS 可见集仅 153/8269 cluster、会隐藏 1968/2221 块」），按当前代码改写为「本区不提供 PVS 开关：debug 侧剔除判据只有距离（`apps/debug/src/renderer/lod-manager.ts` 的 `update`），`LodStats.pvsHidden` 每轮刷新写 0」；② **路径记录块**（原「…跳变 >100 HU 处自动断开…tick 段按折角着色：绿 ≤5° / 黄 ≤20° / 红 >20°…64 档 ≈ 67 节点/秒」）整段按 `apps/debug/src/renderer/path-recorder.ts` 重写：两条线各记脚底中心点（`camY = posY + eyeHeight` 才是眼睛）、按各自计算节点采样（渲染线 = 主线程 predPhys 每 rAF 一步；tick 线 = Worker 权威帧，节点率 = 权威固定步长的倒数，步长经 `AuthLoop.setFixedDt`）、`JUMP_BREAK = 100` 处断开、**折角着色只有两档**（≤20° 保持琥珀 / >20° 染红，见 `turnColor`）——旧注的「绿 ≤5°」第三档**在实现里不存在**；③ 参数数值框注释按 `bindSlider` 写明双向同步（滑块 input 写回框、框 change 写回滑块、NaN 直接忽略）。**另新增 2 条注释**（HTML 注释 21 → 23）：在 `#pathBuildTag` 上方写明「静态文本、无代码读写、本页不校验构建版本」，在 `#lightingModeHint` 上方写明「说明段为静态文本（id 无读取点），切换由单选落到 `setLightingMode`」——两条都是本轮实测出的**死 id**。**判据（规范篇陷阱第 13 条族）**：`verify.ps1` 结构计数全绿（`行=698 CRLF=698 bareLF=0 bareCr=0 BOM=False 尾空白/空行尾空格 0/基线0 禁用词 0/基线0 禁用路径 0/基线0 外部锚 0`），`strict/stripTrail` 的 `d21` 即被改写的注释行本身（HTML 注释对工具是代码 ⇒ 判据不适用）；`anchor-scan` **锚点 0 / 违规 0**；`html-struct-proof` **标签序列 518 = 518 逐项相同**；`content-review` **9 条仓内路径 + 8 条「路径 的 符号」断言全命中**（为此把注释里的路径/符号引号统一成反引号，使断言可被机器核验）；**主控自建第二套证明** `.tmp/tools/html-markup-proof.mjs`：剥掉 HTML/CSS 注释后**标记与文本逐字符相同**（24485 = 24485）、标签序列相同（294 = 294）、**属性值多重集相同**（580 = 580）⇒ `HTML-MARKUP-IDENTICAL`（该证明比结构门更强：能同时证明 `title=`/`placeholder=` 等属性文本零改动）。**工具修复（本轮实测的必要修复）**：`html-struct-proof.mjs` 原先直接拿 `git show`（LF）与工作区（CRLF）比对，而属性值里含 `>` 的标签会被正则切成**跨行记号** ⇒ 同一标签因行尾 CR 被判成不同（该件曾误报 `HTML-STRUCT-DIFF 4`）；已加入行尾归一，复跑三个 HTML 资产全部 exit 0。**登记待裁决 1 条**：同件 `title` 属性（**代码字符串，未改**）仍写「tick 段按折角着色：绿 ≤5° / 黄 ≤20° / 红 >20°」，与 `turnColor` 的两档实现不符 ⇒ 属代码改动 | `apps/debug/web/index.html`、`.tmp/tools/{html-struct-proof,html-markup-proof}.mjs`、`.tmp/cap/{patch-debug-index,patch-debug-index-backticks}.mjs` |
| 2026-09-22 | **主控复核并收口三批（WG4b 末件 / WG5b 末批 / WG12 `src/` 侧 8 件）＋ 一处口径更正 ＋ AGENTS.md 台账更新** | **复验方式**：三批共 13 件由主控在静止期逐件跑门（不采信子代理自述），结论与交付一致：WG4b 末件 `apps/debug/scripts/input-replay-verify.mjs`（`stripTrail 1061/1061 d0`、126 模板逐字节相同）、WG5b 末批 4 件（`verify.ps1 0/4`、`stripTrail` 全 d0、76 模板逐字节相同）、WG12 `src/` 侧 8 件（`verify.ps1 10/12`，唯二两件是工具盲区）。**主控自建的两套替代判据**：`.tmp/tools/py-proof.py`（Python：`ast` 抹 docstring 后 `ast.dump` 相同 + `tokenize` 码流相同 + 真实代码行 41/41 相同 ⇒ `PY-CODE-IDENTICAL`，用于 `src/serve.py`）与 `.tmp/tools/cmd-proof.mjs`（cmd：命令行多重集 + `nonASCII` 计数 ⇒ `CMD-CODE-IDENTICAL`，用于 `install-wasm-bindgen.cmd`）。**抽查（不采信自述）**：`phys-seed-smoke.mjs` 掩码数组实测 **30 项**（10+9+11）而前 24 项推进、`masks.slice(24)` 6 项做对照 ⇒ 注释「24+6」成立；`apps/game/scripts/build-dist.mjs` 的 `KEEP_SINGLE` 确无 `coi-serviceworker.js` 而 `KEEP_MULTI` 有 ⇒「single 产物必 404」成立；`dist-pack.mjs` 的 `bytes = text.length` 与注释一致；`src/serve.py` 头注「三个 start-dev.cmd 与 debug/game 的 play.cmd 都传 `"%~dp0."`」经全仓 `serve\.py` 检索**恰好 5 处**（`apps/viewer/play.cmd` 不直接调用）成立；`createMainSharedState`/`crossOriginIsolated` 分派在 `src/ts-shared/auth/shared-state.ts` 实存。**口径更正（重要）**：`input-replay-verify.mjs` 的「录制载荷恒空」不是「`inputRecorder` 缺失」而是**实测确认的事实**——`InputRecorder.record()` 在全部 `apps/debug/src` 内只有**一个**调用点（`apps/debug/src/app.ts` 的回放分支 `replayCapture`），故用户录制器 `inputRecorder` 永不落样本、`exportJson()` 的 frames 恒空；另实测脚本按 `f.dt` 取步长而 `InputFrame` 的字段只有 `t/dx/dy/keys`（**无 `dt`**）⇒ dt 判据恒假；页面还缺 `inputRecStatus` 等 **7 个**被 `document.getElementById` 查询的 id。三条均已写入 §7.3 #66。**AGENTS.md 同步更新**：§7.2 的 WG4/WG4b/WG5b/WG12 四行改为实测状态、§7.3 新增 **#65–#68**（范围盘点与配置面口径、input-replay 5 条、WG5b 末批 15 条、WG12 `src/` 侧 10 条）、§7.4 重写为「四组收尾 → WG12 收尾 → WG9/WG10/WG11」；文件 50294 → 57606 字节（仍在 65536 截断线内，**后续增量须继续走台账**）。**另更正一处旧记录**：`AGENTS.md` 原记「9 个工程 `.cmd` 实测 0 条注释」**有误**——实测 7 件共 48 条 `REM`/`::` 注释（全部 ASCII，`nonASCII` 字节 0），只有 `apps/{game,viewer}/build-dist.cmd` 确为 0 条；7 件已派 1 个子代理按「纯 ASCII + 英文注释 + `cmd-proof` 零改动」改写。**顺带记两条工具/环境坑**：① `gate-all.ps1` 末尾 `exit` 会终止整个 pwsh 进程，`*>` 重定向时末行汇总会丢失（判读改为过滤 `--- failing chunk`）；② **不要用 PowerShell 的 `Get-Content`/`Set-Content` 改 UTF-8 文件**——本会话控制台把 UTF-8 读成 GBK，且 `Set-Content -Encoding utf8` 会加 BOM 并吃掉换行（`html-struct-proof.mjs` 因此被写坏一次，已用 write 工具重写并复查首字节 35,33,47）；判读中文内容一律用 read 工具 | `AGENTS.md`、`documents/plan/progress-log.md`、`.tmp/tools/{py-proof,html-markup-proof,html-struct-proof}.mjs` |


## §7.3 待裁决项（逐行原样移出，2026-09-22 三次瘦身）

> 出处：根 `AGENTS.md` §7.3 的累积待裁决行（#26–#69，共 28 条）。**逐行原样移出，内容未改**；AGENTS.md 侧只保留 #1–#11 的短规则与一行一条的索引。
| 26 | **疑似代码缺陷（子代理上报，静态读码，未运行验证；②类只记录不修）**：`bsp_to_gltf_core/gltf_builder.rs` 的 `push_material` 在 `material.transform` 存在时往 material 写 `extensions.KHR_texture_transform`，但 **BSP 导出路径从不把该扩展名加进 `Root::extensions_used`**（全仓只有 `lightmap.rs` 的 `inject_lightmap_json` 与 `model_integrator` 的 `process_lights` 动过该字段）⇒ 按 `extensionsUsed` 决定是否启用该扩展的消费端不会生效。**未跑 wasm/浏览器验证** | 已知；只记录不修 |
| 27 | **疑似代码缺陷 4 条（子代理上报，静态读码，未运行验证；②类只记录不修）**：均在 `src/wasm-core/texture_utils/vtf.rs` ① `VTFHeader::write` 在 `version[0] < 7` 分支声明头长 64 但**实写 63**；而 `VTF::create` 写死 `[7, 1]` 且不写像素 ⇒ 产出回灌 `VTF::read` 后偏移越界；② `ResourceList::read` 先吞一个 u64 占位，`ResourceList::write` 不写它（写侧由 `VTFHeader::write` 补），单独调用 `write` 写不出可读区段；③ `get_offset` 把 `frame` 与 `face` 加成同一线性项，而头里没有面数字段 ⇒ 两者同时 > 1 时索引撞同一偏移；④ `get_mip_size` 恒用头里的高清图幅，`VTFImage::get_frame` 却按视图自身宽高切片。**另记死代码**（已在注释标注）：`ResourceType::has_resource_type`、`HAS_NO_DATA_CHUNK`、`VTF::save_as_png`、`texture_utils::create`、`VTF::lowres_image` 在本仓**零调用点** | 已知；只记录不修 |
| 28 | **错误消息里仍有 1 处外部引用（属代码，未擅改）**：`src/wasm-core/bsp_to_gltf_core/lightmap.rs` 的 `check_face_luxel_size` 错误文本含「外部参照实现的 `Lightmap.cs:64` 仅 65536 项」。它是**面向用户的字符串字面量**而非注释，按「代码零改动」未改；复验工具把这类 `*.cs:NN` 计为信息项「外部锚」（该文件实测 **1**，其余已重编文件均为 0）。是否改写文案由 owner 决定 | 已知；待 owner 裁决 |
| 30 | **疑似代码缺陷 8 条（`mosaic/mtz.rs`，子代理上报，静态读码、未运行验证；②类只记录不修）**：① `pack_regions` 用 `as u8` 写 meta 的 `w`/`h`/色数/`sig` 长度，而 `parse_bytecode` 允许 `w*h ≤ 100_000` ⇒ `w`/`h` > 255 的文本压缩→解压后**尺寸变值**；② `opacity` 只在 `< 250` 时写出，故 `T[250..=254]` 经 `parse_json`→`render_json` **丢失**（回落 255）；③ `pack_regions` 不校验 `sig` 长度而 `unpack_regions` 只收 0/2/4 ⇒ `G[1]` 能压不能解（**读写接受集不一致**）；④ `parse_bytecode` 的 `w * h > 100_000` 在 `u32` 上相乘且无单值上界（与 §7.3 第 20 条 decode.rs 同型）；⑤ `unpack_regions` 不校验 `w`/`h ≥ 1`（只有 `parse_bytecode` 校验）⇒ 可产出 `B[key:0x0]` 而该文本再入 `parse_bytecode` 必被拒；⑥ `FLAG_NAMES_LZ`..`FLAG_INDICES_LZ` 五个常量**零引用**（带 `#[allow(dead_code)]`）；⑦ `emit_match` 的扩展长度字节 `(len - 3 - MAX_LEN3) as u8` 在 `len > 265` 时会静默截断（当前唯一调用点用 `min(n-i, 265)` 兜住）；⑧ `unb64` 丢弃末尾不足 8 bit 的余位且不校验组数 ⇒ `A[x]` 解出空 `Vec` 而不报错 | 已知；只记录不修 |
| 31 | **疑似代码缺陷 6 条（`vbsp/data/entity.rs`，子代理上报，静态读码、未运行验证；②类只记录不修）**，其中**第 ① 条是跨工程实锤级**：① `apps/debug` 与 `apps/game` 的 `crates/wasm/src/lib.rs` 各有一处 `.prop("StartDisabled")`（大写 S/D），而 `src/wasm-core/vbsp/reader.rs` 的 `read_entities` 对整段实体文本已 `to_ascii_lowercase()`、`RawEntity::prop` 是**逐字节**比较 ⇒ 该次取值**必然** `Err(NoSuchProperty)`，被 `.unwrap_or(false)` 吞掉 ⇒ **`start_disabled` 恒为 false**（同处 `prop("spawnflags")` 是小写、正常）；② 两个变体名与 classname **交叉**（`HealthPackFull` ↔ `item_ammopack_full`、`AmmoPackFull` ↔ `item_healthkit_full`；两者字段集相同故无行为差异）；③ 5 个变体缺 `#[serde(borrow)]`（`LogicRelay`/`LogicAuto`/`DustMotes`/`PathTrack`/`RopeMove`，其余 23 处都有）；④ `LightGlow::horizontal_size` 的键字面量是 `horizontalhlowsize`（多一个 `h`）；⑤ 3 处 `rename_all = "lowercase"` 在其未显式 `rename` 的字段上是冗余属性；⑥ 零调用点：`RawEntity::as_str`、`RawEntity::prop_parse`、`vbsp/data/mod.rs` 的 `as_prop_placement` | 已知；只记录不修（第 ① 条是否修由 owner 裁决） |
| 36 | **`MODE_HANDOVER_MATRIX` 的 `summary` 是代码字面量，注释重编动不了它（⑤类：需改代码才能让文档成立）**：`src/ts-shared/auth/compute-mode.ts` 的六个 `summary` 字符串里含**已删文档的分节引用**（`§3.4.C 步骤 a-f`、`§3.4.D`、`§3.2`）与"修复前/原实现"式措辞，但它们是**代码**（对象字面量的属性值），按「代码零改动」本次**逐字保留**，只在其上方注释里注明「`summary` 是数据、不是注释」。是否改写这些字面量由 owner 裁决 | **待 owner 裁决** |
| 38 | **`apps/debug/scripts/jump-apex-verify.mjs` 内嵌"修复前行为"复刻（脚本资产，属 WG4 范围）**：第 213 行注释写「修复前行为内联复刻（`authority-calibrator.ts` 旧代码：无条件 `onGround=true`）」，并以此为对照面跑差分。它依赖的"旧行为"已不在源码中，脚本自带一份副本。另 `jump-apex-serve.mjs` 用**源码文本切片**生成回退版（切片锚点 = `if (sm.teleport === false) {` 与 `      if ((env.getComputeMode`），**故这两处代码行文本形态被脚本依赖**——本次注释重编已确认两个锚点在 `worker-dispatch.ts` 中仍各可命中（主控实测 `i0 < i1` 成立、切片块完整） | 已知；脚本未改，切片锚点已实测未破坏 |
| 40 | **两处历史编号只存在于断言标签字符串里（属代码，未改）**：`src/ts-shared/auth/tick-authority.test.ts` 的 `assert.equal(ctx.c.stats.floorSkips, 1, '迟到地板计入 floorSkips（门上游，Q1 口径）')` 与 `assert.equal(ctx.c.gate.stats.leadMiss, 1, 'leadMiss 计入（§8.5 停顿语义）')`——`Q1` 与 `§8.5` 指向**已删文档**。子代理第一稿误改这两条字符串，`verify.ps1` 立刻判「★代码部分不同 4 行」，已**逐字还原**（新稿 `stripTrail` 回到 d0）。是否清理须作为**代码改动**由 owner 裁决（同 §7.3 #36 口径） | **待 owner 裁决** |
| 41 | **owner 指令：子代理并发上限（2026-09-22）** —— 并发过多会触发 API 计费加额。**处置**：此后**同时最多 3 个子代理**；主控自己承担更多改注释工作。**背景事实（如实登记）**：本轮曾一次铺开 **19 个子代理**（WG4 12 + WG5 2 + WG6 4 + WG1 件），随后**全部被掐断**（`failed before it finished`），其中 3 个批次**零落盘**（debug 15 件小文件批、viewer 4 批）、若干件**半成品**（如 `lightmap-shader.ts` 只改了 120/75 行且留下 5 处「此前」变更史 + 1 处外部行号引用）。**复验纪律**：子代理批量失败后必须先做「静止期全量盘点」（此刻无写入者）再判定，逐件跑 13 项而非采信其自述 | **已生效**（并发 ≤3） |
| 43 | **口径对撞（B 类，待终审裁决）**：`documents/plan/project-survey.md` §9 记「旧文档树（全仓 `.md`）原 **68 篇**」，而 `git status` 实测删除面为 `documents/` **47 篇** `.md` + 根级 **4 篇** `.md` + 退役 harness **5 篇** `.md` = **56 篇**（外加 27 个非 `.md` 路径）。本文件 §2 原先写的「45 篇」也是错的，已按实测改为 47。**处置**：不擅改控制文件，登记待终审；WG10 重建 index 时以**实际存在的文件**为准，不以任一计数为准 | **待 owner / 终审裁决** |
| 50 | **WG5 面板批复验中登记的疑似缺陷（只记录不修；静态读码，未运行浏览器验证）**：① **γ 滑块量程与接受窗口不一致**——滑块量程 `0.5..6`，而 `apps/game/src/renderer/lightmap-shader.ts` 的 `setLightGamma` 只在 `value <= 0 || value > 1` 之外才写入共享 uniform ⇒ 拖到 >1 时 `config` 变了、画面不变（注释已如实写明该接受窗口）；② `panel-controller.ts` 的 `bindSlider` 数值框路径把钳制结果只写回滑块与 `config`，**不回写数值框文本** ⇒ 数值框可持续显示越界值；③ `applyCrosshair` 内 `const dot = el.querySelector('.ch-dot')` 声明后未被使用（显隐由 `no-dot` 类控制）⇒ 死变量；④ **同链路延伸（本轮 WG5 复验登记）**：`apps/game/src/config.ts` 的默认 `lighting.lightGamma = 2.2` 同样落在 `(0, 1]` 之外 ⇒ `apps/game/src/renderer/renderer-main.ts` 的 `setLightGamma(config.lighting?.lightGamma ?? 0.5)` 初始化写入被忽略，共享 uniform 保持自身初值 | **待 owner 裁决** |
| 51 | **规范篇里的禁用词是「引用对象」**：`documents/norms/annotation-and-verification.md` §1 必须**列出**被禁止的措辞才能定义「什么算推测」，因此它是全仓**唯一**允许出现这些字样的新稿位置（doc 内已写明这一点）。其余新稿一律 0 命中；`check-doc-drift.mjs` 对这些字样不判失败（它只管锚点与路径） | 已说明；非缺陷 |
| 52 | **已删文档的编号只残留在代码字面量里（属代码，未擅改）**：`apps/debug/scripts/check-wasm-api.mjs` 的 `console.log('=== F4: WASM API 契约检查 ===')` 里 `F4` 是既有输出标签，注释已不再解释其出处（不可从当前工作区定位其定义）。同类另见 #36（`MODE_HANDOVER_MATRIX` 的 `summary`）与 #40（`tick-authority.test.ts` 的断言标签） | **待 owner 裁决**是否清理 |
| 53 | **`apps/game` 类型面/配置面疑似缺陷（本轮 WG5 复验登记；只记录不修，静态读码、未运行验证）**：① `worker/worker-types.ts` 的类型面落后于实际载荷——`WasmInitMessage` 未声明 `wasmB64`/`mtzB64`、`InitMessage` 的 `width`/`height`/`dpr` 既无发送方也无读取点、`WorkerMessage`/`MainMessage` 两个联合的成员集与实际收发不符；② `input/input-bridge.ts` 的 `sendConfig` 对 `hud` 段下发的是全量**物理参数**载荷且 `section` 名原样保留 ⇒ worker 侧 `applyConfigPatch('hud', …)` 会把 `gravity` 等键并入 `config.hud`（worker 不读 `hud` 段，静态看无行为影响）；③ `savepoint.ts` 的 `SavePoint.t` 只写不读（列表按插入顺序渲染）、`SavePointStore.getMap()` 与 `clear()` 零调用点 | **待 owner 裁决** |
| 54 | **代码字面量级矛盾（⑤类：改注释解决不了，需改代码；独立审查员发现，只记录不修）**：`apps/viewer/src/replay/timeline.ts` 的两条 `title` 文案写「0 = 起跑帧，prerun 不在播放区间」，而默认播放窗口**包含** prerun 负段——`apps/viewer/src/replay/player.ts` 的 `applyFullRange` 取 `rangeStart = Math.min(0, t0)`、`resetRange` 把 `time` 置为 `rangeStart`（可为负），`apps/viewer/src/replay/tracks.ts` 的 `localTime` 只在早于片头时返回 null ⇒ prerun 帧会被采样播放；夹具侧 `apps/viewer/test/replay-selftest.ts` 亦断言 `t[0] = −113/tickrate`。同文件头注（本轮新写）所述与此相反 | **待 owner 裁决**（属代码改动） |
| 55 | **审查登记的零调用点 / 死支路（只记录不修；均已按代码如实写入注释，未改代码）**：① `apps/viewer/src/replay/timeline.ts` 的 **A-B 区间带恒不显示**——该分支下分子 `min(rangeStop, dur) − rangeStart` 恒等于分母 `winLen` ⇒ `width ≡ 100`，落不进 `width < 99.95` 的绘制条件（注释已写明该推导）；② `ReplayPlayer.sampleClipAt` 与 `ReplayPlayer.horizontalSpeed` 全仓零调用点（遥测 HUD 自算 `Math.hypot(vel[0], vel[2])`）；③ `apps/viewer/src/replay/sampling.ts` 的 `lerpAngle` / `lerp` 仅本文件内使用（导出但无外部消费者）；④ 跑段高亮宽度在 `track.offset ≠ 0`（`apps/viewer/src/replay/trackpanel.ts` 的偏移输入框可写）时两项混基，位置/宽度会偏 | **待 owner 裁决** |
| 58 | **`apps/viewer/src/core` + `ui` 批复验中登记的疑似缺陷（只记录不修，静态读码、未跑浏览器；10 条）**：① `core/scene.ts` 构造期 `setLightGamma(2.2)` 落在发光着色器接受窗口 `(0, 1]` 之外 ⇒ γ 写入被忽略、uniform 保持初值 1（与 #50④ 同源，viewer 侧同病）；② `core/fly.ts` 死字段：`allowPointerLock` 无写无读、`onLockChange` 无赋值点（回调永不触发）；③ 零调用点：`core/pose.ts` 的 `pitchClampedRad`/`eyeHeight`、`core/constants.ts` 的 `RAD2DEG`（`apps/viewer/src` 内）、`ui/mapinfo.ts` 的 `MapPanel.spawnPoints` getter 与 `setMap(null)` 清空分支；④ `core/bsp.ts` 的 `ensureWasm` 把**首次失败永久缓存**（rejected 后从不重置，且无 catch ⇒ 未处理拒绝），一次瞬时 fetch 失败后本页无法自愈；⑤ `ui/telemetry.ts` 自算 `Math.hypot(vel[0], vel[2])` 与 `replay/sampling.ts` 的 `horizontalSpeed` 重复；⑥ 同件 `setTracks` 对 `this.horizEl.parentElement as HTMLElement` 直接调 `classList`（容器脱离文档时为 null 会抛 TypeError，当前路径恒在文档中）；⑦ `ui/mapinfo.ts` 的光照 `<select>` 只写不回填（外部调 `setLightingMode` 后下拉与实况脱节）；⑧ `core/dom.ts` 的 `numField` 把空串按 `Number('')===0` 判合法，两个消费点又忽略 `valid` ⇒ 空串会当 0 写入变换；⑨ `core/scene.ts` 的 `optimizeScene` 选块边长用的 worldBox 只累计「单材质且有 position」的 Mesh，进 keptMeshes 的数组/缺失材质 Mesh 不参与统计 | **待 owner 裁决** |
| 59 | **viewer `replay/` 5 件批复验中登记的疑似缺陷（只记录不修，静态读码、未跑浏览器；10 条）**：① `visuals.ts` 的 `disposeTree` 只处理 `isMesh`——轨迹线（`THREE.Line`）与 tick 点（`THREE.Points`）的几何体/材质在 `clear()` → `setTracks()` 重建时从不释放（GPU 资源累积）；② `importer.ts` 的 `createObjectURL` 未配对 `revokeObjectURL`（每次重起 Worker 泄漏一个 blob URL）；③ 同件 `workerBroken` 之后的 promise 不被 reject，调用方的 `.catch` 永不触发（当前调用点用 `await` 包装，故未产生未处理拒绝）；④ `import()` 无超时/取消：`postMessage` 成功而 Worker 不回消息时永久挂起且面板 `busy` 卡死；⑤ 零调用点：`ReplayImporter.dispose()`、`TrackPanelOptions.onPresence`（`panel.ts` 构造 `TrackPanel` 时未传 ⇒ 该面板永不发轨道数通知）、导出类型 `ImportPhase`/`ProgressFn`、`ImportResult.resolvedPath`、`tracks.ts` 的 `TRACK_PALETTE`；⑥ `Track.offset` 无上界钳制（偏移输入框只挡 NaN）；⑦ `visuals.ts` 对 `Clip.count = 0` 的轨道返回 null，而 `trackpanel.ts` 仍为该轨道渲染一行控件（两处口径不一致）；⑧ 代码字面量级（不可从工作区定位）：`visuals.ts` 的 `'与 debug 权威帧方点同款观感'`、`panel.ts` 的 `cos=0.9992` 属**代码字符串**，未改；⑨ `panel.ts` 平移输入的 `hint` 写「默认 0」而 `step: 10`；⑩ 同件 `title` 文案「`.replay` = Shavit 原生录像，零配置直入」与 `accept = '.replay'` 的口径（`app.ts` 另支持拖拽与 URL 深链）不一致 | **待 owner 裁决**（⑧ 属代码改动） |
| 60 | **debug 脚本批复验登记的疑似缺陷 + 一处链路级断裂（只记录不修；owner 裁决待定）**：① **⚠ 链路级**：`apps/debug/scripts/jump-apex-measure.mjs` 的就绪判据取 `window.__jumpProbe.state()` 的 `diag.posY`，而该方法来自 `apps/debug/src/renderer/renderer-main.ts` 的 `RendererMain.getCurrentState()`（返回**嵌套** `{pos, yaw, pitch, vel, onGround}`）⇒ 扁平字段恒 `undefined`；同一脚本的 3s 采样表达式也读 `s.posY`/`s.velY`（`undefined.toFixed` 抛错）⇒ **整条 jump-apex 采样链（measure → auth-diag / report / window / trace）当前跑不通**（180s 超时后 `finish(1)`）；`jump-apex-smoke.mjs` 同源。修法二选一（改 probe 或改脚本读数）属**代码改动**，未擅改。② `frame-bench.mjs` 的缺省地图路径 `join(repoRoot, 'maps', 'surf_666.bsp')` 不存在（实测 `Test-Path maps` = False）⇒ 不带参数必以 2 退出；`glb-mesh-count.mjs` 用的是存在的 `test/maps/…` ⇒ 两件旧注同写「默认 maps/surf_666.bsp」而代码不同（B 类）。③ `glb-mesh-count.mjs` 头注实测规模已被产物推翻：旧注 117 mesh / 34409 primitive / 377385 顶点 / 319 材质 / GLB 136.0 MB，本次实跑 **483 / 35202 / 489436 / 119 / 515 / 64.6 MB**。④ `optimize-scene-verify.mjs` 的基线常量同为旧值且断言只与自身常量自洽；其 `WORLD = 16320` 的原值依据**无法从工作区定位**（实跑 GLB 世界包围盒 32152×32592×32624）。⑤ `frame-bench.mjs` 头注「PVS 隐藏数」口径错位：`updateCullStatsUI` 的「隐藏 N」读 `pvs.pvsHidden`，而 `apps/debug/src/renderer/lod-manager.ts` 的 `update` 每轮把它写 0（真隐藏数是 `far`）。⑥ `build-dist.mjs` 头注「含缺失纹理回退」在本脚本不可定位。⑦ `jump-apex-serve.mjs` 的 `X-Variant` 请求头不存在（只读查询串 `?variant=`）；其 `reverted` 变体的切片范围是「分支 + 紧随那次全态 `set_state`」（`i0 < i1` 实测成立）。⑧ debug 侧 `phys-surf-crouch-smoke.mjs` 头注引用的外部实现名 `CanUnduck` 在工作区只剩 `apps/game/scripts/phys-surf-crouch-smoke.mjs` 一处（他人范围）。⑨ 同批登记的**采样表达式字段不存在**：`jump-apex-measure.mjs` 的采样读 `s.posX/posY/posZ/velX/velY/velZ` 与 `au.frame.posY/velY/velX/velZ`，两处前缀在当前结构下都不存在 ⇒ 即便越过就绪门，落盘样本也只剩 `i/t/dt/g` 四个键，`jump-apex-report.mjs` 的数值门无从运行。⑩ **`apps/debug` 的 `test:jump-apex` 空转**：`jump-apex-verify.mjs` 一进入就落 SKIP 分支（打印 `[SKIP]` 并 `exit 0`），而 `.github/workflows/ci-gates.yml` 仍在跑它 ⇒ 该 CI 门恒绿但什么都不验（属 §7.3 #4 的实锤补充）。 | **待 owner 裁决**（① 属代码改动；②③④ 属脚本常量是否随产物更新） |
| 61 | **`apps/viewer/crates/wasm/src/lib.rs` 复验登记的疑似缺陷（只记录不修，静态读码、未跑 wasm/浏览器；6 条）**：① **`.MDL` 三件套替换隐患**：判据 `name.to_ascii_lowercase().ends_with(".mdl")` 大小写不敏感，而配对名用 `name.replace(".mdl", …)`（大小写敏感且替换全部出现）⇒ zip 条目为 `.MDL` 时 `vvd_name`/`vtx_name` 仍等于 `.mdl` 名，两次 `pack.get` 取回**同一份 .mdl 字节**填进 `vvd`/`vtx` 槽位，「三件齐」检查形同虚设；② 模型名匹配（`referenced.contains(name)`，精确）与材质查找（`PakIndex`，大小写不敏感）口径不一致；③ `new` 里 `bsp.pack.clone().into_zip().lock().unwrap()` 的锁中毒会 panic（wasm 陷阱），未像其它失败点转成 `JsValue`；④ `resolve_pakfile_materials` 的去重键是材质名，不同模型对同名材质给不同 `search_paths` 时首次命中的 VMT 会被复用（有意或遗漏静态不可判定）；⑤ 同一份元数据两套待遇：`packed_files` 构造期缓存，而 `num_static_props` 每次 `metadata()` 重新线性扫描；⑥ TS 契约：`map_name` Rust 端恒为空串、TS 端可选 ⇒ 该字段实际不可用（注释已写明，是否删字段由 owner 定） | **待 owner 裁决** |
| 62 | **game 脚本（`phys-rate-parity.mjs` 与 `t13-*` 三件）复核中登记的疑似缺陷 / 口径问题（只记录不修，静态读码；owner 裁决待定）**：① `apps/game/scripts/phys-rate-parity.mjs` 的「混合分区」段**时长与结果两头都不对**——每轮 `1/64 + 2×(1/144)`、共 42 轮再补一个 `1/144`，实测总时长 ≈ **1.2465 s**（旧注写「近似 1s」），且其终态 `c` 声明后从未参与输出（与 64/144 两档的差值只由 `a`、`b` 算出）；② 同件 `flatTop(topY, zEdge, yBot)` 的**宽相位 AABB 与平面覆盖不一致**：`min`/`max` 取 `z ∈ [zEdge − 4000, zEdge]`，而平面本身允许 `z ≥ −4000`；实验④传 `zEdge = zEnd + 5000` ⇒ AABB 从 `z = zEnd + 1000` 起（`src/phys/world.rs` 的候选筛选与空间网格都读 `min`/`max`）⇒ 接缝后 1000 u 内该块不参与查询。是否改脚本常量由 owner 定；③ `t13` 前缀与 `src/Cargo.toml` 里出现的 `t3` 标签在仓内**无定义来源**（只出现在三个脚本名与该条 Cargo 注释里），与 #52 同型；④ `apps/game/scripts/check-wasm-api.mjs` 的 `PHYS_API` 覆盖缺口见 #9（同一批脚本的门禁面）。 | **待 owner 裁决** |
| 63 | **game 脚本 11 件批复核中登记的疑似缺陷（只记录不修，静态读码；owner 裁决待定）**：① `apps/game/scripts/_dbg_floor.mjs` 的 `onGround` 列**恒为 `undefined`**——它从 `state_full_json` 解析后读 `s.onGround`，而种子 JSON 的键是 snake_case（`src/phys/seed.rs` 的 `on_ground`），camelCase 名只出现在 `state()` 出口；② 死赋值 / 死局部：`phys-gate-probe2.mjs` 的 `let st = null, prev = null` 中 `prev` 只被赋值、从不被读，`phys-p2-ground.mjs` 的 `const coll = …` 算完即弃（入行的是 `collide`）；③ `phys-p2-regression.mjs` 的 `ALL PASS` 分支在当前产物下**不可达**（交付方实跑 5/12 发散），脚本也不以退出码表达判定 ⇒ 「发散」不影响 CI；④ **代码字面量级（⑤类）**：`phys-smoke.mjs` 的 FAIL 串含已删计划编号 `D-16`、`phys-diag-flat.mjs` 的模板字符串写着与实跑不符的期望值（`t1 y≈99.95 v=-6.25`，实测 `y=99.9023 v=-12.500`）——两处都在**代码字符串**里，改注释解决不了；⑤ `phys-teleport-gate.mjs` 场景 1 的判据在「从未接触坡面」时也不会失败（`contact = −1` 不判 FAIL），前置条件（真正进入 `surfing`）本身没有被断言；⑥ `phys-surf-crouch-smoke.mjs` 的**行数口径**：任务书与台账旧记 145，实测 **157**（debug 侧同名件的旧记同为 145、实测 157 → 160；两处 145 均为漏计），与 #43 同类，属控制文件口径待终审；⑦ 两件 gitignored 脚本（`_dbg_keys` / `_dbg_floor`）**没有 HEAD 参照集**，`verify.ps1` 无法判定 ⇒ 已改为「编辑前快照 + 同款两遍算法」的替代判据并写入规范篇陷阱第 12 条（工具 `.tmp/tools/snapshot-diff.mjs`）。 | **待 owner 裁决**（④ 属代码改动；⑥ 属控制文件口径） |
| 64 | **WG6b（viewer 脚本/测试 5 件）复核中登记的疑似缺陷（只记录不修，静态读码、未跑浏览器；owner 裁决待定）**：① **跨 3 文件链路级**：`test/maps/surf_null_4.replay` 已不在该路径——`test/maps/` 实测只有 4 个 `.bsp`，同名同尺寸（53,365 B）的文件在 `test/replay/`；受影响：`apps/viewer/test/replay-selftest.ts` 的 `FIXTURE_URL`（`[2]`/`[8]` 前半整段落 SKIP，交付方实跑 ok=93 / FAIL=0 / SKIP=1）、`apps/viewer/test/smoke-cdp.mjs` 的 `LOCAL_REPLAY`（`[3]`–`[11]` 全 SKIP）、`apps/viewer/scripts/build-dist.mjs` 的示例源（构建告警 + dist 深链示例缺失）；② `smoke-cdp.mjs` 的 `[3b2]` 断言按键数 = 6、标签集 `{W,A,S,D,跳,蹲}`，而 `apps/viewer/src/ui/telemetry.ts` 的 `KEYS`（8 项 `KeyDef`）渲染 **8** 个 `.tm-key`（Q/W/E/A/S/D/蹲/跳）⇒ 当前 UI 下必 FAIL；③ 同件 `[0]` 段的 3 条静态断言（classic `./app.js`、无 `<script type="module"`、根目录无 `worker.js`/`*.wasm`）只对 single 产物成立，`--multi` 产物下必 FAIL；④ 同件缺省 `SMOKE_URL` 指向 `http://127.0.0.1:8080/web/index.html`（`apps/debug` 的 dev 端口），而 viewer 的 `dev` 是 8100（`apps/viewer/package.json`）⇒ 不设环境变量时只在导航失败处报「dev server 起了吗？」；⑤ 旧注里的三处「实测」数字（`cos=0.9992`、锚定 bug `~10.7k HU`、「HEAD 中已验证的转换产物」）在工作区无法核对，已在注释中删除（同 #59⑧ 口径）；⑥ `apps/viewer/scripts/dist-README.md` 是 prose 资产、`verify.ps1` 的代码同一性判据不适用（主控已裁决接受文本重写，规则见规范篇陷阱第 13 条）。 | **待 owner 裁决**（①②③④ 属代码改动） |
| 65 | **范围补漏盘点（工具 `.tmp/tools/coverage-scan.mjs`，主控自建）+ 配置面口径冲突**：以 `git diff --name-only HEAD` ∪ `git ls-files --others --exclude-standard` 对比工作区实际存在的范围文件 ⇒ 未改动数由 95 降到 77，逐条分诊为四类：① 当时在跑件；② 三方/产物类（`src/vendor/vmdl/**` 19 件、`apps/game/temp/*.txt` 13 件、三个 `web/{app.js,worker.js}` 与 `web/coi-serviceworker.js`、`src/phys/{LICENSE,NOTICE}`、三个 `package-lock.json`、三个 `package.json`）；③ **WG1 未覆盖的真实漏项**：`src/lib.rs`、`src/serve.py`、`src/scripts/**` 5 个 `.mjs` + 3 个 `.cmd`、三工程 `{build-dist,play,start-dev}.cmd` 9 件、三个 `web/index.html` 与 `web/styles.css` 6 件 ⇒ 已全部纳入 WG12 并按资产族判据处理；④ **配置面（口径冲突）**：7 个 `Cargo.toml`（含 `src/Cargo.toml` 里带 `t3` 标签的那条注释）、4 个 `.gitignore`、3 个 `tsconfig.json` 含注释，而 §4 的 P0–P4 既未列出、排除清单也未排除它们。**另**：⑧ 原先「9 个工程 `.cmd` 无注释」的记录已在 §7.2 WG12 更正为「7 件有 48 条 REM 注释」 | ③ 已处置；**④ 待 owner 裁决** |
| 66 | **`apps/debug/scripts/input-replay-verify.mjs` 复验登记的疑似缺陷（只记录不修；5 条，均经主控独立复核）**：① **⚠ 链路级**：`InputRecorder.record()`（`apps/debug/src/input/input-recorder.ts`）在全部 `apps/debug/src` 内**只有一个调用点**，且落在回放分支的 `replayCapture`（`apps/debug/src/app.ts`，`replayCapture` 与该文件的 `inputRecorder` 都是 `InputRecorder` 实例）⇒ 用户录制器 `inputRecorder` **永不落样本**、`exportJson()` 的 `frames` 恒为空 ⇒ 脚本中依赖「录制载荷」的相位在本页面无法通过（`apps/debug/src/app.ts` 的注释已按代码写明这一点）；② **判据字段不存在**：脚本按 `f.map((r) => r.dt)` / `f.dt` 取步长，而 `InputFrame`（同文件）的字段只有 `t / dx / dy / keys`，**没有 `dt`** ⇒ 带 dt 帧数恒 0，必走「录制未带每帧物理步长 dt」失败分支；步长实际写进 `record()` 的第 5 参 `dtS`，而本仓调用点省略该参（模块头注写明「大于 0 才写入，0 表示未记录」）；③ **页面缺 7 个被查询的 id**：`apps/debug/src/app.ts` 用 `document.getElementById` 取 `inputRecStatus`、`inputRecToggleBtn`、`inputRecClearBtn`、`inputRecExportBtn`、`inputRecLoadBtn`、`inputRecStopPlayBtn`、`inputRecFile`（均标 `| null`），而 `apps/debug/web/index.html` 的 106 个 id 中一个都没有 ⇒ 录制面板状态行与按钮在本页恒为 null（脚本自身也查 `#inputRecStatus`）；④ `sumDx` 只写不读（声明处清零、采样循环累加，输出串只打印 `sumDy`）⇒ 死累加；⑤ `finish()` **不立即退出**：它把 `process.exit(code)` 排进 `setTimeout(…, 250)`，故 `#bspFile` 找不到时「打印错误 + `finish(1)`」之后仍会继续执行，后续以 `TypeError` / `unhandledRejection` 路径收场（由顶部的 `uncaughtException` 再兜一次）。**修法均属代码改动，未擅改** | **待 owner 裁决** |
| 67 | **WG5b 末批 4 件（`apps/game/scripts/{phys-seed-smoke,build-dist,phys-rate-parity-v2,phys-dual-pipe}.mjs`）复核中登记的疑似缺陷（只记录不修，静态读码；15 条，未运行脚本）**：① `phys-dual-pipe.mjs` 的 `K` 为死常量、`authTimes` / `authTickCount` 只写不读、`analyze` 的返回值被丢弃、d/e 两档到达分支等价、脚本**不以退出码表达判定**；② `phys-rate-parity-v2.mjs` 的 `isNew` 恒真（死判据）、无退出码、「首碰」判据在斜面上同样成立；③ `phys-seed-smoke.mjs` 的 `r2` 是空窗口死调用（同处注释已如实说明）、F 段 `near([...])` 的首个析取式恒假（死表达式）、`throwOk` 不校验错误类型；④ `build-dist.mjs` 的 `__VBSP_TEXTURES_MTZ_B64__` 在 `apps/game/src` **无读取点**（debug 侧有）、single 产物 `dist/index.html` 引用的 `coi-serviceworker.js` 不在 `KEEP_SINGLE` ⇒ **必然 404**、五处 `[5/5]` 历史标签仍在**代码字符串**里。**另有 3 处不可定位字样只存在于代码字符串**（`phys-seed-smoke` 的 ok/fail 文案含「活性缺口」「bench s3c/s4b/s5」、`phys-rate-parity-v2` 的输出标签「实验⑥：Q3 双管道复现」、`build-dist` 的 `console.warn(… 可能仍是 module script…)`）⇒ 属代码改动（同 #36/#40/#52 口径） | **待 owner 裁决** |
| 68 | **WG12 `src/` 侧 8 件复核中登记的疑似缺陷（只记录不修，静态读码；10 条）**：① **`src/scripts/check-shared-sync.mjs` 的四项门禁当前恒定失败**——`manifests` 仍列退役工程的 `Cargo.toml` 路径，`checkVmdlPatch` 必报「缺少模块清单」（属 §7.3 #4 的又一实例，注释已按代码写明、未复述该路径）；② 同件 `sha256` 只比 `slice(0,16)`（64 bit）却按「全等」表述；③ 同件 `checkEyeStand` 比的是字面量**文本**（`64.09` 与 `64.090` 会判不一致）；④ `check-doc-drift.mjs` 以字节数近似「行数」、放行「总行数 + 1」、并硬编码退役工程路径；⑤ `dist-pack.mjs` 的 `bytes = text.length`（UTF-16 码元数，非字节数）；⑥ `wasm-api-contract.mjs` 的 `!endsWith(`.d.ts`)` 恒真、`findPkgEntryJs` 取 readdir 首项（未排序）、`scanDir` 不跳 `node_modules` 且不捕获读异常、不采集 `import * as ns`；⑦ `src/serve.py` 顶层即起服务（无 `__main__` 守卫）且绑所有网卡，`Access-Control-Allow-Origin: *` 在同源 Worker 场景下无消费点；⑧ `wasm-stale-check.mjs` 纯 mtime 判据、roots 为空时静默判「新鲜」；⑨ `src/lib.rs` 的旧注曾漏 `phys::`（已按代码改正，见台账）；⑩ 同批 5 条 `[待确认]` 均未写入注释（旧注里的外部因果与实测记录，工作区无法核对） | **待 owner 裁决** |
| 69 | **9 个工程 `.cmd` 复核中登记的疑似缺陷与口径（只记录不修，静态读码、未运行脚本；owner 裁决待定）**：① **门/消费方 wasm 路径错配（三工程 `start-dev.cmd`）**——过期门盯 `pkg\…_bg.wasm`，而被服务的开发页取 `web\…_bg.wasm`（`apps/debug/src/main-wasm.ts`、`apps/game/src/app.ts`、`apps/viewer/src/core/bsp.ts`）⇒ `web/` 缺失或陈旧而 `pkg/` 新鲜时判「新鲜」并跳过重建；② **同型但更尖（`apps/viewer/play.cmd`）**——门测 `pkg\websurf_viewer_wasm_bg.wasm`，而 viewer 的 `apps/viewer/scripts/build-dist.mjs` **只读** `web/websurf_viewer_wasm_bg.wasm`（该文件无 `pkg` 引用）⇒ 跳过 wasm 步后在读文件处失败（`apps/{debug,game}/scripts/build-dist.mjs` 读的恰是 `pkg/`，那两件门与消费方一致）；③ **`apps/viewer/play.cmd` 的 python 守卫使其兜底不可达**——守卫在转发 `dist\play.cmd` 之前 `exit /b 1`，而代劳方 `apps/viewer/dist/play.cmd` 实现了 `npx --yes serve -l %PORT% .` 兜底 ⇒ 有 Node 无 Python 的机器上工程根入口先死；④ `apps/viewer/play.cmd` 的端口占用分支打开 `<port>/index.html` 只在占用者服务 `dist/` 时正确（`apps/{debug,game}/play.cmd` 是镜像假设 `/dist/index.html`，而 `apps/viewer/` 工程根无 `index.html`（dev 页在 `apps/viewer/web/index.html`，dist 入口 `index.html` 由 `apps/viewer/scripts/build-dist.mjs` 生成到 `apps/viewer/dist/`，该目录是构建产物、不入库））；⑤ `start-dev.cmd` 只守 `python`，无 npm/wasm-pack 守卫（`build-dist.cmd` 有 `TOOLCHAIN_OK` 检查）⇒ 缺 `wasm-pack` 要到 `npm run build:wasm` 才暴露。**修法均属代码改动且跨工作组**（`apps/*/scripts/build-dist.mjs`、`apps/*/start-dev.cmd`、`apps/viewer/play.cmd`） | **待 owner 裁决** |
| 2026-09-22 | **配置面专项盘点（主控自做）：9 个 `Cargo.toml` + 4 个 `.gitignore` 全部未改动，3 个 `tsconfig.json` 实测 0 注释（从配置面剔除）；并实测出 5 处对已退役 `test/` workspace 的过期引用** | **盘点口径**：`git status --porcelain` 对每个配置面文件单独判状态 + 逐文件实测注释条数与退役引用。**结果**：① `Cargo.toml` 共 **9 个**（根、`src/`、`src/wasm-core/`、`apps/{debug,game,viewer}/`、`apps/{debug,game,viewer}/crates/wasm/`），**全部 `unchanged`**（原注释一字未动）；② `.gitignore` 共 4 个：根 `.gitignore` 已被改动（`M`，96 行 / 33 行注释，含 `.ak/` 与 `test/game-core/` 相关的忽略规则与说明），`apps/{debug,game,viewer}/.gitignore` 均 `unchanged`（11 / 6 / 2 行注释，无退役引用）；③ `tsconfig.json` 3 个**实测 `//` 出现次数 = 0**（JSON 无注释）⇒ 与三个 `package.json` 同属**无对象**，配置面由「7+4+3」更正为 **9 + 4（+0）**。**实测出的过期内容（均为原注释，未改；`test/dual-mode-harness` 已退役 ⇒ 引用不存在的 workspace）**：`Cargo.toml:9`「模块 crate 保留各自 workspace（debug/ 等 **4 个**…）」与 `:18`「各模块 workspace 的同款 patch 在 debug/game/viewer/**test** 各自的 Cargo.toml」——两者都按「含 `test/` 的 4 个模块」计数，实测现存模块 workspace 只有 **3 个**；`apps/viewer/Cargo.toml:11`「与 debug/game/**test** 同款 `[patch.crates-io]`」；`src/Cargo.toml:8` 与 `src/wasm-core/Cargo.toml:6`「与 websurf-phys、**四个模块 wasm crate** 同 workspace」同型。**另记两条原注释里的不可核验内容**：`src/Cargo.toml:9` 的变更史式「曾为独立 workspace root，**2026-09-06** 合并」（版本沿革 + 日期）与 `:10` 起的 `t3:` 标签注释（含「实测 `origin[2]` 10.478655362066775 → …776」，与 §7.3 #62③ 的 `t3` 无出处同源）。**结论与建议（待 owner 裁决）**：配置面**确含过期事实**（5 处 `test/` 引用 + 2 处计数），因此建议把 **9 个 `Cargo.toml` + 4 个 `.gitignore`** 纳入重编范围（`tsconfig.json` 3 个无对象、不入范围）；本轮**未擅改**（属 §6 第⑥类配置改动 + #65④ 待裁决项）。判据将沿用资产族口径（配置文件无「代码同一性」判据：`.gitignore` 的注释是文本、`Cargo.toml` 的注释行按 `//` 不可判 ⇒ 需另建 `toml-comment-proof` 或按「剥注释后配置项文本逐字符相同」判） | `Cargo.toml`、`src/Cargo.toml`、`src/wasm-core/Cargo.toml`、`apps/{debug,game,viewer}/Cargo.toml`、`apps/{debug,game,viewer}/crates/wasm/Cargo.toml`、`.gitignore` ×4、`apps/*/tsconfig.json`（只读盘点，未修改） |
| 2026-09-22 | **WG10 部分落地（主控自做）：根 `README.md`、`CHANGELOG.md`、`documents/index.md` 三篇新建并过门；另实测出 CI 门禁作业当前必失败** | **三篇新稿**：根 `README.md`（112 行：受控范围表 / 仓库结构 / 快速开始 / 构建链 / 验证与门禁 / 共享层依赖方向 / 文档地图 / 已知缺口摘要 / 许可与第三方）、`CHANGELOG.md`（26 行：只登记**当前工作区状态**与各包的 `0.1.0` 版本声明处，明确写出「不含更早的版本条目、也不据已删文档或提交历史重建」）、`documents/index.md`（45 行：按**实际存在的文件**重建导航 + 维护约定）。**判据**：三篇 `CRLF` 归一后 `bareLF=0 BOM=false 以换行结尾`；`node src/scripts/check-doc-drift.mjs` ⇒ **23 篇 md / 锚点 488 / 越界 0 / 路径失效 0 / exit 0**；`anchor-scan` 违规 0；`content-review` 首轮抓出 **2 处自造路径**（`README.md:51` 把「app 相对路径」写成仓内路径 `src/worker/main.ts`、`src/app.ts` —— 实际入口在 `apps/<app>/src/**`）⇒ 已按仓内真实路径改写并复跑为 **15 条仓内路径全命中 / bad=0**；禁用词 11 个全 0。**README 里每条锚点都实测**（`apps/*/package.json` 的 `dev`/`build:wasm`/`build:ts`/`build:dist` 行号、`src/serve.py:18/:19/:20`、`Cargo.toml:22`、`src/Cargo.toml:2`、`src/wasm-core/Cargo.toml:10`、`apps/debug/crates/wasm/Cargo.toml:22`+`:24`、`apps/viewer/crates/wasm/Cargo.toml:19`、`apps/game/scripts/build-dist.mjs:60`+`:62`、`src/ts-shared/auth/shared-state.ts:1022`、CI 三份 workflow 的对应行）。**CI 事实核查（新增证据，属 §7.3 #4 的实锤补充）**：`.github/workflows/ci-gates.yml` 的四个作业里，`rust-unit-tests`（`cargo test -p websurf-phys`）、`game-gates`（`test:phys`/`test:seed-smoke`/`test:surf-crouch`）、`viewer-gates`（`test:replay`）与 `apps/debug` 的五个 `test:*` **全部在各自 `package.json` 中存在**（实测逐个核对）；**唯一必失败的是 `harness-and-debug-gates`**——它在 `test/dual-mode-harness` 里跑 `npm ci` / `build:wasm` / `build:ts` / `test:three-mode`，而该目录已从工作区移除（§7.3 #4）。另 `.github/workflows/doc-drift.yml:47` 在 PR 与 main 上跑 `node src/scripts/check-doc-drift.mjs`；`.github/workflows/deploy-pages.yml:129` 以 `--multi` 构建三工程并用 `apps/debug/scripts/pages-index.html` 组装站点首页（`:176`）。**三篇新稿未提交**（本任务不执行提交） | `README.md`、`CHANGELOG.md`、`documents/index.md` |
| 2026-09-22 | **WG11 收尾工具落地（主控自做）：新建一键全量复检 `.tmp/tools/wg11-check.ps1` 并跑通首轮；同时用它在「飞行期」抓到 WG9 三棵子树的 2 类问题并按同一 attempt 派回修正** | **新工具**（纯 ASCII，避免 PS 5.1 误码；依次跑五段）：① **资产族专用证明**——`.py` 走 `py-proof.py`、`.cmd` 走 `cmd-proof.mjs`、`.html` 走 `html-struct-proof.mjs` + `html-markup-proof.mjs`、`.css` 走 `css-proof.mjs`（`verify.ps1` 对这些注释语法无判据）；② **全量行尾/BOM 扫描**（逐文件打印 `CRLF / bareLF / BOM`，收尾判 `bareLF=0 BOM=false`）；③ **文档门**——`check-doc-drift`（全仓）+ `anchor-scan` + `content-review`（只扫已改文档）；④ **编译门**——`cargo check/test -p websurf-phys` + 三工程 `npm run typecheck`；⑤ **`verify.ps1` 分批**（**必须放最后**：它末尾的 `exit` 会终止整个 pwsh 进程，日志先落盘再判读）。**首轮实测（220 个静止路径）**：`py-proof` ⇒ `PY-CODE-IDENTICAL`；`cmd-proof` **9/9 `diff=0 nonASCII=0`**（含 `src/scripts/cargo-env.cmd` 14=14、`install-wasm-bindgen.cmd` 78=78）；`css-proof` **2/2 `CSS-CODE-IDENTICAL`**；`html-struct-proof` **4/4 OK**（`pages-index.html` 的 markup 证明如预期为 DIFF——它的可见文本本就是重写对象）；`cargo check -p websurf-phys` exit 0、`cargo test -p websurf-phys` exit 0 且 **`running 10 tests / 10 passed`**（复核了附录 A 的「10 项全过」口径；先前脚本只截最后一行的 `0 passed` 是第二个测试目标，现按全量输出判读）；三工程 `typecheck` exit 0；`check-doc-drift` **38 篇 md / 1946 锚点 / 越界 0 / 路径失效 0 / exit 0**。**首轮抓到的飞行期问题（已按同一 attempt 发消息派回三个子代理修正）**：① **行尾**——WG9 已落盘的 **12 篇新文档全部是 LF**（`CRLF=0`），违反模板的 `CRLF + 无 BOM` 硬规则，已要求收尾前统一跑 `.tmp/wg9/normalize-crlf.mjs`；② **路径断言不合格 47 处**（`content-review` bad）——两个子类：**(a) 把 app 相对路径当仓内路径**（viewer 7 处：`src/app.ts`、`src/config.ts`、`src/worker/main.ts`、`src/renderer/lightmap-shader.ts`、`test/node-shims.d.ts` 等；game 5 处：`src/app.ts`、`src/config.ts`、`src/savepoint.ts`、`src/world/types.ts`、`src/wasm.d.ts`）——真实位置都在 `apps/<app>/` 下，已要求写全或去掉反引号；**(b) 链接到尚未写出的文件**（各工程 README 列的 `implementation/*.md` 与 `differences.md`），属交付时点问题，已要求「README 列出的文件必须全部存在」。**主控并修掉 AGENTS.md 自身的 4 处同类问题**：`apps/game/favicon.ico`（该文件已删，**去反引号**避免被当成存在性断言，两处）、`test/{replay-selftest.ts,smoke-cdp.mjs,node-shims.d.ts}` → 限定为 `apps/viewer/test/**`（三处），另打破一处「路径 + 符号」误配（`node-shims.d.ts` 与紧随其后的 `verify.ps1` 相距 9 字符被判成断言）⇒ 复跑 `content-review` **64 路径 + 5 符号全命中 / bad=0**。**规范篇补记**：陷阱第 14 条增设第三子项——**PowerShell 的反引号是转义符**，把含反引号的替换写成内联 `node -e "…"` 会让补丁**静默命中 0 次**（本轮实测一次），含反引号的补丁一律落 `*.mjs` 并断言命中数为 1 | `.tmp/tools/wg11-check.ps1`、`documents/norms/annotation-and-verification.md`、`AGENTS.md` |
| 2026-09-22 | **配置面复核追加一条实锤：根 `.gitignore` 的 `.ak/` 规则与其注释描述的归档目录不存在** | `git diff -- .gitignore` 实测：本轮唯一被改动的配置文件是根 `.gitignore`（**+4 行**），新增内容为一条忽略规则与两行注释——`# ── 旧文档归档区（2026-09-22 文档重编任务）──`、`# 重编前把全部旧 md 原样移入 .ak/（保留相对路径）。**不得作为事实来源**，仅作历史留档与回滚。`、`.ak/`。**实测矛盾**：工作区**没有 `.ak/` 目录**（`Test-Path .ak` 为假；`AGENTS.md` §2 亦记「`.ak/` 不存在，该规则为历史遗留」），因此注释所断言的「旧 md 已原样移入 `.ak/`」**无法从工作区核验**，属该配置文件的**事实性断言不合格**（B 类口径）；另注释内含任务日期与任务名，属重编范围内应清理的变更史痕迹。**处置**：不擅改（`.gitignore` 属配置面、见 §7.3 #65④ 待 owner 裁决）；已把该实锤并入 #65 索引行，供 owner 一次性裁决配置面时使用。**其余 3 个 `.gitignore`（`apps/{debug,game,viewer}/.gitignore`）实测未被改动且无退役引用**（注释行数 11 / 6 / 2） | `.gitignore`（只读复核，未修改） |
| 2026-09-22 | **配置类资产判据落地（主控自做）：新建 `.tmp/tools/config-proof.mjs` 并跑出「本仓库唯一被改过的配置项」** | **工具**（`.toml`/`.gitignore` 族，补规范篇陷阱第 13 条的第 ⑪ 项）：先做行尾归一（`git show` 是 LF、工作区 CRLF，否则属性含 `#` 的行会被误判），再剥掉 `#` 注释与空行，把两侧配置文本**逐字符**比对，并打印注释行数前后计数；要求 `CONFIG-CODE-IDENTICAL`。**工具局限已在文件头写明**：`#` 出现在 TOML 字符串字面量内会被一并剥离，故它证明的是「剥注释后的配置文本相同」，不是 TOML 语义等价。**首轮实测 13 个配置文件**：`Cargo.toml` 9 个（根 / `src/` / `src/wasm-core/` / 三工程 / 三工程 `crates/wasm/`）全部 `OK 配置文本逐字符相同`（注释行 21/4/16/10/24/10/22/9/11，前后一致）；`apps/{debug,game,viewer}/.gitignore` 同样 `OK`（注释 11/6/2，配置字符 0/94/66）；**根 `.gitignore` 报 `DIFF`** —— 配置字符 610 → 615、注释行 31 → 33、差异首行即新增的 `.ak/` **忽略规则**（规则是代码不是注释，工具如实报出）。**结论（供 owner 裁决 #65④ 使用）**：13 个配置文件中**只有根 `.gitignore` 被改动过**，且改动**含一条真实规则**（`.ak/`），而该目录在工作区不存在（同日另一行已记该注释的事实性断言不合格）；其余 12 个配置文件的配置文本与注释行数均与 HEAD 一致。**另记盘点工具的一处盲区**：`coverage-scan.mjs` 以 `git status` 为准，**gitignored 文件永远不会出现在「已改动」侧**（本轮实测：`apps/game/scripts/_dbg_keys.mjs`、`_dbg_floor.mjs` 被列为「未改动」，实际已被重编）⇒ 这两件必须用**编辑前快照**判定；本轮已复跑 `.tmp/tools/snapshot-diff.mjs`：`_dbg_keys` `strict/stripTrail 18/18 diff=0`、`_dbg_floor` `28/28 diff=0` ⇒ 双件 `CODE-IDENTICAL`（快照取自 09:17，早于 21:28 的重编，且快照首行带 BOM、比对时已剥离）。**盘点收口结论**：范围 265 件、未改动 69 件，逐条分诊后**全部**落入「配置面（13 件，待裁）+ 无对象（三个 `package.json` / 三个 `package-lock.json` / 九个 `web/{app.js,worker.js,coi-serviceworker.js}` / `apps/debug/web/styles.css` / `src/scripts/ensure-node-deps.cmd`）+ §4 已排除（`src/vendor/vmdl/**` 19 件、`apps/game/temp/*.txt` 13 件、`src/phys/{LICENSE,NOTICE}`）+ 保留资产（`apps/debug/scripts/path-baseline.md`）四类**，**不存在未处理的代码注释文件** | `.tmp/tools/config-proof.mjs`、`documents/norms/annotation-and-verification.md`、`AGENTS.md` |
| 2026-09-22 | **WG9 / `apps/viewer` 子树（12 篇）交付并经主控独立复验；同时补两个「没有门会查」的空档** | **交付**：`documents/viewer/{README,overview,sequences,differences}.md` + `implementation/{app,core,replay,ui,renderer,worker,wasm,scripts-and-test}.md` 共 12 篇（主题划分取自实测目录：`src/{core,replay,ui,renderer,worker}` + 入口 `app.ts`/`wasm.d.ts` + `crates/wasm` + `scripts`+`test`）。**主控复验（不采信自述，逐门重跑）**：12 篇 `CRLF` 计数 48/101/97/30/51/54/79/48/53/33/54/62、`bareLF=0`、无 BOM、均以 CRLF 结尾；`content-review` **101 条仓内路径 + 11 条「路径 的 符号」全命中 / bad=0**；`anchor-scan` 违规 0；禁用词 11 词 + 2 个退役路径 needle **0 命中**；`check-doc-drift`（全仓）exit 0。**新增两个工具补门**：① `.tmp/tools/anchor-open.mjs`——把 `路径:行号` 锚点**逐条打开并打印该行内容**（均匀抽样或 `--all`，同时标出缺失文件与越界），本轮对 viewer 抽 44 条**全部命中所述符号**（例：`apps/viewer/src/renderer/lightmap-shader.ts:1788 → export function setLightGamma`、`apps/viewer/scripts/build-dist.mjs:61 → const KEEP_MULTI = [`、`apps/viewer/src/replay/trackpanel.ts:19 → export interface TrackPanelOptions`）；对共享层三篇抽 15 条同样全中（`src/phys/mod.rs:111 → pub struct PhysWorld` 等）。② `.tmp/tools/link-check.mjs`——查 **markdown 相对链接**是否可解析：`check-doc-drift` 只查行数声明/行号锚点越界/反引号路径失效/裸名歧义，**链接形式的目标完全没人查**；首轮全仓 52 篇 25 条相对链接里**只发现 1 处坏链**（台账自身一行把链接写成 `documents/plan/progress-log.md`，从该文件出发解析成双层路径），已改为同目录相对名并复跑 `LINK-CHECK-OK`。**主控独立复核 3 条实质断言**：`timeline.ts:292-293` 的 A-B 区间带宽度公式与绘制条件（配合 :269 注释 ⇒ 该带恒不显示成立）、`lightmap-shader.ts:1789` 的 γ 接受窗口 `(0,1]`、三份 `lightmap-shader.ts` 的 SHA256 相同。 | `documents/viewer/**`、`.tmp/tools/{anchor-open,link-check}.mjs` |
| 2026-09-22 | **主控修正一处「注释与事实不符」（由 WG9 读码发现）：三份 `lightmap-shader.ts` 同构副本的自述行** | **问题（实测）**：三份副本**逐字节相同**（SHA256 一致），而共享的第 8 行写「本文件是 `apps/debug/src/renderer/lightmap-shader.ts`」——在 `apps/game` 与 `apps/viewer` 两份里这句**是错的**（属本轮重编范围内的注释不准确，且是跨工程口径冲突）。**修法（保持三份仍然逐字节相同）**：把该行改为**与路径无关**且在三份里都成立的写法——「本文件是上述三份同构副本之一（按所在工程目录定位）。改动只对所在工程生效。」**证据**：三份改后 SHA256[:24] 仍全等（`EA710E007FBE8AEEB1B754C1`）；`verify.ps1` **0/3**（`strict:890/890/d0`、`stripTrail:890/890/d0` ⇒ **纯注释改动**、代码行 890 行不变；`禁用词 0/基线8`）；`template-proof` **PROOF-OK**（33 个模板字符串逐字节相同）；`anchor-scan` 违规 0。**连带更新 2 篇 viewer 文档**（它们原先把这个冲突写成「已知缺口」）：`documents/viewer/implementation/renderer.md` 的第 1 条改为「副本自述与路径无关（已按代码校准）」，`documents/viewer/differences.md` 的对照行同步改写 ⇒ 缺口从「三份文件里两句是错的」变为「三份文件里三句都成立」。**方法论记一笔**：这是「文档重编反过来暴露并修掉代码注释错误」的第一个实例，判据是「同构副本的自述行必须对每一份都成立」。 | `apps/{debug,game,viewer}/src/renderer/lightmap-shader.ts`、`documents/viewer/{implementation/renderer.md,differences.md}` |
| 2026-09-22 | **口径更正三则（WG9 viewer 读码复核我此前的台账条目；按「已知错误另起一行更正」处理）** | ① **#59⑧ 归类错误**：我记「`visuals.ts` 的 `'与 debug 权威帧方点同款观感'` 属**代码字符串**」——实测该串在 `apps/viewer/src/replay/visuals.ts:192` 是 **`//` 注释**（真正的代码字符串是 `timeline.ts:160` 的 tick 点 `title`）⇒ 归类更正为「注释里的跨工程文字引用」，本轮已按代码改写。② **#59③ 与代码不符**：我记「`workerBroken` 之后的 promise 不被 reject、调用方 `.catch` 永不触发」——实测 `apps/viewer/src/replay/importer.ts:122` 返回 `Promise.reject(new Error('__NO_WORKER__'))`，且 `:153` 有转主线程分支 ⇒ 该条**不成立**，不写入正文。③ **#58④ 后半句不可核验**：我记「rejected 后从不重置，且无 catch ⇒ 未处理拒绝」——实测 `ensureWasm()` 无独立调用点，唯一调用点 `apps/viewer/src/app.ts:247` 在 `try/catch` 内 ⇒ 只保留可核验的前半句（「首次失败永久缓存、本次会话不自愈」）。**处置**：三条以本行为准（#59⑧ 改归类、#59③ 作废、#58④ 收窄），对应正文已由 viewer 子树按代码写成现状。 | `documents/plan/progress-log.md`（本行）、`apps/viewer/src/replay/{visuals.ts,importer.ts}`、`apps/viewer/src/app.ts`（只读复核） |
| 2026-09-22 | **WG9 / `apps/game` 子树（14 篇）交付并经主控独立复验** | **交付**：`documents/game/{README,overview,sequences,differences}.md` + `implementation/{app-entry,config,renderer,worker,input,panel,savepoint,types,scripts,wasm-crate}.md`（主题按实测目录取）。**主控复验（逐门重跑）**：14 篇全部 `CRLF`、`bareLF=0`、无 BOM、均以 CRLF 结尾；`content-review` **90 条仓内路径 + 25 条「路径 的 符号」全命中 / bad=0**；`anchor-scan` 违规 0/0/0；`link-check` 0 缺失；禁用词 11 词 0 命中；`check-doc-drift`（全仓）exit 0。**锚点开箱抽样 45 条**（每篇 5 条）全部命中所述声明（例：`apps/game/src/app.ts:317 → getElementById('loadMapBtn')?.addEventListener(...)`、`apps/game/scripts/check-wasm-api.mjs:55 → const PHYS_API = [`、`apps/game/scripts/build-dist.mjs:60 → const KEEP_SINGLE = [...]`、`apps/game/src/panel/panel-controller.ts:37 → export class PanelController`、`apps/game/src/renderer/lightmap-shader.ts:1789 → if (!Number.isFinite(value) || value <= 0 || value > 1) return;`）。**交付方本次新发现并写入正文的缺口 13 条（A–M，供登记）**：A `apps/game/src/app.ts:680` 的 `LOAD_STAGE_PCT` 含无生产者的阶段名（死键）；B `apps/game/src/input/input-bridge.ts:83` 的 `sendSetDeathThreshold` 零调用点（权威侧保持 `src/phys/mod.rs:165` 的 `death_y = -100_000.0` 初值）；C `apps/game/src/app.ts:253` 用 `p instanceof Promise` 判 `requestLock` 返回值恒真 ⇒ else 不可达；D `apps/game/src/renderer/renderer-main.ts:113` 的 `ENABLE_PVS = false` 而 `pvsManager` 与 `clusterIds` 仍逐块构造（零效果计算）；E 同件 `:166` 的 `sampleEpoch` 只写不读；F `apps/game/crates/wasm/src/lib.rs:480` 有一处**悬空** `#[wasm_bindgen]`（与 `:492` 叠加；交付方实跑 `cargo check` **exit 0**，未报重复属性 ⇒ 如实记为「不影响当前构建」）；G `apps/game/src/renderer/lightmap-shader.ts` 内 6 个旋钮/查询导出在本工程零导入点（`:1752/:1757/:447/:313/:1817/:1795/:1836`）另 4 个 GLSL 常量；H `renderer-main.ts:1340` 的 `worldBox` 只累计单材质网格（多材质/无材质网格进 `keptMeshes` 早退分支）；I `apps/game/src/world/types.ts` 在本工程零导入点；J 实测 `apps/game/scripts/` 20 件里**只有 7 件**用退出码（其余 13 件结论只在 stdout 末行）；K 交付方**实跑** `phys-p2-regression.mjs`：12 组 5 组发散、末行「参考矩阵：5/12 发散」、**exit 0**（与 #63③ 一致）；L `apps/game/scripts/check-wasm-api.mjs` 的两级清单与实现不同源（`take_event` 被列进 `EXPORT_API` 实为 `PhysWorld` 方法；`PHYS_API` 实为 17/24）；M 面板侧三处（`panel-controller.ts:568/:577` 数值框路径不回写自身文本、`:773` 死变量 `dot`） | `documents/game/**` |
| 2026-09-22 | **口径更正两则（WG9 game 读码复核我此前的台账条目）** | ① **#67④ 反转（重要）**：我记「`apps/game/scripts/build-dist.mjs` 的 `__VBSP_TEXTURES_MTZ_B64__` 在 `apps/game/src` 无读取点 ⇒ single 产物的内嵌纹理包不可用」。**实测更正**：`apps/game/src` 内确无**直接**读取点，但 `apps/game/src/app.ts:515` 把 `decompressMtz: decompress_mtz` 注入 `buildWorldBundle`，而 `src/ts-shared/phys/world-builder.ts:228` 起「仅在注入 `decompressMtz` 时才走默认纹理包」并在 `:230-231` 读 `globalThis.__VBSP_TEXTURES_MTZ_B64__`、`:235/:239` 交给注入的解压器 ⇒ **single 产物的内嵌纹理包是可用的**，该缺口**不成立**。**仍然成立的残项**：`mtzB64` 的 **Worker 通道**闲置——`src/ts-shared/auth/worker-dispatch.ts:297` 接受该键，而本工程无发送方。② **#5 行号复校**：favicon 两条声明实测在 `apps/game/web/index.html:22-23`（我原先记 23-24，差一行），已按实测更正；结论不变（`apps/game` 下无 favicon.ico ⇒ 两条 404）。 | `documents/plan/progress-log.md`（本行）、`apps/game/src/app.ts`、`src/ts-shared/phys/world-builder.ts`、`apps/game/web/index.html`（只读复核） |
| 2026-09-22 | **WG9 / `apps/debug` 子树（13 篇）交付并经主控独立复验 ⇒ WG9 三棵子树 39 篇全部完成** | **交付**：`documents/debug/{README,overview,sequences,differences}.md` + `implementation/{app,renderer,worker,input,world,physics,web,scripts,wasm-bindings}.md`。**主控复验（逐门重跑）**：13 篇全 `CRLF`、`bareLF=0`、无 BOM、均以 CRLF 结尾；`content-review` **147 路径 + 17 符号全命中 / bad=0**；`anchor-scan` **75 锚点 / 20 完整路径目标 / 违规 0**；`link-check` 0 缺失；禁用词 11 词 0 命中；`check-doc-drift`（全仓）exit 0。**锚点开箱抽样 65 条**（每篇 5 条）全部命中所述内容（例：`apps/debug/src/app.ts:2395 → replayCapture.record(...)`、`lod-manager.ts:262 → this.stats.pvsHidden = 0;`、`web/index.html:670 → <div id="cullStats">…`、`wasm.d.ts:80 → export class PhysWorld {`、`input-recorder.ts:297 → record(nowMs, dx, dy, keys, dtS = 0)`）。**交付方本次新发现 12 组（写入正文，供登记）**：① **页面缺的不是 7 个 id 而是 9 个**——除 `inputRec*` 七个（`apps/debug/src/app.ts:119`-`:125`）外还有 `pathVisibleChk`（`:112`，唯一消费者 `:710` ⇒ 监听永不触发）与 `pvsEnabled`（`:95`，消费者 `:376`/`:457`/`:1481`），而 `clearTeleportsBtn` **不是**缺失 id（由 `renderHTML` 动态生成 `:2059` 后再查询 `:2075`）**⇒ 更正本台账 #66③ 的「7 个」口径**；② **`apps/debug/web/styles.css` 全工程零引用**（页面既不 `<link>`、内联 `<style>` 亦无 `.health-log` 规则，`build-dist.mjs` 的 `KEEP_SINGLE`/`KEEP_MULTI` 都不含它）⇒ `#health-log` 的类无样式生效；③ `apps/debug/tsconfig.json:19` 起 5 个路径别名零导入；④ **`wasm.d.ts` 缺口比 #8/#9 更大**：`BspProcessor` 声明 13 个成员 vs Rust 24 个（另有两个自由导出 `export_visleaf_pvs`/`start` 未声明）；⑤ **死消息/死字段面**（`set-cull-distance` 无接收分支、`physics-event` 无生产者、`resize` 无发送点也无分支、`mode-ack` 未声明等 7 组）；⑥ **三段配置零读取点**（`config.movement`/`config.smoothing`/`config.teleport` 仍随 `syncFullConfig` 下发）；⑦ 零调用点批（`vec3.ts` 13 个函数、`spawn-loader.ts` 整模块、`TeleportManager` 六成员等）；⑧ **参数双源**：面板 `PARAM_DEFS.jumpHeight`（57）与 `config.physics.jumpSpeed`（302）经两条通路写同一个 Rust 键 `jump_height`，无交叉校验；⑨ 诊断覆盖只在模块初始化生效（`window.__vbsp*` 只在 uniform 初值处读一次）；⑩ `PathRecorder.addTick` 的 `residual` 恒传 `undefined` ⇒ 残差统计样本恒 0；⑪ 脚本侧（`jump-apex-verify.mjs` 头注自引用行号失效、`input-replay-verify.mjs` 无任何 npm/CI 入口等）；⑫ debug 侧 `.cmd` 与 #69①⑤ 同型。 | `documents/debug/**` |
| 2026-09-22 | **⚠ 交付面重要发现 + B2 合规实测：三棵子树的 12 篇顶层文档落在 HEAD 旧文档的同名路径上** | **发现**：`git status` 显示 `documents/{debug,game,viewer}/{README,overview,sequences,differences}.md` 共 **12 篇为 `M`（已修改）而非未跟踪** ⇒ 这些路径在 **HEAD 里存在旧文档**（例：`git show HEAD:documents/debug/README.md` 首行是「# WebSurf（Debug Build，调试测试页面）」），即「旧文档树已删除」这一状态**只存在于工作区、未提交**；三个子代理写新稿时正是落在这些同名路径上。**另实测**：旧 `implementation/*.md` 共 **17 个路径仍为 `D`（debug 3 / game 10 / viewer 4，未被重建——符合 B2：不得据旧文档重建）**，新增 `implementation/*` **27 篇为未跟踪**。**B2 合规实测（主控自建 `.tmp/cap/b2-check.mjs`）**：把 12 个路径的 HEAD 旧文**实质行**（去空行、去短行与纯标记行，共 **789 行**）与新稿逐行做集合比对，结果 **12/12 全部 `逐字命中 0`、复用率 0.0%** ⇒ `B2-CLEAN`，即新稿确为按当前代码重写、**未复用旧文档任何一行**。**主控裁决（答 debug 交付方的提问）**：旧 `implementation/{loading-pipeline,physics-panel,rendering}.md` **不重建**（B2 明文禁止据旧文档重建；WG10 索引按**实际存在的文件**生成，不为已删路径留位）。**对后续动作的影响**：① `verify.ps1` / `content-review` 不受影响（前者不用于 `.md`，后者只查存在性）；② 但**任何以「工作区已删 = HEAD 已删」为前提的推断都必须停用**——本轮已按实测纠正该前提。 | `.tmp/cap/b2-check.mjs`、`documents/{debug,game,viewer}/**`、`AGENTS.md` |
| 2026-09-22 | **WG11 全量复检完成（主控自做，静止期一次跑满）：四类判据全绿，WG11 判定通过** | **清单口径**：`git status --porcelain -uall` 实测已落地文件 **249 个**（`.ts` 98 + `.md` 53 + `.mjs` 44 + `.rs` 37 + `.cmd` 9 + `.html` 4 + `.css` 2 + `.py` 1 + `.gitignore` 1）。**① 代码类**：`verify.ps1` 分 5 批（40/40/40/40/19）覆盖 **179 件**（`.ts`+`.mjs`+`.rs`），**失败 0 / 179**；`strict` 的差异只出现在「行尾注释改写」（实例：`apps/debug/crates/wasm/src/lib.rs` d2、`apps/debug/scripts/build-dist.mjs` d8、`apps/debug/scripts/frame-bench.mjs` d2、`apps/debug/scripts/input-replay-verify.mjs` d2），按 `stripTrail` 全 `d0` 判 OK；禁用词/禁用路径一律 ≤ 基线。**② 文档类**：`check-doc-drift` 全仓 **58 篇 md ｜ 行数声明 2（漂移 0）｜锚点 2909（越界 0）｜路径失效 0 ｜歧义 27**（跨工程裸文件名，非错误），`exit 0`；`anchor-scan` 改动文档 **锚点 77 / 完整路径目标 21 / 违规 0**；`content-review` **仓内路径 401 + 路径+符号 54 全命中 `bad=0`**；`link-check` 0 缺失。**③ 资产类**：`py-proof` `PY-CODE-IDENTICAL`（真实代码行 41/41）；`cmd-proof` **9/9** `diff=0 nonASCII=0`；`html-struct-proof` **4/4** 标签序列逐项相同（`apps/debug/scripts/pages-index.html` 另报 `HTML-MARKUP-DIFFERS`＝本次有意改写的文案，标签序列与属性值多重集均同）；`css-proof` **2/2** `CSS-CODE-IDENTICAL`。**④ 编译类**：`cargo check -p websurf-phys` exit 0；`cargo test -p websurf-phys` exit 0（**10 passed / 0 failed**，另有 1 个 0 测试目标；两次独立取 `$LASTEXITCODE` 均为 0）；三工程 `npm run typecheck` **3/3** exit 0。**⑤ 行尾面**：`.tmp/tools/eol-sweep.mjs` 全量 `scanned=249 problems=2`，两处均为 HEAD 既有行尾空白，按基线保留。 | `.tmp/tools/wg11-check.ps1` 复检日志与 `.tmp/gate/` 下的分步日志 |
| 2026-09-22 | **⚠ WG11 首轮的假绿与更正（主控自做）：`git status --porcelain` 的目录塌缩使 7 个目录的文档整轮漏扫；另实修 1 处真缺陷** | **现象**：首轮扫描吐出 7 行形如 `BAD documents/norms/ CRLF=node:fs:732 bareLF=return BOM=binding.read(fd,` 的「失败行」。**实测成因**：不带 `-uall` 时 git 把**完全未跟踪的目录**折叠成一条目录路径，脚本把它当文件读 ⇒ `readFileSync` 抛 `EISDIR`，异常文本恰被格式化进计数位（假失败）；更重的是这 7 个目录（`documents/` 下的 `architecture`、`materials`、`norms`、`phys`、`plan`、`ts-shared`、`wasm-core`）内的**全部 `.md` 一整轮没被扫过**（含 WG8 共享层 5 篇与计划三篇）。**更正**：新建 `.tmp/tools/eol-sweep.mjs`（`-uall` + 目录递归 + `-PathType Leaf` 过滤，输出 `scanned=N problems=M`）重扫 ⇒ `scanned=249 problems=2`，两处均为 HEAD 既有尾空白（`src/wasm-core/bsp_to_gltf_core/convert.rs` 58 处、`src/wasm-core/texture_utils/vtf.rs` 1 处，与 `verify.ps1` 基线 `58/58`、`1/1` 一致 ⇒ 不清除）。**实修 1 处真缺陷**：本轮新写的 `documents/phys/overview.md` 第 56 行表格行尾多一个空格（写盘工具产物），已清（`\r\n` 未动、`bareLF=0`、无 BOM），并重过 4 道门（漂移 `0/0/0`、`content-review` 18+3 `bad=0`、`link-check` OK）。**新陷阱已入规范篇**：`documents/norms/annotation-and-verification.md` 陷阱第 **16** 条（目录塌缩 + `EISDIR` 伪装成计数 + 清单一律 `-uall` + 尾空白同 `verify.ps1` 按基线判）。 | `.tmp/tools/eol-sweep.mjs`、`documents/norms/annotation-and-verification.md`、`documents/phys/overview.md` |
| 2026-09-22 | **⚠ 补检发现并修复：`documents/plan/progress-log.md` 此前从未被 `content-review` 查过（WG11 首轮的目录塌缩把它整篇漏掉）；逐条判读后修掉 13 处路径文本 + 2 处不存在的符号名** | **实测**：直接对台账跑内容审查 ⇒ 仓内路径 `checked=327 ok=297 bad=30`、路径+符号 `checked=74 ok=68 bad=6`。**已修 ①** 12 处「把 app 相对路径写成仓内路径」的简写：WG4 行的 `src/app.ts` → `apps/debug/src/app.ts`；WG6b 行的 `test/{node-shims.d.ts,replay-selftest.ts,smoke-cdp.mjs}` → `apps/viewer/test/…`；WG12 game 行的 `src/{app.ts,panel/panel-controller.ts}` → `apps/game/…`；viewer 行的 `src/{wasm.d.ts,worker/main.ts}` → `apps/viewer/…`；三工程同构件的 `src/renderer/lightmap-shader.ts` → `apps/{debug,game,viewer}/src/renderer/lightmap-shader.ts`（两处，实测三份副本都在）。**已修 ② 两处符号名实测不存在**：`#64②` 的 `KEY_DEFS` 在 `apps/viewer/src/ui/telemetry.ts` 里**不存在**——真实标识符是 `KEYS`（`readonly KeyDef[]`，8 项，元素类名 `tm-key {cls}`），「渲染 8 个 `.tm-key`」这一实质断言成立（`apps/viewer/test/smoke-cdp.mjs` 断言 6 键 ⇒ 仍为待裁决项）；`dist-README` 行引的「`apps/debug/src/input/keyboard.ts` 的 `control` 映射」应为 `ControlLeft`/`ControlRight`（两行均映射到 `duck`，故「默认蹲键是 Ctrl」的实质断言成立）。**已修 ③ 一处证据句写实**：`#69④` 原写「`apps/viewer/index.html` 实测不存在」改为「`apps/viewer/` 工程根无 `index.html`（dev 页在 `apps/viewer/web/index.html`；dist 入口 `index.html` 由 `apps/viewer/scripts/build-dist.mjs` 生成到 `apps/viewer/dist/`，该目录是构建产物、不入库）」，并复核实测 `apps/{debug,game}/play.cmd` 打开 `http://localhost:%PORT%/dist/index.html` 而 `apps/viewer/play.cmd` 打开 `…:%PORT%/index.html`。**残余 17 条路径 + 4 条符号判为「非当前断言」**：① **已删文件的历史引用 5 条**（行文本自带「已删 / 已退役」限定：WG2 行的 `documents/game/implementation/console-fix-contract.md`、条目 `#29` 的 `test/dual-mode-harness/src/worker-a.ts`、WG6 行的 `shavit-replay-format.md`、条目 `#21` 的 `lighting-merge-plan.md`、WG9 行的 `apps/game/favicon.ico`）；② **引用的错误写法样本 12 条**（正是 WG9 那轮 47 处 bad 的原样引用，行文本本身就在讲「把 app 相对路径当仓内路径」）；③ **工具名误配 4 条**（`verify.ps1` / `stripTrail` 这两个判据名被当成符号名）。**判读口径已写入规范篇 §4**（交付面文档按 `bad=0` 判；过程记录需按上述三类逐条判读），规范篇自身复核后 `content-review` **16 路径 + 4 符号 `bad=0`**、漂移 0/0/0、`link-check` OK。 | `documents/plan/progress-log.md`、`documents/norms/annotation-and-verification.md` |
| 2026-09-22 | **owner 裁决：清掉退役工程的残留引用（本轮唯一的代码 / CI 改动，授权明确、逐件附证据）** | **授权**：owner 在「是否处理退役工程残留引用」一项选择「处理：清掉这些残留引用」。**改动 6 个文件**：① `src/scripts/check-doc-drift.mjs` —— `scopeRoots` 删掉两处 `test/dual-mode-harness` 候选根；**行为不变**（改前改后漂移体检输出逐字相同：`58 篇 ｜ 行数声明 2（漂移 0）｜锚点 2909（越界 0）｜路径失效 0 ｜歧义 27`，exit 0）；② `src/scripts/check-shared-sync.mjs` —— `checkVmdlPatch` 的 `manifests` 删掉 `test/dual-mode-harness/Cargo.toml`，**门禁由「恒失败」转为全过**（改前实测 `❌ 门禁失败 1 项：[vmdl-patch] 缺少模块清单` + exit 1；改后 `✅ 四项子检查全部通过` + exit 0）；③ `apps/debug/scripts/jump-apex-verify.mjs` —— 两个产物常量由退役 harness 的 `pkg/websurf_test_wasm*.{js,wasm}` 改指**本工程自己的** `apps/debug/pkg/websurf_wasm.{js}` 与 `_bg.wasm`（先实测该 pkg 导出 `PhysWorld` + `initSync`，且脚本用到的 `set_hull` / `build_world` / `set_params` / `tick` / `state` / `set_state` / `set_velocity` 在 `apps/debug/pkg/websurf_wasm.d.ts` 的 `PhysWorld` 上**全部存在**），顺带删掉只为旧路径存在的 `REPO` 常量；**该门由「必然 SKIP 空转」变为实跑**——`npm run test:jump-apex` 实测 **exit 0、输出 198 行、`[SKIP]` 命中 0 次**、跑满 25 格（末格 240 Hz：飞行段 79、顶高中位 1.3、`>70HU` 0、死跳 53、land 26）；④ `.github/workflows/ci-gates.yml` —— 删掉 4 个 harness 步骤（`npm ci` / `build:wasm` / `build:ts` / `test:three-mode`），job 由 `harness-and-debug-gates`（显示名 `harness + debug gates`）改名为 `debug-gates`（`debug gates`），**全仓先确认无其它文件引用该 id**；文件头的「由来 / 门禁清单 / 已知未纳入」三段按现工况重写；判据 `.tmp/tools/yaml-check.mjs`（新建，复用 DSH checkout 内的 `yaml` 包，不新增仓内依赖）实测 **YAML 可解析**：jobs(4) = rust-unit-tests / debug-gates / game-gates / viewer-gates、steps **35 → 31**、每步都有 `run` 或 `uses`、无 tab；该文件是全仓唯一 **LF** 的 yml（全文件 `crlf=0`），**保持 LF 未强改**；⑤ `.github/workflows/deploy-pages.yml` —— 去掉不可复核的旧实测数字（harness 147 s、`run 0ec7769` 的 12.6 min 与四段编译耗时），职责边界与触发路径按现有 `paths:`（`apps/**`、`src/**`、`Cargo.toml`、`Cargo.lock`、本文件）与 `workflow_dispatch` 写实；`yaml-check` 实测可解析（jobs=2、steps=16）；⑥ `.github/PULL_REQUEST_TEMPLATE.md` —— 影响范围删「test（WebSurf-test）」、补「viewer（`apps/viewer`）」、共享层描述由「两端」改「三个工程」；测试项路径改为 `apps/{debug,game,viewer}` 与两端各一份的 `scripts/check-wasm-api.mjs`、`apps/game/scripts/phys-smoke.mjs`。**判据与判读**：三个 `.mjs` 过 `node --check`；两片 workflow 过 `yaml-check.mjs`；`verify.ps1` 对三个 `.mjs` 报「代码部分不同」**属预期**（本轮授权改代码），逐行核对差异**只含被删的退役引用与被改指的代码行**（`check-doc-drift` stripTrail d3、`check-shared-sync` 单行删除、`jump-apex-verify` stripTrail d11），且禁用路径计数**下降**（`jump-apex-verify` 3/基线3 → 0/基线3）。**全仓复扫**（321 个文件，排除过程记录 `documents/**` 与 `AGENTS.md`）后仅剩**一处**：根 `.gitignore` 的 `test/game-core/` 规则 —— **刻意保留**（见下一行的口径说明）。 | 见左列 6 个路径 |
| 2026-09-22 | **口径说明：根 `.gitignore` 的 `test/game-core/` 规则为何保留** | 该规则属**配置项**而非注释，删除它会改变 gitignore 语义；owner 对配置面的授权是「重编注释」（配置文本须逐字符不变），故本轮**只改注释、保留规则**，并把注释改写为当前事实：`# 该规则排除 test/game-core/：当前工作区无该目录，规则保留作本地隔离实验工程位。`。同时根 `.gitignore` 的注释重编已实测**规则 52 行逐行未变**（`.tmp/cap/gitignore-rules-proof.mjs` 以编辑前快照逐行比对 ⇒ `GITIGNORE-RULES-IDENTICAL`）；该文件相对 HEAD 的**唯一**非注释差异是既有的 `+.ak/` 一行（本会话之前就已存在，非本轮所加）。若 owner 要求连规则一并删掉，属**配置改动**，请示后再动。 | `.gitignore`、`.tmp/cap/gitignore-rules-proof.mjs` |
| 2026-09-22 | **owner 裁决：配置面纳入注释重编 —— 9 个 `Cargo.toml` + 5 个 `.gitignore` 全部完成（件数由「13」更正为「14」）** | **授权**：owner 选择「纳入：按现工况重编这 13 个文件的注释」。**实测件数更正**：`.gitignore` 实为 **5 个**（根、`apps/{debug,game,viewer}` 各一、**外加 `src/.gitignore`**——此前记「4 个」时漏掉 `src/` 那份），`Cargo.toml` 9 个、`tsconfig.json` 3 个（0 注释、无对象）不变 ⇒ 本组共 **14 个文件**。**执行**：9 个 `Cargo.toml` 由两个子代理分批交付、**主控逐件独立复验**；5 个 `.gitignore` 由主控自做。**判据**：`config-proof.mjs`（剥 `#` 注释 + 行尾归一后与 `git show HEAD:<path>` 逐字符比对）⇒ 9 个 `Cargo.toml` **全部「配置文本逐字符相同」**（配置字符 187/173/173/173/1595/1595/517/382/1317 **前后完全一致**）、4 个 app/src `.gitignore` 同样逐字符相同；根 `.gitignore` 报 DIFF，但**差异恰是本轮之前既有的 `+.ak/` 一行**（该文件规则 **52 行**经编辑前快照逐行比对 ⇒ `GITIGNORE-RULES-IDENTICAL`）。`eol-sweep` 14 件 `problems=0`；禁用词 / 禁用路径 / 注释内行号三项全 0。**被实测推翻的旧注**：① 三个 `apps/*/crates/wasm/Cargo.toml` 的「模块结构」头注把 `src/vbsp/`、`src/bsp_to_gltf_core/`、`src/model_integrator/`、`src/texture_utils/`、`pakfile_models.rs` 写在**各自 crate 内**——实测这些 crate 目录下**只有 `Cargo.toml` 与 `src/lib.rs`**，上述模块全在共享 crate `src/wasm-core/`；② 根与 `src/wasm-core` 头注的「**四个**模块 wasm crate 同 workspace」——实测模块 wasm crate **三个**且**都不在根 workspace**（根 `members = ["src", "src/wasm-core"]`、`target/` 在仓库根；三工程各自成 workspace 并各有 `Cargo.lock` 与 `target/`）；③ `apps/viewer/Cargo.toml` 的「与 `websurf-phys` 同一 wasm-bindgen 主版本」——实测 **`websurf-phys` 不在 `apps/viewer/Cargo.lock`**（222 包）内；④ 同件「PAKFILE 模型解析（`load_vmdl`）」——`load_vmdl` 全仓只在 debug / game 的导出层（`apps/debug/crates/wasm/src/lib.rs:261`、`apps/game/crates/wasm/src/lib.rs:264`）；⑤ `src/wasm-core/Cargo.toml` 的「model-integrator / pakfile-models 共用」——`src/wasm-core/pakfile_models.rs` 只 `use std::collections::HashMap`（外加函数内局部的 `super::parse_vmt`），vmdl 的消费点是 `model_integrator`、vtf / vmt-parser / tf-asset-loader 的消费点在 `bsp_to_gltf_core`。**删除的不可核断言**：`NODE_OPTIONS` / `--use-system-ca`（全仓 grep 只命中该注释自身）、`2026-08 实测`、`wasm-opt 通常再省 5-15%`、`2026-09-06 合并`、`t3` 标签、`origin[2] 10.478655362066775`、`DDS`、「减小体积」、「无 /dev/urandom」、「40+ 处产物名」。**刻意保留的既有配置项**：根 `.gitignore` 的 `test/game-core/` 规则（按「只改注释」口径保留，见上一行）。**新登记待裁决 2 条**：① `src/vendor/vmdl/Cargo.toml` 第 8 行称该 patch「通过**两端**工程 workspace 根引用」且路径写作 `../src/vendor/vmdl`——实测为 **4 处**声明（根 + 三工程）、三工程内写作 `../../src/vendor/vmdl`；该文件属 §7.2 WG12 ⑩ 明确排除的 vendored 树，**未改**；② 三个 app 的 `crates/wasm` 依赖表里多项在本 crate 无使用点（game 的 `js-sys` / `md5` / `serde-wasm-bindgen`、viewer 的 `gltf` 等），保留原因在仓内不可定位 ⇒ 是否属配置冗余待 owner 裁决（**未动任何依赖行**）。 | 9 个 `Cargo.toml` + 5 个 `.gitignore` |
| 2026-09-22 | **静止期末轮全量复检（主控自做）：`-uall` 口径下再抓出 1 篇交付面文档的 4 处路径断言并修复；同时加固行尾扫描对「入库即 LF」文件的假阳性** | **① 抓出并修复**：`-uall` 口径让 `documents/architecture/overview.md`（此前被目录塌缩整篇漏查）首次进入 `content-review`，抓出 **4 处**不合格——`src/ts-shared/phys/angles.js`（实为 ESM import 说明符；来源文件是 `src/ts-shared/phys/angles.ts`，实测调用点 `apps/debug/src/world/spawn-loader.ts:17`）与 3 处 app 相对路径（`src/renderer/lightmap-shader.ts`、`src/renderer/renderer-main.ts`、`src/app.ts`）⇒ 已改写为「import 说明符 + 来源文件」与 `apps/{debug,game,viewer}/…`、`apps/<app>/…` 形式；改后该篇 `content-review` **15 路径 + 1 符号 `bad=0`**、漂移 `0/0/0`、`anchor-scan` 违规 0、`link-check` 0 缺失。**② 工具加固**：`.tmp/tools/eol-sweep.mjs` 增加「HEAD 版本行尾」判据——全 LF 文件只在「HEAD 是 CRLF（本轮写坏）」或「新增文件」时判失败，**入库即 LF** 的文件改报 `INFO`（实测全仓 **1 个**：`.github/workflows/ci-gates.yml`，本轮**保持其 LF 未强改**）；同时修掉纯 LF 文件被 `bareLF` 误判的问题。**③ 末轮全量数字**（**265** 个落盘路径）：`py-proof` / `cmd-proof` 9/9 / `html-struct-proof` 4/4 / `css-proof` 2/2 四项资产证明全绿；`check-doc-drift` **58 篇 ｜ 行数声明 2（漂移 0）｜锚点 2900（越界 0）｜路径失效 0 ｜歧义 27**、`anchor-scan` **79 锚点 / 22 完整路径目标 / 违规 0**、`link-check` **89 篇 / 63 条相对链接 / 0 缺失**、`content-review`（54 篇改动 md）**仓内路径 874（850 ok / 24 bad）+ 路径与符号 154（149 ok / 5 bad）**——这 **24 + 5** 条**全部**落在台账 `documents/plan/progress-log.md`，且逐条属已定性的三类**非当前断言**（① 已删文件的历史引用 10 条、② 引用的错误写法样本 14 条、③ 工具名/旧符号名误配 5 条，判读口径见规范篇 §4）；`cargo check -p websurf-phys` 与 `cargo test -p websurf-phys` exit 0（10 passed）、三工程 `npm run typecheck` **3/3** exit 0；`verify.ps1` 分批覆盖 **179 件**，**失败 3 件**＝本轮 owner 授权改动的三个 `.mjs`（差异逐行核对只含退役引用的删除与改指），其余 176 件全过；行尾扫描 `scanned=265 problems=2`（两处为 HEAD 既有尾空白，与 `verify.ps1` 基线 `尾空白=58/基线58`、`1/基线1` 一致 ⇒ 不清除）。**④ 结论**：文档 / 注释重编范围**已全部完成**，无未处置的交付面失败项；剩余只有 §7.3 索引里按「只记录不修」口径保留的**疑似代码缺陷待裁决项**。 | `.tmp/tools/eol-sweep.mjs`、`documents/architecture/overview.md`、`.tmp/gate/final.txt` |
| 2026-09-22 | **修复排除范围外的那处配置注释：`src/vendor/vmdl/Cargo.toml` 的「两端工程」错误说法；并补全 5 个 manifest 对副本差异的描述** | **问题（实测）**：该 vendored manifest 原写「通过**两端**工程 workspace 根 Cargo.toml 的 `[patch.crates-io] vmdl = { path = "../src/vendor/vmdl" }` 引用」——实测 `[patch.crates-io]` 声明共 **4 处**，路径写法两种：仓库根 `Cargo.toml` 的 `[patch.crates-io]` 段为 `vmdl = { path = "src/vendor/vmdl" }`，`apps/debug/Cargo.toml:14`、`apps/game/Cargo.toml:14`、`apps/viewer/Cargo.toml:13` 均为 `vmdl = { path = "../../src/vendor/vmdl" }`。**修法**：只改注释（配置文本逐字符不变），把 4 处声明与两种路径写法如实写入该 manifest 头注。**同时纠正一处「报告级」不完整**：先前记录称副本「只改写了 `Strip::indices`」——主控本轮**自行从 `~/.cargo/registry/cache/index.crates.io-1949cf8c6b5b557f/vmdl-0.2.0.crate` 解包上游**并逐文件比对（上游 35 个文件 vs 本地 17 个），实测**代码级差异是 2 个文件**：① `src/vendor/vmdl/src/vtx/mod.rs` 的 `Strip::indices`（`0..len` → `0..len.saturating_sub(2)`、`[idx, idx+1-cw, idx+2-cw]` → `[idx, idx+1+cw, idx+2-cw]`）；② **`src/vendor/vmdl/src/lib.rs` 的 2 处返回类型生命周期标注**（`impl Iterator<Item = SkinTable>` → `SkinTable<'_>`、`Item = Mesh` → `Mesh<'_>`）。另实测**未 vendor 的 19 个上游文件**（examples / benches / tests / CI / flake / README / 截图 / `Cargo.lock` / `.cargo_vcs_info.json` / `Cargo.toml.orig` 等）、manifest 由上游 **95 条配置行**精简为 **23 条**（去掉 `[[example]]`×3 / `[[test]]` / `[[bench]]`×2 / `[profile.dev.package."*"]` / 全部 `[dev-dependencies.*]`），本地另有上游包内没有的 `LICENSE`。⇒ **5 个 manifest 的注释已一并改写为完整描述**（vendored + 仓库根 + 三工程；根件同时清掉上一轮留下的断裂句）。**判据**：`config-proof` **5/5「配置文本逐字符相同」**（配置字符 503/187/173/173/173 前后一致）；`content-review` **10 仓内路径 + 4 路径与符号全命中**——首轮这些路径**不带反引号**导致工具 `checked=0`（**门盲区**），已给路径加反引号使其进入可核验范围；`eol-sweep` 5 件 `problems=0`；`cargo metadata` 5 个 manifest 全部 **exit 0**。 | `src/vendor/vmdl/Cargo.toml`、`Cargo.toml`、`apps/debug/Cargo.toml`、`apps/game/Cargo.toml`、`apps/viewer/Cargo.toml` |
| 2026-09-22 | **「app crate 依赖表多项在本 crate 无使用点」升级为编译器级实测清单（两种独立方法结果一致；未动任何配置行）** | **方法 ①（源码引用面，主控自写 `.tmp/cap/deps-usage.mjs`）**：解析各 `apps/*/crates/wasm/Cargo.toml` 的 `[dependencies]`，在该 crate 自己的 `src/**` 内查 `use X` / `X::` / `X!` / `extern crate X` / `#[X…]` 形态。**方法 ②（编译器权威）**：`RUSTFLAGS='-W unused-crate-dependencies' cargo check --target wasm32-unknown-unknown`（三工程各跑一次，**exit 0** ⇒ 该 lint 不影响构建）。**两法结果完全一致**：debug `websurf-wasm` **26 项**、game `websurf-wasm` **29 项**（＝debug 的 26 项 + `js-sys` / `md5` / `serde-wasm-bindgen`）、viewer `websurf-viewer-wasm` **1 项**（`gltf`）。**方法 ② 另给出两条独立发现**：`websurf-wasm-core` 自身 **3 项**无引用点（`getrandom` / `getrandom_03` / `path_dedot`）、vendored `vmdl` **1 项**（`tracing`；**上游 manifest 同样声明该行** ⇒ 非本仓引入，已在 `src/vendor/vmdl/Cargo.toml` 注释里如实注明「按上游原样保留」）。**可核事实（供裁决）**：debug 的 26 项**全部**也在共享层 `src/wasm-core/Cargo.toml` 的依赖表（30 项）里；game 的 29 项中有 26 项同样在共享表内，仅 `js-sys` / `md5` / `serde-wasm-bindgen` 不在。**判读与建议（未改配置）**：lint 判「无引用点」**不等于可删**——`getrandom` / `getrandom_03` / `path_dedot` 这类声明的作用是**打开 feature**（本仓 4 个 manifest 的注释本轮已写明该用途），删除会改变 feature 统一结果；如需瘦身，建议**逐项删除 + 每次以 `cargo check` 与 `npm run build:wasm` 为证据**，而不是整批删。**状态**：登记为待 owner 裁决（AGENTS §7.3 索引 #70）。 | `.tmp/cap/deps-usage.mjs`、`.tmp/gate/unused-{debug,game,viewer}.txt` |
| 2026-09-22 | **根 `.gitignore` 的 `test/game-core/` 规则：复核后维持「保留规则 + 注释写实」** | 复核实测：第 55 行注释为「该规则排除 `test/game-core/`：当前工作区无该目录，规则保留作本地隔离实验工程位」，第 56 行规则仍生效（`git check-ignore -v test/game-core/` 解析到 `.gitignore:56`），`Test-Path test/game-core` = False。**裁决依据**：该行是**配置项**而非注释，删除会改变 gitignore 语义，而它正是「本地隔离实验工程不入库」的机制；owner 对配置面的授权是「重编注释、配置文本逐字符不变」⇒ **未删**。若 owner 要求连规则一并去掉，属配置改动，需明确指示。 | `.gitignore` |
