{
  "name": "websurf-test",
  "version": "0.1.0",
  "description": "WebSurf-test — 双模物理 + OffscreenCanvas 渲染时序验证工程（主线程仅输入转发/UI → SAB 无锁 → WorkerA 双模物理 → WorkerB 渲染）。",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build:wasm": "cd crates/wasm && wasm-pack build --release --target web --out-dir ../../pkg && node -e \"require('fs').copyFileSync('../../pkg/websurf_test_wasm_bg.wasm','../../websurf_test_wasm_bg.wasm')\"",
    "typecheck": "tsc --noEmit",
    "build:ts": "npm run typecheck && esbuild src/main.ts --bundle --outfile=app.js --format=esm --target=es2022 && esbuild src/worker-a.ts --bundle --outfile=worker-a.js --format=esm --target=es2022 && esbuild src/worker-b.ts --bundle --outfile=worker-b.js --format=esm --target=es2022",
    "build": "npm run build:wasm && npm run build:ts",
    "build:dist": "node scripts/build-dist.mjs",
    "dev": "python ../src/serve.py 8080 .",
    "check:api": "node scripts/check-wasm-api.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/three": "^0.165.0",
    "esbuild": "^0.23.0",
    "typescript": "~5.7.2"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "three": "^0.165.0"
  }
}
