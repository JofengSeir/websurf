# WebSurf 仓库级 .gitignore
#
# 收录范围（2026-09-22 按现工况精简）：① 当前工作区里**真实存在**且不入库的产物；
# ② 本仓脚本**确实会生成**的产物（规则上方注明生成者）。空转规则已删除。
# 分层：各工程/共享层另有自身 .gitignore 补充特有排除；规则按「后出现的覆盖先出现的」排序。

# ── 构建产物（生成者：cargo / npm / wasm-pack / esbuild 与各工程 scripts/build-dist.mjs）──
**/dist/
**/pkg/
**/target/
**/node_modules/
**/web/app.js
**/web/worker.js
**/web/websurf_wasm_bg.wasm
# 注意：**/web/textures.mtz 不在此列——apps/debug/web/textures.mtz 与 apps/game/web/textures.mtz 是
# 公共默认纹理包 src/materials/textures.mtz 的副本（跟踪，服务运行必需）；apps/viewer 无该副本。

# ── Cargo / wasm-pack 缓存（生成者：src/scripts/cargo-env.cmd 的 CARGO_HOME / WASM_PACK_CACHE）──
**/.cargo-home/
**/.wasm-pack-cache/

# ── 临时 / 验证产物（生成者：本仓脚本默认写 .tmp/；各工程 temp/ 存一次性构建日志）──
**/.tmp/
**/temp/
# 生成者：apps/debug/scripts/plot-path.mjs（输出目录为 <输入 json 目录>/plot-<输入基名>/）
**/plot-*/

# ── Python 运行期（生成者：python src/serve.py，见各工程 start-dev.cmd 与 play.cmd）──
__pycache__/
*.pyc

# ── test/ 夹具目录：只上传目录本身（.gitkeep 占位），内容留在本地不入库 ──
# 地图与录像体积大（test/maps 下单个最大 144 MB，超 GitHub 单文件 100 MB 上限），且属本地数据
test/maps/*
!test/maps/.gitkeep
test/replay/*
!test/replay/.gitkeep
*.bsp
*.dem
*.replay

# ── Agent 工作记忆（非文档树、不入库）──
**/.workbuddy/
