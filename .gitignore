# WebSurf 仓库级 .gitignore（分层：各工程另有自身 .gitignore 补充特有排除）
#
# 注意 gitignore 语义：
# - 含内部斜杠的模式（如 web/app.js）只匹配仓库根层级 → 需用 **/ 前缀覆盖任意层级
# - 无斜杠的模式（如 dist/、target/）匹配任意层级
# - 后出现的规则覆盖先出现的（地图区块依此排序）

# ── 构建产物（覆盖 debug/ 与 game/）──────────
# 运行时 app.js/worker.js/wasm 均为构建产物：克隆后需 npm run build:wasm + build:ts 再生成
**/dist/
**/pkg/
**/web/app.js
**/web/worker.js
**/web/websurf_wasm_bg.wasm
# 注意：**/web/textures.mtz 不在此列——该文件是公共默认纹理包的副本（跟踪，服务运行必需）

# ── Rust / Cargo ─────────────────────────
**/target/
**/.cargo-home/
**/.tmp/
**/.wasm-pack-cache/
**/.wasm-bindgen/
# Cargo.lock 保留提交（debug/game/materials-mini 为应用项目，锁定依赖版本保证可复现构建；
# 共享层 src/ 的 Cargo.lock 由 src/.gitignore 忽略——其依赖版本由两端 workspace lock 解析）

# ── Node ─────────────────────────────────
**/node_modules/

# ── Python（src/serve.py 与各脚本工具链）────
__pycache__/
*.pyc

# ── 地图 / 录像 / 演示数据 ────────────────
# BSP 地图体积大（surf_666.bsp 75MB 超 GitHub 50MB 推荐限制），一律不入库；
# 本地数据统一放 test/maps/（仓库根 maps/ 已废弃），需要分享走网盘/直链。
*.bsp
*.dem
*.replay
maps/*
test/maps/

# 例外：CI 验收夹具（debug/scripts/path-acceptance.mjs 的输入）必须入库——
# 它是"修复前最差真实录制"，用于给 tick→render 垂距验收门留一条可复现的失败基线。
# 放在地图区块之后：gitignore 后出现的规则覆盖先出现的（同上文排序约定）。
!debug/fixtures/path/
!debug/fixtures/path/*.json

# ── 临时 / 验证产物（任意层级）─────────────
# **/temp/：各工程 temp/ 一次性试验产物（如 game/temp 的 wasm 备份/bench json）；
# 注意根级 temp/* 不含内部斜杠只匹配根，必须用 **/temp/ 才能覆盖子工程。
**/temp/
temp_*
.tmp-*
*.glb
.dsh-shots/
**/scripts/verify-*
**/scripts/cdp-*
# plot-path.mjs 的 PNG 输出目录（落在输入 json 同目录，如 debug/.tmp/.../plot-xxx/）
**/plot-*/
*.tsbuildinfo
*.bak
*~
*.orig
*.rej

# ── 工具缓存 / 归档产物 ───────────────────
**/coverage/
**/.cache/
.eslintcache
*.zip
*.7z
*.mp4
*.mov

# ── 编辑器 / 系统 ────────────────────────
**/.agent-teams/
**/.code-review/
**/.team-docs/
**/.workbuddy/
**/.vscode/
**/.idea/
*.log
.DS_Store
Desktop.ini
Thumbs.db
