# websurf — Rust workspace 根
#
# 单一 WASM crate（crates/wasm），负责 BSP 解析、碰撞体导出、GLB 生成、
# 传送点/PVS 数据解析等全部编译到 WebAssembly 的 Rust 逻辑。

[workspace]
members = ["crates/wasm"]
resolver = "2"

# ============================================================
# 性能优化配置（WASM 构建需启用 LTO + 优化）
# ============================================================
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
